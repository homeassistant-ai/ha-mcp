# Codex (OpenAI) - Accuracy: 3/5

## Accuracy Rating: 3/5
**Consistent across 5-8 repos** - TOML format unique.

## Summary
Codex uses TOML configuration format, different from other clients.

## Verified Configuration

### Config Location
- `~/.codex/config.toml`

### TOML Format
```toml
[mcp_servers.server-name]
command = "npx"
args = ["@package/mcp-server"]

[mcp_servers.server-name.env]
API_KEY = "your-key"
```

### HTTP Remote
```toml
[mcp_servers.server-name]
url = "https://server.com/api/mcp"
```

### CLI Commands
```bash
codex mcp add <name> <command> [args...]
codex mcp list
codex mcp remove <name>
```

## Key Notes
- TOML format (unique among MCP clients)
- Both CLI and file-based configuration
- Supports stdio and HTTP transports

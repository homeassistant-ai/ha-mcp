name: "Home Assistant MCP Server"
description: "AI assistant integration for Home Assistant via Model Context Protocol (MCP)"
version: "7.2.0"
slug: "ha_mcp"
url: "https://github.com/homeassistant-ai/ha-mcp"
arch:
  - aarch64 # 64-bit ARM (Raspberry Pi 4+, modern ARM devices)
  - amd64 # 64-bit x86 (most PCs and servers)
  # Note: 32-bit platforms (armhf, armv7, i386) not supported
  # uv base image only provides amd64 and arm64 builds
init: false
startup: application
boot: manual
# Enable access to Supervisor API for auto-discovery
hassio_api: true
hassio_role: default
# Enable access to Home Assistant API
homeassistant_api: true
# Enable host network mode for local network access
host_network: true
# Use pre-built Docker images
image: "ghcr.io/homeassistant-ai/ha-mcp-addon-{arch}"
# Options for user configuration
options:
  backup_hint: "normal"
  enable_skills: true
  enable_skills_as_tools: true
  enable_tool_search: false
  enable_yaml_config_editing: false
  tool_search_max_results: 5
  # --- GENERATED TOOL CONFIG START (do not edit) ---
  tools_add-ons:
    enabled: true
    ha_call_addon_api: "enabled"
    ha_get_addon: "enabled"
  tools_areas_floors:
    enabled: true
    ha_config_list_areas: "enabled"
    ha_config_list_floors: "enabled"
    ha_config_remove_area: "enabled"
    ha_config_remove_floor: "enabled"
    ha_config_set_area: "enabled"
    ha_config_set_floor: "enabled"
  tools_automations:
    enabled: true
    ha_config_get_automation: "pinned"
    ha_config_remove_automation: "enabled"
    ha_config_set_automation: "pinned"
  tools_blueprints:
    enabled: true
    ha_get_blueprint: "enabled"
    ha_import_blueprint: "enabled"
  tools_calendar:
    enabled: true
    ha_config_get_calendar_events: "enabled"
    ha_config_remove_calendar_event: "enabled"
    ha_config_set_calendar_event: "enabled"
  tools_camera:
    enabled: true
    ha_get_camera_image: "enabled"
  tools_dashboards:
    enabled: true
    ha_config_delete_dashboard: "enabled"
    ha_config_delete_dashboard_resource: "enabled"
    ha_config_get_dashboard: "enabled"
    ha_config_list_dashboard_resources: "enabled"
    ha_config_set_dashboard: "enabled"
    ha_config_set_dashboard_resource: "enabled"
    ha_dashboard_find_card: "enabled"
  tools_device_registry:
    enabled: true
    ha_get_device: "enabled"
    ha_remove_device: "enabled"
    ha_rename_entity: "enabled"
    ha_update_device: "enabled"
  tools_entity_registry:
    enabled: true
    ha_get_entity: "enabled"
    ha_get_entity_exposure: "enabled"
    ha_set_entity: "enabled"
  tools_files:
    enabled: true
    ha_delete_file: "enabled"
    ha_list_files: "enabled"
    ha_read_file: "enabled"
    ha_write_file: "enabled"
  tools_groups:
    enabled: true
    ha_config_list_groups: "enabled"
    ha_config_remove_group: "enabled"
    ha_config_set_group: "enabled"
  tools_hacs:
    enabled: true
    ha_hacs_add_repository: "enabled"
    ha_hacs_download: "enabled"
    ha_hacs_info: "enabled"
    ha_hacs_list_installed: "enabled"
    ha_hacs_repository_info: "enabled"
    ha_hacs_search: "enabled"
  tools_helper_entities:
    enabled: true
    ha_config_list_helpers: "enabled"
    ha_config_remove_helper: "enabled"
    ha_config_set_helper: "enabled"
    ha_get_helper_schema: "enabled"
    ha_set_config_entry_helper: "enabled"
  tools_history_statistics:
    enabled: true
    ha_get_automation_traces: "enabled"
    ha_get_history: "enabled"
    ha_get_logs: "enabled"
    ha_get_statistics: "enabled"
  tools_integrations:
    enabled: true
    ha_delete_config_entry: "enabled"
    ha_get_integration: "enabled"
    ha_set_integration_enabled: "enabled"
  tools_labels_categories:
    enabled: true
    ha_config_get_category: "enabled"
    ha_config_get_label: "enabled"
    ha_config_remove_category: "enabled"
    ha_config_remove_label: "enabled"
    ha_config_set_category: "enabled"
    ha_config_set_label: "enabled"
  tools_scripts:
    enabled: true
    ha_config_get_script: "enabled"
    ha_config_remove_script: "enabled"
    ha_config_set_script: "enabled"
  tools_search_discovery:
    enabled: true
    ha_deep_search: "enabled"
    ha_get_overview: "pinned"
    ha_get_state: "enabled"
    ha_get_states: "enabled"
    ha_search_entities: "pinned"
  tools_service_device_control:
    enabled: true
    ha_bulk_control: "enabled"
    ha_call_service: "enabled"
    ha_get_operation_status: "enabled"
    ha_list_services: "enabled"
  tools_system:
    enabled: true
    ha_backup_create: "pinned"
    ha_backup_restore: "pinned"
    ha_check_config: "enabled"
    ha_config_set_yaml: "pinned"
    ha_get_system_health: "enabled"
    ha_get_updates: "enabled"
    ha_reload_core: "pinned"
    ha_restart: "pinned"
  tools_todo_lists:
    enabled: true
    ha_add_todo_item: "enabled"
    ha_get_todo: "enabled"
    ha_remove_todo_item: "enabled"
    ha_update_todo_item: "enabled"
  tools_utilities:
    enabled: true
    ha_eval_template: "enabled"
    ha_install_mcp_tools: "enabled"
    ha_report_issue: "pinned"
  tools_zones:
    enabled: true
    ha_get_zone: "enabled"
    ha_remove_zone: "enabled"
    ha_set_zone: "enabled"
  # --- GENERATED TOOL CONFIG END ---
schema:
  backup_hint: list(strong|normal|weak|auto)
  secret_path: str?
  enable_skills: bool?
  enable_skills_as_tools: bool?
  enable_tool_search: bool?
  enable_yaml_config_editing: bool?
  tool_search_max_results: int?
  # --- GENERATED TOOL SCHEMA START (do not edit) ---
  tools_add-ons:
    enabled: bool?
    ha_call_addon_api: "list(enabled|pinned|disabled)?"
    ha_get_addon: "list(enabled|pinned|disabled)?"
  tools_areas_floors:
    enabled: bool?
    ha_config_list_areas: "list(enabled|pinned|disabled)?"
    ha_config_list_floors: "list(enabled|pinned|disabled)?"
    ha_config_remove_area: "list(enabled|pinned|disabled)?"
    ha_config_remove_floor: "list(enabled|pinned|disabled)?"
    ha_config_set_area: "list(enabled|pinned|disabled)?"
    ha_config_set_floor: "list(enabled|pinned|disabled)?"
  tools_automations:
    enabled: bool?
    ha_config_get_automation: "list(enabled|pinned|disabled)?"
    ha_config_remove_automation: "list(enabled|pinned|disabled)?"
    ha_config_set_automation: "list(enabled|pinned|disabled)?"
  tools_blueprints:
    enabled: bool?
    ha_get_blueprint: "list(enabled|pinned|disabled)?"
    ha_import_blueprint: "list(enabled|pinned|disabled)?"
  tools_calendar:
    enabled: bool?
    ha_config_get_calendar_events: "list(enabled|pinned|disabled)?"
    ha_config_remove_calendar_event: "list(enabled|pinned|disabled)?"
    ha_config_set_calendar_event: "list(enabled|pinned|disabled)?"
  tools_camera:
    enabled: bool?
    ha_get_camera_image: "list(enabled|pinned|disabled)?"
  tools_dashboards:
    enabled: bool?
    ha_config_delete_dashboard: "list(enabled|pinned|disabled)?"
    ha_config_delete_dashboard_resource: "list(enabled|pinned|disabled)?"
    ha_config_get_dashboard: "list(enabled|pinned|disabled)?"
    ha_config_list_dashboard_resources: "list(enabled|pinned|disabled)?"
    ha_config_set_dashboard: "list(enabled|pinned|disabled)?"
    ha_config_set_dashboard_resource: "list(enabled|pinned|disabled)?"
    ha_dashboard_find_card: "list(enabled|pinned|disabled)?"
  tools_device_registry:
    enabled: bool?
    ha_get_device: "list(enabled|pinned|disabled)?"
    ha_remove_device: "list(enabled|pinned|disabled)?"
    ha_rename_entity: "list(enabled|pinned|disabled)?"
    ha_update_device: "list(enabled|pinned|disabled)?"
  tools_entity_registry:
    enabled: bool?
    ha_get_entity: "list(enabled|pinned|disabled)?"
    ha_get_entity_exposure: "list(enabled|pinned|disabled)?"
    ha_set_entity: "list(enabled|pinned|disabled)?"
  tools_files:
    enabled: bool?
    ha_delete_file: "list(enabled|pinned|disabled)?"
    ha_list_files: "list(enabled|pinned|disabled)?"
    ha_read_file: "list(enabled|pinned|disabled)?"
    ha_write_file: "list(enabled|pinned|disabled)?"
  tools_groups:
    enabled: bool?
    ha_config_list_groups: "list(enabled|pinned|disabled)?"
    ha_config_remove_group: "list(enabled|pinned|disabled)?"
    ha_config_set_group: "list(enabled|pinned|disabled)?"
  tools_hacs:
    enabled: bool?
    ha_hacs_add_repository: "list(enabled|pinned|disabled)?"
    ha_hacs_download: "list(enabled|pinned|disabled)?"
    ha_hacs_info: "list(enabled|pinned|disabled)?"
    ha_hacs_list_installed: "list(enabled|pinned|disabled)?"
    ha_hacs_repository_info: "list(enabled|pinned|disabled)?"
    ha_hacs_search: "list(enabled|pinned|disabled)?"
  tools_helper_entities:
    enabled: bool?
    ha_config_list_helpers: "list(enabled|pinned|disabled)?"
    ha_config_remove_helper: "list(enabled|pinned|disabled)?"
    ha_config_set_helper: "list(enabled|pinned|disabled)?"
    ha_get_helper_schema: "list(enabled|pinned|disabled)?"
    ha_set_config_entry_helper: "list(enabled|pinned|disabled)?"
  tools_history_statistics:
    enabled: bool?
    ha_get_automation_traces: "list(enabled|pinned|disabled)?"
    ha_get_history: "list(enabled|pinned|disabled)?"
    ha_get_logs: "list(enabled|pinned|disabled)?"
    ha_get_statistics: "list(enabled|pinned|disabled)?"
  tools_integrations:
    enabled: bool?
    ha_delete_config_entry: "list(enabled|pinned|disabled)?"
    ha_get_integration: "list(enabled|pinned|disabled)?"
    ha_set_integration_enabled: "list(enabled|pinned|disabled)?"
  tools_labels_categories:
    enabled: bool?
    ha_config_get_category: "list(enabled|pinned|disabled)?"
    ha_config_get_label: "list(enabled|pinned|disabled)?"
    ha_config_remove_category: "list(enabled|pinned|disabled)?"
    ha_config_remove_label: "list(enabled|pinned|disabled)?"
    ha_config_set_category: "list(enabled|pinned|disabled)?"
    ha_config_set_label: "list(enabled|pinned|disabled)?"
  tools_scripts:
    enabled: bool?
    ha_config_get_script: "list(enabled|pinned|disabled)?"
    ha_config_remove_script: "list(enabled|pinned|disabled)?"
    ha_config_set_script: "list(enabled|pinned|disabled)?"
  tools_search_discovery:
    enabled: bool?
    ha_deep_search: "list(enabled|pinned|disabled)?"
    ha_get_overview: "list(enabled|pinned|disabled)?"
    ha_get_state: "list(enabled|pinned|disabled)?"
    ha_get_states: "list(enabled|pinned|disabled)?"
    ha_search_entities: "list(enabled|pinned|disabled)?"
  tools_service_device_control:
    enabled: bool?
    ha_bulk_control: "list(enabled|pinned|disabled)?"
    ha_call_service: "list(enabled|pinned|disabled)?"
    ha_get_operation_status: "list(enabled|pinned|disabled)?"
    ha_list_services: "list(enabled|pinned|disabled)?"
  tools_system:
    enabled: bool?
    ha_backup_create: "list(enabled|pinned|disabled)?"
    ha_backup_restore: "list(enabled|pinned|disabled)?"
    ha_check_config: "list(enabled|pinned|disabled)?"
    ha_config_set_yaml: "list(enabled|pinned|disabled)?"
    ha_get_system_health: "list(enabled|pinned|disabled)?"
    ha_get_updates: "list(enabled|pinned|disabled)?"
    ha_reload_core: "list(enabled|pinned|disabled)?"
    ha_restart: "list(enabled|pinned|disabled)?"
  tools_todo_lists:
    enabled: bool?
    ha_add_todo_item: "list(enabled|pinned|disabled)?"
    ha_get_todo: "list(enabled|pinned|disabled)?"
    ha_remove_todo_item: "list(enabled|pinned|disabled)?"
    ha_update_todo_item: "list(enabled|pinned|disabled)?"
  tools_utilities:
    enabled: bool?
    ha_eval_template: "list(enabled|pinned|disabled)?"
    ha_install_mcp_tools: "list(enabled|pinned|disabled)?"
    ha_report_issue: "list(enabled|pinned|disabled)?"
  tools_zones:
    enabled: bool?
    ha_get_zone: "list(enabled|pinned|disabled)?"
    ha_remove_zone: "list(enabled|pinned|disabled)?"
    ha_set_zone: "list(enabled|pinned|disabled)?"
  # --- GENERATED TOOL SCHEMA END ---
# Add-on exposes HTTP port for MCP communication (fixed internal port)
ports:
  9583/tcp: 9583

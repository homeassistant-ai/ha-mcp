name: PR Validation Pipeline

on:
  pull_request:
    branches: [ master ]
  workflow_dispatch:

env:
  PYTHON_VERSION: "3.13"
  UV_CACHE_DIR: /tmp/.uv-cache

jobs:
  # Comprehensive E2E validation for all PRs
  e2e-validation:
    name: E2E Validation
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
    - uses: actions/checkout@v5

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Install uv
      uses: astral-sh/setup-uv@v6
      with:
        version: "latest"

    - name: Set up Python
      run: uv python install ${{ env.PYTHON_VERSION }}

    - name: Install dependencies
      run: uv sync --all-extras --dev

    - name: Run full E2E test suite (n=3 optimal)
      run: |
        echo "🚀 Running full E2E test suite with 2 workers..."
        uv run pytest tests/src/e2e/ \
          -n3 \
          --tb=short \
          -v
        echo "✅ Full E2E test suite passed"
      env:
        HOMEASSISTANT_URL: "http://localhost:8123"
        HOMEASSISTANT_TOKEN: "test-token"

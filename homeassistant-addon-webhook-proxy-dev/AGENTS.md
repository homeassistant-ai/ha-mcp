# Webhook Proxy Add-on (dev flavor)

This dev flavor shares one contributor doc with the stable flavor — do not
duplicate it here. **Before any work in this directory, read
[`../homeassistant-addon-webhook-proxy/AGENTS.md`](../homeassistant-addon-webhook-proxy/AGENTS.md).**
It covers both flavors: the dev-first promote-only flow, the version-bump guard
(any change in this tree needs `config.yaml` + `mcp_proxy_dev/manifest.json`
bumped together), promotion, and testing.

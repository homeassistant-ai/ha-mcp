[
  {
    "name": "ha_get_app",
    "title": "Get Apps (add-ons)",
    "description": "Get installed or available Home Assistant apps (add-ons), or details for one.\n\nDo not use this tool to change app state or configuration; use\n``ha_manage_app``. Use ``slug`` for details, ``source=\"installed\"`` for an\ninventory, or ``source=\"available\"`` for store discovery.\n\nRequires Home Assistant OS or Supervised.",
    "inputSchema": {
      "properties": {
        "source": {
          "type": "Literal['installed', 'available'] | None",
          "description": "App (add-on) source. With source='available', 'version' is the version you would get by installing (Supervisor's version_latest) and 'version_installed' is the running one, null when the app is not installed — so compare the two, not 'version' alone, to tell whether an installed app is current.",
          "default": null
        },
        "slug": {
          "type": "str | None",
          "description": "App (add-on) slug (e.g., '<prefix>_nodered'). Slug prefixes vary by app repository — omit to list all apps and discover the actual installed slug.",
          "default": null
        },
        "include_stats": {
          "type": "bool",
          "description": "Include CPU/memory usage statistics (only for source='installed')",
          "default": false
        },
        "repository": {
          "type": "str | None",
          "description": "Filter by repository slug, e.g., 'core', 'community' (only for source='available')",
          "default": null
        },
        "query": {
          "type": "str | None",
          "description": "App (add-on) name/description filter (only for source='available')",
          "default": null
        }
      }
    },
    "annotations": {
      "openWorldHint": true,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Apps (add-ons)"
    ],
    "source_file": "tools_addons.py"
  },
  {
    "name": "ha_manage_app",
    "title": "Manage App (add-on)",
    "description": "Manage Home Assistant apps (add-ons) or proxy an app API.\n\nFor app inventory, status, and Supervisor metadata, call ``ha_get_app``\nfirst; use proxy mode here for documented app-specific read APIs, and\ndo not infer private app API schemas.\n\nUse exactly one mode: lifecycle/store action, configuration fields,\n``path`` proxy, or ``path`` with ``array_patch``.\n\nRequires Home Assistant OS or Supervised. When ha-mcp itself runs as an\napp it cannot update its own running slug; update ha-mcp from the Home\nAssistant Apps UI. ``options`` merges top-level keys and one nested\nmapping level; supply complete values for deeper nested mappings\nbecause they are replaced. Prefer Ingress: direct-port access requires\na shared container network and may require weakening the target app\nauthentication. If a Supervisor lifecycle, configuration, or repository\nwrite has an unknown outcome, verify durable state with ``ha_get_app``\nbefore retrying. That cannot prove whether ``restart`` or ``rebuild``\nran; inspect Supervisor jobs and logs and do not automatically replay\nthem. For a proxy or array-patch write, query the target app's own read\nAPI before retrying.",
    "inputSchema": {
      "properties": {
        "slug": {
          "type": "str",
          "description": "App (add-on) slug (e.g., '<prefix>_nodered', '<prefix>_frigate'). Slug prefixes vary by app repository — call ha_get_app() to discover the actual installed slug. Required except for action='add_repository' / 'remove_repository' (which take 'repository' instead) and action='check_updates' (which takes neither).",
          "default": ""
        },
        "path": {
          "type": "str | None",
          "description": "Proxy mode: API path relative to the app (add-on) root (e.g., '/flows', '/api/events', '/api/stats'). Required for proxy mode.",
          "default": null
        },
        "method": {
          "type": "str",
          "description": "Proxy mode only. HTTP method: GET, POST, PUT, DELETE, PATCH.",
          "default": "GET"
        },
        "body": {
          "type": "dict[str, Any] | str | None",
          "description": "Proxy mode only. Request body for POST/PUT/PATCH — or, with websocket=True, the initial WebSocket message. Pass a JSON object or JSON string.",
          "default": null
        },
        "debug": {
          "type": "bool",
          "description": "Proxy mode only. Include diagnostic info (request URL, headers sent, response headers).",
          "default": false
        },
        "port": {
          "type": "int | None",
          "description": "Proxy mode only. Connect to this port instead of the Ingress port. Use ha_get_app(slug='...') to find available ports. Some apps, including Node-RED, reject direct access unless their leave_front_door_open option is enabled and the app is restarted.",
          "default": null
        },
        "offset": {
          "type": "int",
          "description": "Proxy mode only. HTTP: skip this many items in a JSON array response.",
          "default": 0
        },
        "limit": {
          "type": "int | None",
          "description": "Proxy mode only. HTTP: return at most this many items from a JSON array response.",
          "default": null
        },
        "websocket": {
          "type": "bool",
          "description": "Proxy mode only. Use WebSocket instead of HTTP for an app (add-on) WebSocket API. Sends 'body' as the initial message and collects responses; command names and body schemas are app/version-specific.",
          "default": false
        },
        "wait_for_close": {
          "type": "bool",
          "description": "Proxy mode only. WebSocket: True waits for the server to close a run-to-completion stream. False returns after the first response batch; use for one-shot command/response or bounded capture on a channel that stays open.",
          "default": true
        },
        "message_limit": {
          "type": "int | None",
          "description": "Proxy mode only. WebSocket: cap on messages collected from the wire, bounded by an internal safety ceiling. None = collect up to the ceiling.",
          "default": null
        },
        "message_offset": {
          "type": "int",
          "description": "Proxy mode only. WebSocket: drop this many messages from the start of the collected list before returning.",
          "default": 0
        },
        "summarize": {
          "type": "bool",
          "description": "Proxy mode only. WebSocket: when True, collapse runs of non-signal messages (typically YAML config dumps) into short elision markers. Set to False to return the raw stream.",
          "default": true
        },
        "python_transform": {
          "type": "str | None",
          "description": "Proxy mode only. Sandboxed Python expression that post-processes the response. Variable `response` is exposed — a list[dict | str] for WebSocket (parsed JSON or raw text), or dict/list/str for HTTP (parsed body). Supports in-place mutation (response.append(...)) or reassignment (response = [...]). Example: response = [m for m in response if 'ERROR' in str(m)].",
          "default": null
        },
        "options": {
          "type": "dict[str, Any] | None",
          "description": "Config mode: App (add-on) configuration values (the 'Configuration' tab in the UI).",
          "default": null
        },
        "network": {
          "type": "dict[str, Any] | None",
          "description": "Config mode: Complete desired host-port override map (e.g., {'5800/tcp': 8081}). A non-empty map replaces current overrides, so omitted entries are cleared. Omit 'network' to leave mappings unchanged. An empty map is ignored and does not by itself select config mode.",
          "default": null
        },
        "boot": {
          "type": "str | None",
          "description": "Config mode: Boot strategy — 'auto' (start with HA) or 'manual'.",
          "default": null
        },
        "auto_update": {
          "type": "bool | None",
          "description": "Config mode: Enable or disable automatic updates for this app (add-on).",
          "default": null
        },
        "watchdog": {
          "type": "bool | None",
          "description": "Config mode: Enable or disable Supervisor watchdog (auto-restart on crash).",
          "default": null
        },
        "array_patch": {
          "type": "dict[str, Any] | None",
          "description": "Array-patch mode: atomically GET a JSON array endpoint, apply ordered ops, then POST the mutated array back. Requires 'path'; mutually exclusive with body / websocket / offset / limit and config params. Use ha_get_skill_guide for operation shapes.",
          "default": null
        },
        "request_headers": {
          "type": "dict[str, str] | None",
          "description": "Proxy/array-patch mode: extra HTTP headers for the app (add-on) API. Useful for app-specific requirements such as Node-RED's `Node-RED-Deployment-Type: full`. Ingress routing headers override caller values on Ingress routes; direct-port calls have no internal routing headers. `Content-Type` is derived from the body when supplied. Not valid in config or websocket mode.",
          "default": null
        },
        "action": {
          "type": "str | None",
          "description": "Lifecycle mode: run a Supervisor app (add-on) action. One of 'install', 'uninstall', 'start', 'stop', 'restart', 'rebuild', 'update'. 'install'/'update' require the app's repository to be registered (it appears in ha_get_app(source='available')); 'rebuild' is for a local app whose source changed but whose version did not. Store-repository mode: 'add_repository' / 'remove_repository' register or unregister a custom app store repository — these use the 'repository' param instead of 'slug'. Store-wide mode: 'check_updates' reloads the store so Supervisor re-scans its repositories (e.g. after editing a local app's config.yaml) — it takes neither 'slug' nor 'repository', refreshes available metadata only, and installs nothing; follow with action='update' for a newer version, or action='rebuild' for a local app whose source changed without a version change. Returns 'changed' and 'updates_available', null when the store could not be read (see 'warnings').",
          "default": null
        },
        "repository": {
          "type": "str | None",
          "description": "Store-repository mode only (action='add_repository' or 'remove_repository'). For add_repository: the repository URL (e.g., 'https://github.com/balloob/home-assistant-addons'). For remove_repository: the repository slug (e.g., '0f1cc410', as shown in ha_get_app(source='available')). Required for those actions; rejected otherwise.",
          "default": null
        }
      }
    },
    "annotations": {
      "openWorldHint": true,
      "destructiveHint": true,
      "idempotentHint": false,
      "readOnlyHint": false
    },
    "tags": [
      "Apps (add-ons)"
    ],
    "source_file": "tools_addons.py"
  },
  {
    "name": "ha_list_floors_areas",
    "title": "List Floors and Areas",
    "description": "List floors sorted by level ascending, each with their assigned areas nested, plus areas without a floor.\n\nUse for location-based reasoning where floor-to-area relationships matter, such as \"which rooms are on the ground floor\" or operations scoped to a level.\n\nFloors with level=None sort alongside level 0 (ground floor). Areas without a floor assignment appear in unassigned_areas; areas whose floor_id points to a non-existent floor appear in orphaned_areas. When the ha_mcp_tools component's registries capability is available, both registries come from a single in-process snapshot, so this classification is always consistent. Without it (legacy path), the two registries are fetched via independent WebSocket calls and a registry change between reads may transiently misclassify an area.",
    "inputSchema": {
      "properties": {
        "fields": {
          "type": "str | list[str] | None",
          "default": null,
          "description": "Return only the specified top-level response keys to reduce response size (e.g. [\"floors\"]). None = full response. Available keys: success, floor_count, area_count, unassigned_count, orphaned_count, floors, unassigned_areas, orphaned_areas, message."
        },
        "area_fields": {
          "type": "str | list[str] | None",
          "default": null,
          "description": "Project each area record (in floors[].areas, unassigned_areas, and orphaned_areas) to only the specified keys. E.g. [\"area_id\", \"name\"] returns slim area records. None = full records. Unknown keys yield empty records. Available keys: area_id, name, icon, floor_id, aliases, picture, labels."
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Areas & Floors"
    ],
    "source_file": "tools_areas.py"
  },
  {
    "name": "ha_remove_area_or_floor",
    "title": "Remove Area or Floor",
    "description": "Remove a Home Assistant area or floor.\n\nRemoving an area unassigns its entities and devices (the entities and\ndevices themselves are not removed). Removing a floor unassigns its\nareas. May break automations referencing the removed area/floor.",
    "inputSchema": {
      "properties": {
        "kind": {
          "type": "Literal['area', 'floor']",
          "description": "Which registry to delete from: 'area' or 'floor'"
        },
        "id": {
          "type": "str",
          "description": "Area ID or floor ID to delete (use ha_list_floors_areas to find IDs)"
        }
      },
      "required": [
        "kind",
        "id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": true
    },
    "tags": [
      "Areas & Floors"
    ],
    "source_file": "tools_areas.py"
  },
  {
    "name": "ha_set_area_or_floor",
    "title": "Create or Update Area or Floor",
    "description": "Create or update a Home Assistant area or floor.\n\nPass kind='area' (with optional floor_id, picture, labels) or kind='floor' (with optional level).\nProvide name only to create a new entry; provide id to update an existing one.\nCross-kind parameters (e.g., picture or labels under kind='floor') are rejected with VALIDATION_INVALID_PARAMETER.\n\nEXAMPLES:\nha_set_area_or_floor(kind=\"area\", name=\"Kitchen\")\nha_set_area_or_floor(kind=\"area\", id=\"kitchen\", floor_id=\"ground_floor\")\nha_set_area_or_floor(kind=\"area\", id=\"kitchen\", labels=[\"site_home\"])\nha_set_area_or_floor(kind=\"floor\", name=\"Basement\", level=-1)\nha_set_area_or_floor(kind=\"floor\", id=\"ground_floor\", level=0)",
    "inputSchema": {
      "properties": {
        "kind": {
          "type": "Literal['area', 'floor']",
          "description": "Which registry to operate on: 'area' for rooms, 'floor' for building levels"
        },
        "name": {
          "type": "str | None",
          "description": "Name (required when creating; optional when updating, e.g., 'Living Room', 'Ground Floor')",
          "default": null
        },
        "id": {
          "type": "str | None",
          "description": "Existing area_id or floor_id to update (use ha_list_floors_areas to find IDs)",
          "default": null
        },
        "floor_id": {
          "type": "str | None",
          "description": "Floor assignment when kind='area' (use empty string to clear).",
          "default": null
        },
        "level": {
          "type": "int | None",
          "description": "Numeric level when kind='floor' (0=ground, 1=first, -1=basement).",
          "default": null
        },
        "icon": {
          "type": "str | None",
          "description": "Material Design Icon (e.g., 'mdi:sofa', 'mdi:home-floor-1', empty string to remove)",
          "default": null
        },
        "aliases": {
          "type": "str | list[str] | None",
          "description": "Alternative names for voice assistant recognition (e.g., ['lounge'], empty list to clear)",
          "default": null
        },
        "picture": {
          "type": "str | None",
          "description": "Picture URL when kind='area' (empty string to remove).",
          "default": null
        },
        "labels": {
          "type": "str | list[str] | None",
          "description": "Label IDs when kind='area' (replaces the area's label set; empty list to clear). Omit to leave labels unchanged. Floors have no labels.",
          "default": null
        }
      },
      "required": [
        "kind"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Areas & Floors"
    ],
    "source_file": "tools_areas.py"
  },
  {
    "name": "ha_manage_pipeline",
    "title": "Manage Assist Pipeline",
    "description": "Manage Home Assistant Assist pipelines.\n\nUse action='list' to discover pipeline IDs, action='get' to inspect one\npipeline, action='create' or action='update' to write pipeline settings,\naction='set_preferred' to choose the preferred pipeline, and\naction='process' to run a sentence through Assist.\n\naction='process' sends the sentence straight to Assist's conversation\nagent, so a matched intent executes: it turns on the light rather than\nreporting that it would. Its result carries response_type\n('action_done', 'query_answer' or 'error') and, on an error, error_code\nsuch as 'no_intent_match' — Assist declining a sentence is an answer,\nnot a tool failure, so inspect those fields rather than expecting a\nraised error. Use ha_call_service to act on an entity directly; use\nthis to test what Assist itself understands. When the built-in agent\nanswers, a matching conversation trigger runs its automation: that\nagent checks its sentence triggers before it matches intents, so this\nis not limited to intents. Even with pipeline_id, the sentence goes\nto the agent directly. So with an agent other than the built-in one, neither\nsentence triggers nor prefer_local_intents apply — a full pipeline run\nis what adds those for other agents.\n\nEXAMPLES:\n- Get the preferred pipeline: ha_manage_pipeline(action=\"get\", pipeline_id=\"preferred\")\n- Create by cloning a pipeline: ha_manage_pipeline(action=\"create\", base_pipeline_id=\"preferred\", name=\"Local Assist\", conversation_engine=\"conversation.local_llm\")\n- Update the agent and clear the TTS voice: ha_manage_pipeline(action=\"update\", pipeline_id=\"preferred\", conversation_engine=\"conversation.local_llm\", tts_voice=\"\")\n- Run a sentence through one pipeline's agent: ha_manage_pipeline(action=\"process\", sentence=\"turn on the kitchen light\", pipeline_id=\"preferred\")\n- Continue a conversation: ha_manage_pipeline(action=\"process\", sentence=\"and the hallway?\", conversation_id=\"<id from the previous response>\")\n\nNon-nullable fields such as name, language, conversation_language, and\nconversation_engine must be omitted or non-empty.",
    "inputSchema": {
      "properties": {
        "action": {
          "type": "PipelineAction",
          "description": "Pipeline operation: list, get, create, update, set_preferred, or process."
        },
        "pipeline_id": {
          "type": "str | None",
          "description": "Assist pipeline ID. Required for get, update, and set_preferred. Optional for process, where it selects the conversation agent and language that pipeline is configured with.",
          "default": null
        },
        "sentence": {
          "type": "str | None",
          "description": "Natural-language command to run through Assist. Required when action='process'.",
          "default": null
        },
        "conversation_id": {
          "type": "str | None",
          "description": "For process only, the conversation to continue. Returned in the response so follow-up sentences keep their context.",
          "default": null
        },
        "agent_id": {
          "type": "str | None",
          "description": "For process only, the conversation agent entity ID to answer, e.g. 'conversation.home_assistant'. Overrides the agent taken from pipeline_id; omit both for the default agent.",
          "default": null
        },
        "name": {
          "type": "str | None",
          "description": "Pipeline display name. Required when action='create'.",
          "default": null
        },
        "conversation_engine": {
          "type": "str | None",
          "description": "Conversation agent entity ID or engine ID. Required when action='create'.",
          "default": null
        },
        "base_pipeline_id": {
          "type": "str | None",
          "description": "Pipeline ID to clone when creating. Omit to clone the preferred pipeline. Ignored for non-create actions.",
          "default": null
        },
        "conversation_language": {
          "type": "str | None",
          "description": "Conversation language, usually '*'.",
          "default": null
        },
        "language": {
          "type": "str | None",
          "description": "Pipeline language, e.g. 'en'. For process, the language to recognise the sentence in.",
          "default": null
        },
        "stt_engine": {
          "type": "str | None",
          "description": "Speech-to-text engine. Pass empty string to clear.",
          "default": null
        },
        "stt_language": {
          "type": "str | None",
          "description": "Speech-to-text language. Pass empty string to clear.",
          "default": null
        },
        "tts_engine": {
          "type": "str | None",
          "description": "Text-to-speech engine. Pass empty string to clear.",
          "default": null
        },
        "tts_language": {
          "type": "str | None",
          "description": "Text-to-speech language. Pass empty string to clear.",
          "default": null
        },
        "tts_voice": {
          "type": "str | None",
          "description": "Text-to-speech voice. Pass empty string to clear.",
          "default": null
        },
        "wake_word_entity": {
          "type": "str | None",
          "description": "Wake-word entity ID. Pass empty string to clear.",
          "default": null
        },
        "wake_word_id": {
          "type": "str | None",
          "description": "Wake-word ID. Pass empty string to clear.",
          "default": null
        },
        "prefer_local_intents": {
          "type": "bool | None",
          "description": "Whether Home Assistant local intents should be preferred before the conversation engine.",
          "default": null
        },
        "make_preferred": {
          "type": "bool",
          "description": "For create/update only, also set the resulting pipeline as preferred. Ignored for other actions.",
          "default": false
        }
      },
      "required": [
        "action"
      ]
    },
    "annotations": {
      "openWorldHint": true,
      "destructiveHint": true,
      "idempotentHint": false,
      "readOnlyHint": false
    },
    "tags": [
      "Assist"
    ],
    "source_file": "tools_assist_pipeline.py"
  },
  {
    "name": "ha_config_get_automation",
    "title": "Get Automation Config",
    "description": "Get Home Assistant automation configuration.\n\nReturns the complete configuration including triggers, conditions, actions, and mode settings.\n\nThe returned `config_hash` is stable across consecutive reads of an unchanged config — `compute_config_hash` documents the underlying contract.\n\nThe returned `automation_id` is the resolved entity_id (canonical\nform, e.g. `automation.morning_routine`) when the registry lookup\nsucceeds, falling back to the input `identifier` otherwise.\n\nEXAMPLES:\n- Get automation: ha_config_get_automation(\"automation.morning_routine\")\n- Get by unique_id: ha_config_get_automation(\"my_unique_automation_id\")\n\nFor comprehensive automation documentation, use ha_get_skill_guide.",
    "inputSchema": {
      "properties": {
        "identifier": {
          "type": "str",
          "description": "Automation entity_id (e.g., 'automation.morning_routine') or unique_id"
        }
      },
      "required": [
        "identifier"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Automations"
    ],
    "source_file": "tools_config_automations.py"
  },
  {
    "name": "ha_config_remove_automation",
    "title": "Remove Automation",
    "description": "Delete a Home Assistant automation permanently.\n\nThe returned `automation_id` is the resolved entity_id (canonical\nform, e.g. `automation.morning_routine`) when the registry lookup\nsucceeded before the delete, falling back to the input\n`identifier` otherwise.\n\nEXAMPLES:\n- Delete automation: ha_config_remove_automation(\"automation.old_automation\")\n- Delete by unique_id: ha_config_remove_automation(\"my_unique_id\")",
    "inputSchema": {
      "properties": {
        "identifier": {
          "type": "str",
          "description": "Automation entity_id (e.g., 'automation.old_automation') or unique_id to delete"
        },
        "wait": {
          "type": "bool",
          "description": "Wait for automation to be fully removed before returning.",
          "default": true
        }
      },
      "required": [
        "identifier"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": true
    },
    "tags": [
      "Automations"
    ],
    "source_file": "tools_config_automations.py"
  },
  {
    "name": "ha_config_set_automation",
    "title": "Create or Update Automation",
    "description": "Create or update a Home Assistant automation.\n\nMUST call ha_get_skill_guide OR refer to your locally installed skills first.\n\nPrefer native triggers/conditions/actions over templates in logic\npositions; templates belong only in `data.*`, notification text,\n`event_data` and `variables`. The best-practice checker reports\nviolations under `best_practice_warnings` — fix them before\nre-submitting. `automation-patterns.md` and `template-guidelines.md`\nship under `skill_content` by default. Test any unavoidable template\nwith ha_eval_template first.\n\nConsider a dedicated tool first: a state snapshot with no trigger ->\nha_config_set_scene; a value derived from other entities ->\nha_config_set_helper(helper_type='template'); a counter / timer /\nschedule / boolean -> ha_config_set_helper.\n\nMODES (pick one; each can also take `enabled`, which alone with\n`identifier` turns the automation on or off without touching config):\n- python_transform: surgical edits to an existing automation. Requires\n  identifier and config_hash from ha_config_get_automation(). Operates on\n  the fetched config, which uses HA's plural root keys\n  'triggers'/'actions'/'conditions', e.g.\n  python_transform=\"config['triggers'].append({'trigger': 'state', 'entity_id': 'binary_sensor.motion', 'to': 'on'})\"\n- config: new automations or full restructures. Regular automations need\n  alias, triggers and actions; blueprint automations need alias and\n  use_blueprint {path, input}.\n- take_control_of_blueprint: convert a blueprint-backed automation into a\n  standalone one. Takes no config of its own.\n\nIDENTITY: omit identifier and config['id'] to create with a generated ID;\nan unused raw ID creates that specific ID. Reusing an identifier targets\nthe same automation even if the alias changes; to rename or replace it,\nread it first and pass its config_hash — a changed alias without that\nhash is rejected before writing. The returned `automation_id` is the\nresolved entity_id, falling back to the input identifier or the\ngenerated unique_id.\n\nEXAMPLES:\n- Create: ha_config_set_automation(config={\"alias\": \"Morning Lights\", \"triggers\": [{\"trigger\": \"time\", \"at\": \"07:00:00\"}], \"actions\": [{\"action\": \"light.turn_on\", \"target\": {\"area_id\": \"bedroom\"}}]})\n- From a blueprint: ha_config_set_automation(config={\"alias\": \"Motion Light Kitchen\", \"use_blueprint\": {\"path\": \"homeassistant/motion_light.yaml\", \"input\": {\"motion_entity\": \"binary_sensor.kitchen_motion\", \"light_target\": {\"entity_id\": \"light.kitchen\"}}}})\n- Update: current = ha_config_get_automation(identifier=\"automation.x\"); ha_config_set_automation(identifier=\"automation.x\", config_hash=current[\"config_hash\"], config={...})\n- Disable: ha_config_set_automation(identifier=\"automation.x\", enabled=False)\n- Take control: ha_config_set_automation(identifier=\"automation.x\", take_control_of_blueprint=True)\n\nTAKE CONTROL is one-way: later blueprint edits stop reaching the\nautomation; to change an input value, update 'use_blueprint.input'\ninstead. It does NOT free the blueprint — Home Assistant keeps counting\nthe converted automation as a user, so deleting that blueprint stays\nrefused until the automation is removed (an automation reload does not\nclear it). The response names the blueprint in\n`took_control_of_blueprint`. To preview the rendering without writing\nanything, use ha_manage_blueprints(action=\"substitute\", path=..., input=...).",
    "inputSchema": {
      "properties": {
        "config": {
          "type": "dict[str, Any] | None",
          "description": "Complete automation configuration. Purpose-specific triggers/conditions (HA 2026.7+ default: 'trigger': '<domain>.<name>' with 'target'/'options') are valid config. Mutually exclusive with python_transform.",
          "default": null
        },
        "identifier": {
          "type": "str | None",
          "description": "Target automation entity_id or HA config 'id' (unique_id). Omit for creation with a generated ID. Values such as 'new' are literal IDs, not placeholders.",
          "default": null
        },
        "python_transform": {
          "type": "str | None",
          "description": "Python expression to transform existing automation config. Mutually exclusive with config. WARNING: Expressions with infinite loops will hang the server. Examples: Simple: python_transform=\"config['actions'][0]['data']['brightness'] = 255\" Pattern: python_transform=\"for a in config['actions']: if a.get('alias') == 'My Step': a['data']['value'] = 100\" \n\nPYTHON TRANSFORM SECURITY:\n\n✅ ALLOWED:\n- Dictionary/list access: config['views'][0]['cards'][1]\n- Slicing: config['views'][0]['cards'][1:3]\n- Assignment: config['key'] = 'value'\n- Deletion: del config['key'] or config.pop('key')\n- List methods: append, insert, pop, remove, clear, extend\n- Dict methods: update, get, setdefault, keys, values, items\n- Loops: for, if/else, pass, break, continue\n- Comprehensions: [x for x in ...], {k: v for ...}, (x for x in ...)\n- Ternary: x if condition else y\n- Iterable unpacking (* in calls/literals): f(*xs), [*xs, y]\n- Dict unpacking (**) in calls and dict literals: {**d, 'k': v}\n- Keyword arguments: func(key=value)\n- Lambdas (e.g. for `key=`): sorted(items, key=lambda x: x['score'])\n- String methods: startswith, endswith, lower, upper, strip, split, join, replace\n- Safe builtins: isinstance, len, range, enumerate, zip, sorted, reversed,\n  min, max, sum, abs, any, all, round, str, int, float, bool, list, dict,\n  tuple, set\n\n❌ FORBIDDEN:\n- Imports: import, from, __import__\n- File operations: open, read, write\n- Dunder access: __class__, __bases__, __subclasses__\n- Dangerous builtins: eval, exec, compile, getattr, setattr, delattr, hasattr\n- Function definitions: def, class\n- Exception handling: try/except (validate with isinstance/in/.get() instead)\n- While loops: use bounded for loops or comprehensions instead",
          "default": null
        },
        "config_hash": {
          "type": "str | None",
          "description": "Config hash from ha_config_get_automation for optimistic locking. Required for python_transform and when a config update changes an existing automation's alias; otherwise optional for config updates (validates before full replacement if provided).",
          "default": null
        },
        "take_control_of_blueprint": {
          "type": "bool",
          "description": "Convert a blueprint-backed automation into an editable standalone one -- the UI's \"Take control\". Renders the blueprint with its current inputs and saves the result over the same automation, which keeps its entity_id, alias and description and then has its own triggers/conditions/actions and no 'use_blueprint'. Requires identifier; mutually exclusive with config and python_transform.",
          "default": false
        },
        "category": {
          "type": "str | None",
          "description": "Category ID to assign to this automation. Use ha_config_get_category(scope='automation') to list available categories, or ha_config_set_category() to create one.",
          "default": null
        },
        "wait": {
          "type": "bool",
          "description": "Wait for automation to be queryable before returning. Set to False for bulk operations.",
          "default": true
        },
        "enabled": {
          "type": "bool | None",
          "description": "Turn the automation on (True) or off (False) after an optional config update; None leaves it unchanged. Not written into the stored config: Home Assistant keeps the state across restarts, but a config 'initial_state' overrides it whenever the automation is reloaded or HA starts.",
          "default": null
        },
        "MandatoryBPS": {
          "type": "bool",
          "default": true
        },
        "BestPracticeKey": {
          "type": "BestPracticeKeyParam",
          "default": null
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Automations"
    ],
    "source_file": "tools_config_automations.py"
  },
  {
    "name": "ha_manage_blueprints",
    "title": "Manage Blueprints",
    "description": "Manage Home Assistant blueprints — list, read, import, save, delete, or render a standalone config.\n\nOne interface for the whole blueprint lifecycle in the ``automation``\nand ``script`` domains.\n\nDO NOT use this to create an automation or script FROM a blueprint —\nthat is ``ha_config_set_automation`` / ``ha_config_set_script`` with a\n``use_blueprint`` config.\n\nTo duplicate a blueprint, ``get`` it and ``save`` its ``yaml`` under a\nnew ``path``; to edit one in place, ``get`` it, change the text, and\n``save`` it back to the same ``path`` with ``overwrite=True``.\n\n``get`` also reports ``used_by``: the automations or scripts built on\nthe blueprint, which is the UI's \"Show automations using this\nblueprint\". Check it before deleting — Home Assistant refuses to delete a\nblueprint anything still uses (the error lists the consumers), and it\ngoes on counting a consumer that has since taken control of its own\nconfig until that consumer is removed.\n\nCAVEATS: ``get`` returns the on-disk YAML only when something can read\nit — an in-process server, the ha_mcp_tools component, the File & YAML\nTools entry, or the blueprint's ``source_url``; ``yaml_source`` names\nwhich one answered, and ``source_url`` text is a fresh download that can\ndiffer from the installed file. Core's blueprint API alone exposes\nmetadata only, so a locally authored blueprint on a bare install has no\nreadable text. Both writes are snapshotted first when a copy can be\nread, so ``ha_manage_backup(scope=\"edits\")`` can restore the previous\nfile. ``substitute`` only renders — it writes nothing, so pass the\nreturned config to ``ha_config_set_automation`` /\n``ha_config_set_script`` to persist it. To convert an automation or\nscript that ALREADY exists, prefer those tools' ``take_control_of_blueprint=True``:\nit renders with that item's own current inputs and saves the result over\nitself in one call.\n\nEXAMPLES:\n- Get one (with its consumers in ``used_by``): ha_manage_blueprints(action=\"get\", path=\"homeassistant/motion_light.yaml\")\n- Import: ha_manage_blueprints(action=\"import\", url=\"https://example.com/bp.yaml\")\n- Edit in place: ha_manage_blueprints(action=\"save\", path=\"user/motion.yaml\", yaml=<edited text>, overwrite=True)\n- Delete: ha_manage_blueprints(action=\"delete\", path=\"user/motion.yaml\", confirm=True)\n- Render standalone: ha_manage_blueprints(action=\"substitute\", path=\"user/motion.yaml\", input={\"motion_sensor\": \"binary_sensor.hall\"})\n\nRELATED TOOLS: ``ha_config_remove_automation`` / ``ha_config_remove_script``\nto clear consumers blocking a delete, ``ha_search`` to find them.",
    "inputSchema": {
      "properties": {
        "action": {
          "type": "Literal['list', 'get', 'import', 'save', 'delete', 'substitute']",
          "description": "'list' installed blueprints, 'get' one blueprint's metadata/inputs/YAML, 'import' one from a URL, 'save' YAML text to a blueprint path, 'delete' an installed one, or 'substitute' to render a standalone config (the UI's \"Take control\")"
        },
        "domain": {
          "type": "str",
          "description": "Blueprint domain: 'automation' or 'script'. Ignored by action='import' — the blueprint file declares its own domain.",
          "default": "automation"
        },
        "path": {
          "type": "str | None",
          "description": "Installed blueprint path, e.g. 'homeassistant/motion_light.yaml' (action='get' / 'save' / 'delete' / 'substitute'). 'save' appends '.yaml' when it is missing, as Home Assistant does.",
          "default": null
        },
        "url": {
          "type": "str | None",
          "description": "URL to import from — GitHub, Home Assistant Community, or a direct YAML link (action='import')",
          "default": null
        },
        "yaml": {
          "type": "str | None",
          "description": "Blueprint YAML text to write (action='save')",
          "default": null
        },
        "source_url": {
          "type": "str | None",
          "description": "Origin URL to stamp into the saved blueprint's metadata (action='save'); omit for a hand-authored blueprint",
          "default": null
        },
        "overwrite": {
          "type": "bool",
          "description": "Write over an already-installed blueprint (action='import' / 'save'). Home Assistant reloads every automation/script using it.",
          "default": false
        },
        "input": {
          "type": "dict[str, Any] | None",
          "description": "Blueprint input values keyed by input name (action='substitute'); defaults to {}",
          "default": null
        },
        "confirm": {
          "type": "bool",
          "description": "Required confirmation for action='delete'",
          "default": false
        }
      },
      "required": [
        "action"
      ]
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": false,
      "openWorldHint": true
    },
    "tags": [
      "Blueprints"
    ],
    "source_file": "tools_blueprints.py"
  },
  {
    "name": "ha_config_get_calendar_events",
    "title": "Get Calendar Events",
    "description": "Get calendar events from a calendar entity within a time range.\n\nReturns each event's summary, start, end, description and location. To\nfind calendar entities, use ha_search(domain_filter='calendar').\n\nEXAMPLES:\n- Next week (defaults): ha_config_get_calendar_events(\"calendar.family\")\n- Date range: ha_config_get_calendar_events(\"calendar.work\", start=\"2024-01-01T00:00:00\", end=\"2024-01-31T23:59:59\")",
    "inputSchema": {
      "properties": {
        "entity_id": {
          "type": "str",
          "description": "Calendar entity ID (e.g., 'calendar.family')"
        },
        "start": {
          "type": "str | None",
          "description": "Start datetime in ISO format (default: now)",
          "default": null
        },
        "end": {
          "type": "str | None",
          "description": "End datetime in ISO format (default: 7 days from start)",
          "default": null
        },
        "max_results": {
          "type": "int",
          "description": "Maximum number of events to return",
          "default": 20
        }
      },
      "required": [
        "entity_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Calendar"
    ],
    "source_file": "tools_calendar.py"
  },
  {
    "name": "ha_config_remove_calendar_event",
    "title": "Remove Calendar Event",
    "description": "Delete an event from a calendar.\n\nDeletes a calendar event via the WebSocket ``calendar/event/delete``\ncommand. HA's calendar component only registers ``create_event`` and\n``get_events`` as REST services — delete and update live on the\nWebSocket API only. Get the event UID from\nha_config_get_calendar_events().\n\nEXAMPLES:\n- Delete a single event: ha_config_remove_calendar_event(\"calendar.family\", uid=\"event-12345\")\n- Delete one occurrence and all later ones: ha_config_remove_calendar_event(\"calendar.work\", uid=\"recurring-event-67890\", recurrence_id=\"20240115T100000\", recurrence_range=\"THISANDFUTURE\")",
    "inputSchema": {
      "properties": {
        "entity_id": {
          "type": "str",
          "description": "Calendar entity ID (e.g., 'calendar.family')"
        },
        "uid": {
          "type": "str",
          "description": "Unique identifier of the event to delete"
        },
        "recurrence_id": {
          "type": "str | None",
          "description": "Recurrence ID for recurring events",
          "default": null
        },
        "recurrence_range": {
          "type": "Literal['THISANDFUTURE'] | None",
          "description": "Recurrence range: 'THISANDFUTURE' to delete this and future occurrences. Home Assistant compares this value verbatim, so no other spelling (including 'THIS_AND_FUTURE') selects the range.",
          "default": null
        }
      },
      "required": [
        "entity_id",
        "uid"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": true
    },
    "tags": [
      "Calendar"
    ],
    "source_file": "tools_calendar.py"
  },
  {
    "name": "ha_config_set_calendar_event",
    "title": "Create or Update Calendar Event",
    "description": "Create a new event in a calendar, or update an existing one.\n\nCreates a one-off event via the calendar.create_event service, or a\nrecurring series via the WebSocket ``calendar/event/create`` command\nwhen ``rrule`` is provided (the REST service schema does not accept\nrecurrence rules). Passing ``uid`` switches to update mode, which uses\nthe WebSocket ``calendar/event/update`` command — HA registers no REST\nservice for updating an event.\n\n**When NOT to use:**\n- To retrieve calendar events, use ``ha_config_get_calendar_events``.\n- To delete an event, use ``ha_config_remove_calendar_event``.\n- To find the ``uid`` of an event to update, use\n  ``ha_config_get_calendar_events``; this tool does not search.\n\nPassing date-only values (``YYYY-MM-DD``) for both ``start`` and\n``end`` creates an all-day event; passing full ISO datetimes creates\na timed event. The two forms cannot be mixed — a date-only ``start``\nwith a datetime ``end`` (or vice versa) is rejected.\n\nAn update replaces the whole event rather than patching it, so\n``summary``, ``start`` and ``end`` stay required in update mode, and a\n``description`` or ``location`` that is not re-supplied is cleared.\nAn ``rrule`` is the exception: Home Assistant accepts a new rule but\nhas no way to express \"no recurrence\", so an existing rule survives an\nupdate that omits it. Delete the event and create it again to drop the\nrecurrence.\n\nNot every calendar integration supports event creation; recurring\nevents additionally require the integration to support recurrence\n(the built-in Local Calendar does). Update support is narrower still:\nLocal Calendar implements it, while the core Google Calendar and CalDAV\nintegrations do not.\n\nEXAMPLES:\n- Create: ha_config_set_calendar_event(\"calendar.family\", summary=\"Doctor appointment\", start=\"2024-01-15T14:00:00\", end=\"2024-01-15T15:00:00\")\n- Recurring (every Monday, 10 occurrences): ha_config_set_calendar_event(\"calendar.work\", summary=\"Team meeting\", start=\"2024-01-15T10:00:00\", end=\"2024-01-15T11:00:00\", rrule=\"FREQ=WEEKLY;BYDAY=MO;COUNT=10\")\n- Update this and all later occurrences: ha_config_set_calendar_event(\"calendar.work\", summary=\"Team meeting (new time)\", start=\"2024-02-05T11:00:00\", end=\"2024-02-05T12:00:00\", uid=\"recurring-event-67890\", recurrence_id=\"20240205T100000\", recurrence_range=\"THISANDFUTURE\")",
    "inputSchema": {
      "properties": {
        "entity_id": {
          "type": "str",
          "description": "Calendar entity ID (e.g., 'calendar.family')"
        },
        "summary": {
          "type": "str",
          "description": "Event title/summary"
        },
        "start": {
          "type": "str",
          "description": "Event start date or datetime in ISO format"
        },
        "end": {
          "type": "str",
          "description": "Event end date or datetime in ISO format. For all-day events (date-only) the end date is exclusive; a single-day all-day event needs end = start + 1 day."
        },
        "description": {
          "type": "str | None",
          "description": "Event description",
          "default": null
        },
        "location": {
          "type": "str | None",
          "description": "Event location",
          "default": null
        },
        "rrule": {
          "type": "str | None",
          "description": "RFC 5545 recurrence rule, without 'RRULE:' prefix (e.g., 'FREQ=WEEKLY;BYDAY=MO' or 'FREQ=MONTHLY;BYDAY=3SA'). Creates a recurring event series.",
          "default": null
        },
        "uid": {
          "type": "str | None",
          "description": "UID of an existing event to update. Omit to create a new event.",
          "default": null
        },
        "recurrence_id": {
          "type": "str | None",
          "description": "Only meaningful with 'uid': identifies one occurrence of a recurring series to update.",
          "default": null
        },
        "recurrence_range": {
          "type": "Literal['THISANDFUTURE'] | None",
          "description": "Only meaningful with 'uid': 'THISANDFUTURE' to update this and all following occurrences. Home Assistant compares this value verbatim, so no other spelling (including 'THIS_AND_FUTURE') selects the range.",
          "default": null
        }
      },
      "required": [
        "entity_id",
        "summary",
        "start",
        "end"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Calendar"
    ],
    "source_file": "tools_calendar.py"
  },
  {
    "name": "ha_get_camera_image",
    "title": "Get Camera Image",
    "description": "Get a snapshot image from a Home Assistant camera entity.\n\nFetches the current camera image and returns it directly for visual\nanalysis (security checks, delivery verification, confirming a garage\ndoor actually closed). Only cameras exposed to Home Assistant are\naccessible; images come back in their native format (JPEG, PNG, or GIF).\nUse width/height on high-resolution cameras to reduce token usage.\n\nEXAMPLE: ha_get_camera_image(entity_id=\"camera.backyard\", width=640, height=480)",
    "inputSchema": {
      "properties": {
        "entity_id": {
          "type": "str",
          "description": "Camera entity ID (e.g., 'camera.front_door')"
        },
        "width": {
          "type": "int | None",
          "description": "Width to resize the image to",
          "default": null
        },
        "height": {
          "type": "int | None",
          "description": "Height to resize the image to",
          "default": null
        }
      },
      "required": [
        "entity_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Camera"
    ],
    "source_file": "tools_camera.py"
  },
  {
    "name": "ha_get_dashboard_screenshot",
    "title": "Get Dashboard Screenshot",
    "description": "Get rendered images of a Home Assistant Lovelace dashboard view.\n\nWhen not to use: while reading or writing dashboard configuration, use\nha_config_get_dashboard(include_screenshot=True) or\nha_config_set_dashboard(return_screenshot=True) for a single workflow.\n\nUse it for repeatable visual checks, including ordered mobile, tablet,\nand desktop captures. Puppet reports image bytes but does not confirm\nthat the frontend accepted a requested theme or language; structured\nmetadata therefore records the values sent to the engine. A requested\ntheme or dark_mode persists on the engine account's profile; the change\nis reported in warnings but not undone (see ha_manage_theme\naction='set_engine_theme').\n\nTo change the Puppet engine app (add-on) itself (keep_browser_open,\nrestart), use ha_manage_app.",
    "inputSchema": {
      "properties": {
        "dashboard_path": {
          "type": "str | None",
          "description": "Legacy Lovelace frontend path to render, e.g. 'lovelace/0' (default dashboard, first view), 'lovelace-home/kitchen', or 'my-dashboard'. Leading slash optional. Prefer url_path + view_path for a stable named view. Mutually exclusive with url_path.",
          "default": null
        },
        "url_path": {
          "type": "str | None",
          "description": "Stable dashboard URL path, e.g. 'lovelace-home' or 'default'. Use with view_path instead of dashboard_path.",
          "default": null
        },
        "view_path": {
          "type": "str | None",
          "description": "Stable Lovelace views[].path value to render. Requires url_path.",
          "default": null
        },
        "width": {
          "type": "int",
          "description": "Viewport width in px.",
          "constraints": {
            "ge": 64,
            "le": 4096
          },
          "default": 1280
        },
        "height": {
          "type": "int | Literal['auto']",
          "description": "Viewport height in px (64-4096), or 'auto' for content height. full_page=True is a compatibility alias for 'auto'.",
          "default": 800
        },
        "viewport_presets": {
          "type": "list[ViewportPreset] | None",
          "description": "Render one or more named responsive viewports in this order: mobile (390x844), tablet (768x1024), desktop (1280x800). Overrides width/height.",
          "constraints": {
            "min_length": 1,
            "max_length": 3
          },
          "default": null
        },
        "orientation": {
          "type": "Orientation | None",
          "description": "Optional responsive orientation. Swaps viewport dimensions when needed; this does not rotate final pixels.",
          "default": null
        },
        "zoom": {
          "type": "float",
          "description": "Page zoom factor (1.0 = 100%).",
          "constraints": {
            "ge": 0.1,
            "le": 5.0
          },
          "default": 1.0
        },
        "wait_ms": {
          "type": "int",
          "description": "Extra render-settle time (ms) after the dashboard reports loaded. Raise it if a chart card (ApexCharts, mini-graph, history-graph) comes back blank.",
          "constraints": {
            "ge": 0,
            "le": 30000
          },
          "default": 2500
        },
        "full_page": {
          "type": "bool",
          "description": "Capture the whole scrollable dashboard instead of just the viewport (use when content runs below the fold). Uses Puppet's native auto-height capture (currently capped at 4000 px); raise wait_ms for lazy cards.",
          "default": false
        },
        "theme": {
          "type": "str | None",
          "description": "Installed Home Assistant frontend theme name, applied to this render.",
          "default": null
        },
        "dark_mode": {
          "type": "bool",
          "description": "Render the requested theme in dark mode.",
          "default": false
        },
        "language": {
          "type": "str | None",
          "description": "Frontend language code, e.g. 'en' or 'de'.",
          "default": null
        },
        "image_format": {
          "type": "ScreenshotFormat",
          "description": "Image format: png, jpeg, webp, or bmp.",
          "default": "png"
        },
        "render_timeout_seconds": {
          "type": "float",
          "description": "HTTP render timeout in seconds.",
          "constraints": {
            "ge": 1,
            "le": 300
          },
          "default": 60.0
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "tags": [
      "Dashboard",
      "beta"
    ],
    "source_file": "tools_dashboard_screenshot.py"
  },
  {
    "name": "ha_config_delete_dashboard",
    "title": "Delete Dashboard",
    "description": "Delete a storage-mode dashboard completely.\n\nWARNING: This permanently deletes the dashboard and all its configuration.\nCannot be undone. Does not work on YAML-mode dashboards.\n\nHA internal IDs may differ from url_path (e.g. hyphens → underscores); the tool resolves\neither form to the actual registry ID before deletion.\n\nEXAMPLES:\n- Delete dashboard: ha_config_delete_dashboard(\"mobile-dashboard\")\n\nNote: The default dashboard cannot be deleted via this method.",
    "inputSchema": {
      "properties": {
        "url_path": {
          "type": "str",
          "description": "Dashboard URL path or internal ID to delete (e.g., 'my-dashboard' or 'my_dashboard'). Both forms are accepted."
        }
      },
      "required": [
        "url_path"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Dashboards"
    ],
    "source_file": "tools_config_dashboards.py"
  },
  {
    "name": "ha_config_delete_dashboard_resource",
    "title": "Delete Dashboard Resource",
    "description": "Delete a dashboard resource.\n\nRemoves a resource from Home Assistant. The resource will no longer\nbe loaded on dashboards.\n\nWARNING: Deleting a resource used by custom cards in your dashboards\nwill cause those cards to fail to load.\n\nEXAMPLES:\nha_config_delete_dashboard_resource(resource_id=\"abc123\")\n\nNote: Use ha_config_list_dashboard_resources() to find resource IDs\nbefore deleting. Ensure no dashboards depend on the resource.",
    "inputSchema": {
      "properties": {
        "resource_id": {
          "type": "str",
          "description": "Resource ID to delete."
        }
      },
      "required": [
        "resource_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Dashboards"
    ],
    "source_file": "tools_resources.py"
  },
  {
    "name": "ha_config_get_dashboard",
    "title": "Get Dashboard",
    "description": "Get dashboard info - list all dashboards, get config, or search for cards.\n\nMODE 1 — List: list_only=True\n  Lists every dashboard's metadata (url_path, title, icon), storage and\n  YAML alike (metadata only — bodies are never included here).\n\nMODE 2 — Search: any of entity_id / card_type / heading provided\n  Finds cards, badges, and header cards matching the criteria, including\n  cards nested inside stacks, grids, conditional cards, button-card\n  custom_fields, and state-switch states. Each match carries a\n  python_path and a jq_path that locate the card for nested as well as\n  top-level cards. The python_path is a Python subscript chain to be\n  appended after `config` — e.g.\n  python_transform=f'config{m[\"python_path\"]}[\"icon\"] = \"mdi:x\"' (it is\n  NOT valid on its own without the `config` prefix). jq_path is the same\n  location in jq dot-notation.\n  Multiple criteria are AND-ed. Always fetches fresh config, bypassing the cache.\n  Search covers cards/card/custom_fields/states containers up to a depth\n  bound; if the dashboard carries a non-traversed child-bearing shape\n  (e.g. picture-elements `elements`), the result carries a `warnings`\n  entry naming where, so its hidden content is not mistaken for absent.\n  Strategy dashboards are not searchable (no explicit cards).\n\nMODE 3 — Get: Active when list_only=False and no search parameters are provided.\n  Returns the full Lovelace dashboard config, defaulting to the\n  main dashboard if url_path is omitted.\n  With view_path, `config_hash` still covers the FULL config, so a follow-up\n  ha_config_set_dashboard(python_transform=...) addressing\n  config['views'][view_index] validates unchanged. An unknown\n  view_path errors and lists the available view paths.\n  When you only need the render and not the config, use the\n  dedicated ha_get_dashboard_screenshot tool (registered when the\n  dashboard screenshot beta feature is on) instead; it returns images\n  without echoing the config.\n\nMODE 4 — Search all: mode=\"search\" with query=<entity_id or text>\n  Answers \"which dashboards contain this entity/card\" by walking every\n  storage-mode dashboard's views/cards/sections for the query substring.\n  Each match names the url_path, view, card_path, card_type, and the\n  matched field/value. Takes precedence over the other modes (list_only /\n  entity_id / card_type / heading are ignored when mode=\"search\").\n  YAML-mode dashboards are never searched on either path — the component\n  walk skips them in-process and the component-less legacy walk skips any\n  row tagged mode=\"yaml\" — because HA resolves `!secret` when loading a\n  YAML Lovelace config, so searching one could surface resolved secrets.\n  On installs without the ha_mcp_tools component, the default (unnamed)\n  dashboard is also not searched — only dashboards with a url_path are.\n\nReturn a stable `config_hash` (Get and Search modes only; not present in list_only mode) across consecutive reads of an unchanged config — `compute_config_hash` documents the underlying contract.\n\nEXAMPLES:\n- List all dashboards: ha_config_get_dashboard(list_only=True)\n- Get one view only: ha_config_get_dashboard(url_path=\"lovelace-mobile\", view_path=\"office\")\n- Find cards by entity (wildcards allowed): ha_config_get_dashboard(url_path=\"my-dash\", entity_id=\"sensor.temperature_*\")\n- Find heading: ha_config_get_dashboard(url_path=\"my-dash\", heading=\"Climate\", card_type=\"heading\")\n- Which dashboards use an entity: ha_config_get_dashboard(mode=\"search\", query=\"light.bedroom\")\n\nSEARCH WORKFLOW EXAMPLE:\n1. find = ha_config_get_dashboard(url_path=\"my-dash\", entity_id=\"light.bedroom\")\n2. ha_config_set_dashboard(\n       url_path=\"my-dash\",\n       config_hash=find[\"config_hash\"],\n       python_transform=f'config{find[\"matches\"][0][\"python_path\"]}[\"icon\"] = \"mdi:lamp\"'\n   )",
    "inputSchema": {
      "properties": {
        "url_path": {
          "type": "str | None",
          "description": "Dashboard URL path (e.g., 'lovelace-home'). Use 'default' for default dashboard.",
          "default": null
        },
        "list_only": {
          "type": "bool",
          "description": "When True, url_path is ignored.",
          "default": false
        },
        "force_reload": {
          "type": "bool",
          "description": "Force reload from storage (bypass cache). Not applicable in search mode.",
          "default": false
        },
        "entity_id": {
          "type": "str | None",
          "description": "Find cards by entity ID. Supports wildcards, e.g. 'sensor.temperature_*'. Matches cards with this entity in 'entity' or 'entities' field, view-level badges, and header cards.",
          "default": null
        },
        "card_type": {
          "type": "str | None",
          "description": "Find cards by type, e.g. 'tile', 'button', 'heading'.",
          "default": null
        },
        "heading": {
          "type": "str | None",
          "description": "Find cards by heading/title text (case-insensitive partial match).",
          "default": null
        },
        "include_config": {
          "type": "bool",
          "description": "In search mode: include each matched card's own configuration object in results. A container card's body includes its descendants, which are also separate matches with their own bodies, so nested stacks multiply the payload. Bodies are returned only for dashboards provably in storage mode; for a YAML or unconfirmed dashboard they are withheld (they may carry resolved !secret values) and the response says so, with match locations still reported. Ignored outside search mode.",
          "default": false
        },
        "include_screenshot": {
          "type": "bool",
          "description": "Get mode only: also return rendered image(s) of the dashboard for visual verification. Requires the 'dashboard screenshot' beta feature + engine app (add-on)/sidecar. If the feature is disabled the config is returned with a warning; if the engine is configured but the render fails, the call errors. Ignored in list/search mode.",
          "default": false
        },
        "view_path": {
          "type": "str | None",
          "description": "Get mode: return ONLY the view whose Lovelace views[].path matches (response carries 'view' + 'view_index' instead of the full 'config'). With include_screenshot, also selects the view to render. Ignored in list/search mode.",
          "default": null
        },
        "mode": {
          "type": "Literal['search'] | None",
          "description": "Set to 'search' (requires query).",
          "default": null
        },
        "query": {
          "type": "str | None",
          "description": "With mode='search': the entity_id or substring to find across all storage-mode dashboards. Ignored otherwise.",
          "default": null
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "readOnlyHint": true,
      "idempotentHint": true
    },
    "tags": [
      "Dashboards"
    ],
    "source_file": "tools_config_dashboards.py"
  },
  {
    "name": "ha_config_list_dashboard_resources",
    "title": "List Dashboard Resources",
    "description": "List Lovelace dashboard resources (custom cards, themes, CSS/JS).\n\nReturns one page of registered resources; `total_count` and `has_more`\nreport the full set, and `inline_count` and `by_type` summarise every\nresource, not just this page. Each resource has a unique ID for\nupdate/delete operations. For inline resources (created with\nha_config_set_dashboard_resource(content=...)), shows a preview of the\ncontent instead of the full encoded URL to save tokens.\n\nEXAMPLES:\n- Next page: ha_config_list_dashboard_resources(offset=100)\n- List with full content: ha_config_list_dashboard_resources(include_content=True)\n\nHome Assistant 2026.6+ exposes resource management in the UI by default;\nAPI access works regardless of UI availability.",
    "inputSchema": {
      "properties": {
        "include_content": {
          "type": "bool",
          "description": "Include full decoded content for inline resources in the \"_content\" field. When False, a 150-char preview is shown instead. Rows past the per-response content budget carry '_content_truncated': True instead of '_content'; request a smaller page before copying content into an update.",
          "default": false
        },
        "limit": {
          "type": "int",
          "default": 100,
          "constraints": {
            "ge": 1,
            "le": 500
          },
          "description": "Max resources to return per page"
        },
        "offset": {
          "type": "int",
          "default": 0,
          "constraints": {
            "ge": 0
          },
          "description": "Number of resources to skip for pagination"
        }
      }
    },
    "annotations": {
      "openWorldHint": true,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Dashboards"
    ],
    "source_file": "tools_resources.py"
  },
  {
    "name": "ha_config_set_dashboard",
    "title": "Create or Update Dashboard",
    "description": "Create or update a Home Assistant dashboard.\n\nMUST call ha_get_skill_guide OR refer to your locally installed skills first.\n`dashboard-guide.md` and `dashboard-cards.md` ship under `skill_content`\nby default.\n\nMODES (pick one):\n- patch: edit known paths with literal values using JSON Patch\n  add/remove/replace/test and config_hash, e.g.\n  patch=[{\"op\": \"replace\", \"path\": \"/views/0/title\", \"value\": \"Home\"}].\n  move/copy are unsupported. Full guide:\n  https://github.com/homeassistant-ai/ha-mcp/blob/master/docs/dashboard-edits.md\n- python_transform: loops or pattern-based changes across cards and views,\n  e.g. 'config[\"views\"][0][\"cards\"].append({\"type\": \"button\", \"entity\": \"light.bedroom\"})'.\n  After delete/add operations indices shift, so chain multiple ops in ONE\n  expression where possible; a later call needs a fresh config_hash from\n  ha_config_get_dashboard().\n- config: new dashboards only, or a full restructure. Replaces everything.\n  Omit it to create a dashboard without initial config.\n\nUse ha_config_get_dashboard(entity_id=...) to get the path of any card,\nand ha_search / ha_get_overview to find entity IDs — never guess them.\nFor visual re-checks after the write use ha_get_dashboard_screenshot\ninstead of re-sending config.\n\ntitle/icon/require_admin/show_in_sidebar can be updated in a\nmetadata-only call or alongside a full config replacement; with\npython_transform or patch, update metadata in a separate call (combining\nit with patch is rejected). Strategy dashboards (config\n{\"strategy\": {...}}) cannot be converted to custom dashboards here; use\n\"Take Control\" in the Home Assistant UI.\n\nEXAMPLE:\nha_config_set_dashboard(url_path=\"home-dashboard\", title=\"Home Overview\",\n    config={\"views\": [{\"title\": \"Home\", \"type\": \"sections\", \"sections\": [{\"title\": \"Climate\", \"cards\": [{\"type\": \"tile\", \"entity\": \"climate.living_room\"}]}]}]})\n\nThis tool manages storage-mode dashboards only. YAML-mode dashboards (a\n.yaml file registered under ``lovelace: dashboards:`` in\nconfiguration.yaml) and dashboards inlined under ``lovelace:`` are\nedited in their .yaml file; ``ha_config_set_yaml`` can update the\n``lovelace:`` registration but not the dashboard body.",
    "inputSchema": {
      "properties": {
        "url_path": {
          "type": "str",
          "description": "Dashboard URL path (e.g., 'my-dashboard'). Use 'default' or 'lovelace' for the default dashboard. New dashboards must use a hyphenated path."
        },
        "config": {
          "type": "dict[str, Any] | None",
          "description": "Dashboard configuration with views and cards. Omit or set to None to create dashboard without initial config. Mutually exclusive with python_transform and patch.",
          "default": null
        },
        "python_transform": {
          "type": "str | None",
          "description": "Python expression to transform existing dashboard config. Mutually exclusive with config and patch. Requires config_hash for validation. See PYTHON TRANSFORM SECURITY below for allowed operations. Examples: Simple: python_transform=\"config['views'][0]['cards'][0]['icon'] = 'mdi:lamp'\" Pattern: python_transform=\"for card in config['views'][0]['cards']: if 'light' in card.get('entity', ''): card['icon'] = 'mdi:lightbulb'\" Multi-op: python_transform=\"config['views'][0]['cards'][0]['icon'] = 'mdi:lamp'; del config['views'][0]['cards'][2]\" \n\nPYTHON TRANSFORM SECURITY:\n\n✅ ALLOWED:\n- Dictionary/list access: config['views'][0]['cards'][1]\n- Slicing: config['views'][0]['cards'][1:3]\n- Assignment: config['key'] = 'value'\n- Deletion: del config['key'] or config.pop('key')\n- List methods: append, insert, pop, remove, clear, extend\n- Dict methods: update, get, setdefault, keys, values, items\n- Loops: for, if/else, pass, break, continue\n- Comprehensions: [x for x in ...], {k: v for ...}, (x for x in ...)\n- Ternary: x if condition else y\n- Iterable unpacking (* in calls/literals): f(*xs), [*xs, y]\n- Dict unpacking (**) in calls and dict literals: {**d, 'k': v}\n- Keyword arguments: func(key=value)\n- Lambdas (e.g. for `key=`): sorted(items, key=lambda x: x['score'])\n- String methods: startswith, endswith, lower, upper, strip, split, join, replace\n- Safe builtins: isinstance, len, range, enumerate, zip, sorted, reversed,\n  min, max, sum, abs, any, all, round, str, int, float, bool, list, dict,\n  tuple, set\n\n❌ FORBIDDEN:\n- Imports: import, from, __import__\n- File operations: open, read, write\n- Dunder access: __class__, __bases__, __subclasses__\n- Dangerous builtins: eval, exec, compile, getattr, setattr, delattr, hasattr\n- Function definitions: def, class\n- Exception handling: try/except (validate with isinstance/in/.get() instead)\n- While loops: use bounded for loops or comprehensions instead",
          "default": null
        },
        "config_hash": {
          "type": "str | None",
          "description": "Config hash from ha_config_get_dashboard for optimistic locking. REQUIRED for python_transform and patch (validates dashboard unchanged). Optional for config (validates before full replacement if provided).",
          "default": null
        },
        "title": {
          "type": "str | None",
          "description": "Dashboard display name shown in sidebar",
          "default": null
        },
        "icon": {
          "type": "str | None",
          "description": "MDI icon name (e.g., 'mdi:home', 'mdi:cellphone'). Defaults to 'mdi:view-dashboard'",
          "default": null
        },
        "require_admin": {
          "type": "bool | None",
          "description": "Restrict dashboard to admin users only. For existing dashboards, only updated when explicitly provided.",
          "default": null
        },
        "show_in_sidebar": {
          "type": "bool | None",
          "description": "Show dashboard in sidebar navigation. For existing dashboards, only updated when explicitly provided.",
          "default": null
        },
        "MandatoryBPS": {
          "type": "bool",
          "default": true
        },
        "BestPracticeKey": {
          "type": "BestPracticeKeyParam",
          "default": null
        },
        "return_screenshot": {
          "type": "bool",
          "description": "After writing, also return rendered image(s) of the dashboard. Requires the 'dashboard screenshot' beta feature + engine app (add-on)/sidecar; if unavailable, the write result is returned with a warning.",
          "default": false
        },
        "view_path": {
          "type": "str | None",
          "description": "With return_screenshot: stable Lovelace views[].path to render.",
          "default": null
        },
        "patch": {
          "type": "list[dict[str, Any]] | None",
          "description": "Structured dashboard edits: up to 100 JSON Patch add, remove, replace or test operations using RFC 6901 paths. Use /- to append to an array; escape ~ as ~0 and / as ~1 in keys. Mutually exclusive with config and python_transform. Strings in value are preserved literally.",
          "default": null
        }
      },
      "required": [
        "url_path"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Dashboards"
    ],
    "source_file": "tools_config_dashboards.py"
  },
  {
    "name": "ha_config_set_dashboard_resource",
    "title": "Set Dashboard Resource",
    "description": "Create or update a dashboard resource (inline code or external URL).\n\nProvide exactly one of:\n- content: Inline JavaScript or CSS code (embedded in the resource URL\n  as a data: URI — no file storage or external hosting involved).\n  Supports 'module' and 'css' types only (not 'js'); URLs are\n  deterministic (same content = same URL); up to ~128KB. Content must be\n  self-contained: a data: URI has no base URL, so relative imports inside\n  a module and relative url() references inside CSS cannot resolve (use\n  fully-qualified URLs instead). If Home Assistant is behind a reverse\n  proxy that injects a Content-Security-Policy without 'data:' in\n  script-src/style-src, the browser blocks these resources: this call\n  still succeeds and the card simply never renders. Register the code as\n  a file and use url='/local/...' on such a deployment. (HA itself ships\n  no CSP.)\n- url: External resource URL — /local/... (files in /config/www/),\n  /hacsfiles/... (HACS-installed cards) or https://... (CDN). Supports\n  all types: 'module', 'js', 'css'.\n\nEXAMPLES:\n- Inline: ha_config_set_dashboard_resource(content=\"class MyCard extends HTMLElement { ... } customElements.define('my-card', MyCard);\", resource_type=\"module\")\n- HACS card (after ha_manage_hacs(action='download')): ha_config_set_dashboard_resource(url=\"/hacsfiles/lovelace-mushroom/mushroom.js\", resource_type=\"module\")\n- Update: ha_config_set_dashboard_resource(url=\"/local/my-card-v2.js\", resource_type=\"module\", resource_id=\"abc123\")\n\nAfter adding a resource, a browser cache clear or hard refresh\n(Ctrl+Shift+R) is needed to load it.",
    "inputSchema": {
      "properties": {
        "content": {
          "type": "str | None",
          "description": "JavaScript or CSS code to host inline.",
          "default": null
        },
        "url": {
          "type": "str | None",
          "description": "URL of the resource.",
          "default": null
        },
        "resource_type": {
          "type": "Literal['module', 'js', 'css']",
          "description": "Resource type: 'module' for ES6 modules (modern cards), 'js' for legacy JavaScript (older custom cards), 'css' for stylesheets (themes, global styles)",
          "default": "module"
        },
        "resource_id": {
          "type": "str | None",
          "description": "Resource ID to update. If omitted, creates a new resource. Get IDs from ha_config_list_dashboard_resources()",
          "default": null
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Dashboards"
    ],
    "source_file": "tools_resources.py"
  },
  {
    "name": "ha_dev_manage_server",
    "title": "Manage MCP Server (dev)",
    "description": "Manage the running ha-mcp server itself (developer mode).\n\nWhen NOT to use: to restart Home Assistant use ha_restart; to\nupdate Home Assistant Apps (add-ons) or HACS packages use\nha_manage_app / ha_manage_hacs.\n\nCaveats: update_source changes ONLY the ha_mcp_tools custom\ncomponent's separate in-process server entry — it never updates\nthe app (add-on), Docker, standalone, or PyPI server that may be\nserving this connection (update those via ha_manage_app /\ndocker pull / pip). In embedded mode that entry IS this server,\nso the update self-interrupts; elsewhere this connection is\nuntouched and keeps its current version. Success means the\nentry's options were applied — the component then reinstalls in\nthe background, which can take minutes and can still fail\n(check HA logs). update_source requires the component's\nin-process server entry to exist. restart interrupts this MCP\nconnection in embedded and app deployments (the reply\narrives just before the server goes down) and supports those\ntwo deployments only (standalone processes must be restarted\nexternally). approve/deny require the separate\n'dev_tools_security_policy_access' setting, off by default.\n\nEXAMPLES:\nha_dev_manage_server(\"update_source\", pip_spec=\"https://github.com/homeassistant-ai/ha-mcp/archive/refs/pull/1234/head.tar.gz\")\nha_dev_manage_server(\"update_source\", pip_spec=\"clear\", channel=\"stable\")\nha_dev_manage_server(\"approve\", token=\"abc123\")",
    "inputSchema": {
      "properties": {
        "action": {
          "type": "Literal['info', 'update_source', 'restart', 'list_pending', 'approve', 'deny']",
          "description": "info: deployment/version report; update_source: point the ha_mcp_tools component's separate in-process server at a channel or pip spec and reinstall it; restart: restart this server; list_pending: list tool calls blocked on a security-policy approval; approve / deny: decide one blocked call by token"
        },
        "channel": {
          "type": "str | None",
          "default": null,
          "description": "Release channel for update_source: 'stable' or 'dev'"
        },
        "pip_spec": {
          "type": "str | None",
          "default": null,
          "description": "Explicit pip requirement for update_source — a version pin (ha-mcp==7.9.0) or a GitHub tarball URL such as https://github.com/homeassistant-ai/ha-mcp/archive/refs/pull/<PR>/head.tar.gz. The bare name 'clear' (case-insensitive) is reserved: it clears the override and falls back to the release channel instead of being treated as a requirement. An empty string also clears but some MCP clients mangle it in transit — prefer 'clear'."
        },
        "token": {
          "type": "str | None",
          "default": null,
          "description": "Approval token (required for approve/deny)"
        }
      },
      "required": [
        "action"
      ]
    },
    "annotations": {
      "openWorldHint": true,
      "destructiveHint": true
    },
    "tags": [
      "Developer"
    ],
    "source_file": "tools_dev.py"
  },
  {
    "name": "ha_dev_manage_settings",
    "title": "Manage Server Settings (dev)",
    "description": "Manage ha-mcp server settings and the Tools/Policies/Backups surfaces (developer mode).\n\nDrives everything the web settings UI can change: the Server\nSettings matrix, the Tools tab, the Tool Security Policies editor,\nand the auto-backup config. Use ha_dev_manage_server for\nthe live approval queue and to restart.\n\nWhen NOT to use: for HA entity/automation configuration use the\nha_config_* tools.\n\nCaveats: enable/disable/pin and most server settings take effect\nonly after a restart (ha_dev_manage_server action=\"restart\");\nLLM-API exposure, security gates, and policy edits apply live.\nEnv-pinned settings/tools are read-only until the env var is unset.\nThese actions can flip security-sensitive state — treat with the\nsame care as editing the web UI. The policy-override surfaces\n(set_policy, set_tool with gated=, and set/reset of\nenable_tool_security_policies) additionally require the\n'dev_tools_security_policy_access' setting, which is off by\ndefault; reads are always available. That setting itself is never\nwritable by these tools — change it in the web settings UI or via\nHAMCP_DEV_SECURITY_POLICY_ACCESS.\n\nEXAMPLES:\nha_dev_manage_settings(\"list_tools\")\nha_dev_manage_settings(\"set_tool\", tool=\"ha_write_file\", state=\"disabled\")\nha_dev_manage_settings(\"set_tool\", tool=\"ha_call_service\", gated=True)\nha_dev_manage_settings(\"get_policy\")\nha_dev_manage_settings(\"set_backup_config\", backup={\"enable_auto_backup\": False})",
    "inputSchema": {
      "properties": {
        "action": {
          "type": "Literal['list', 'set', 'reset', 'list_tools', 'set_tool', 'get_policy', 'set_policy', 'get_backup_config', 'set_backup_config']",
          "description": "Server-settings matrix: list / set / reset. Tools tab: list_tools / set_tool (enable-disable-pin, LLM-API, security gate). Security policies: get_policy / set_policy. Auto-backup config: get_backup_config / set_backup_config."
        },
        "setting": {
          "type": "str | None",
          "default": null,
          "description": "Setting name (required for set/reset)"
        },
        "value": {
          "type": "bool | int | float | str | None",
          "default": null,
          "description": "New value (required for set)"
        },
        "tool": {
          "type": "str | None",
          "default": null,
          "description": "Tool name (required for set_tool)"
        },
        "state": {
          "type": "Literal['enabled', 'disabled', 'pinned'] | None",
          "default": null,
          "description": "set_tool: enable, disable, or pin the tool"
        },
        "llm_api": {
          "type": "bool | None",
          "default": null,
          "description": "set_tool: expose the tool to HA conversation agents (effective only on the embedded custom-component server)"
        },
        "gated": {
          "type": "bool | None",
          "default": null,
          "description": "set_tool: require user approval before every call to this tool (adds/removes an unconditional security-policy rule)"
        },
        "policy": {
          "type": "dict[str, Any] | None",
          "default": null,
          "description": "set_policy: the full policy object {wait_seconds, approval_ttl_minutes, event_decisions_enabled, rules, version, schema_version}. Replaces the WHOLE document: a field you omit reverts to its default, so send back an edited copy of get_policy rather than a fragment (omitting event_decisions_enabled switches the event-bus approval channel off)"
        },
        "expected_version": {
          "type": "int | None",
          "default": null,
          "description": "set_policy: the version from your last get_policy, for optimistic-concurrency safety (else the policy's own version field is used)"
        },
        "backup": {
          "type": "dict[str, Any] | None",
          "default": null,
          "description": "set_backup_config: {field: value} of auto-backup settings to change (see get_backup_config for field names)"
        }
      },
      "required": [
        "action"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Developer"
    ],
    "source_file": "tools_dev.py"
  },
  {
    "name": "ha_get_device",
    "title": "Get Device (incl. Zigbee/ZHA/Z2M, Z-Wave and Matter)",
    "description": "Get device information with pagination, including Zigbee (ZHA/Z2M) and Z-Wave JS devices.\n\nWithout device_id/entity_id: Lists devices with optional filters and pagination.\nWith device_id or entity_id: Returns full detail for that specific device.\n\n**List devices (paginated):**\n- First page: ha_get_device()\n- Next page: ha_get_device(offset=50)\n- By area: ha_get_device(area_id=\"living_room\")\n- By integration: ha_get_device(integration=\"zigbee2mqtt\")\n- Full details in list: ha_get_device(detail_level=\"full\", limit=10)\n\n**Single device lookup (always full detail):**\n- By device_id: ha_get_device(device_id=\"abc123\")\n- By entity_id: ha_get_device(entity_id=\"light.living_room\")\n\n**Zigbee:** integration=\"zha\" or \"zigbee2mqtt\". Returns ieee_address, radio metrics.\n**Z-Wave:** integration=\"zwave_js\". Returns node_id, node_status.\n**Matter:** integration=\"matter\". Returns node_diagnostics (network type,\nreachability, IPs, fabrics). For management use ha_manage_radio.",
    "inputSchema": {
      "properties": {
        "device_id": {
          "type": "str | None",
          "description": "Device ID to retrieve details for.",
          "default": null
        },
        "entity_id": {
          "type": "str | None",
          "description": "Entity ID to find the associated device for (e.g., 'light.living_room')",
          "default": null
        },
        "integration": {
          "type": "str | None",
          "description": "Filter devices by integration: 'zha', 'zigbee2mqtt', 'zwave_js', 'mqtt', 'hue', etc.",
          "default": null
        },
        "area_id": {
          "type": "str | None",
          "description": "Filter devices by area ID (e.g., 'living_room')",
          "default": null
        },
        "manufacturer": {
          "type": "str | None",
          "description": "Filter devices by manufacturer name (e.g., 'Philips')",
          "default": null
        },
        "limit": {
          "type": "int",
          "default": 50,
          "constraints": {
            "ge": 1,
            "le": 200
          },
          "description": "Max devices to return per page in list mode"
        },
        "offset": {
          "type": "int",
          "default": 0,
          "constraints": {
            "ge": 0
          },
          "description": "Number of devices to skip for pagination"
        },
        "detail_level": {
          "type": "Literal['summary', 'full']",
          "default": "summary",
          "description": "'summary': basic device info and protocol identifiers. 'full': in list mode also include each device's entities. Single-device lookups always return full detail, including radio metrics, node status and Matter diagnostics."
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Device Registry",
      "Matter",
      "Z-Wave",
      "Zigbee"
    ],
    "source_file": "tools_registry.py"
  },
  {
    "name": "ha_remove_device",
    "title": "Remove Device",
    "description": "Remove an orphaned device from the Home Assistant device registry.\n\nWARNING: This removes the device entry from the registry.\n- Use only for orphaned devices that are no longer connected\n- Active devices will typically be re-added by their integration\n- Associated entities may also be removed\n\nThis uses the config entry removal which is the safe way to remove devices.\nIf the device has multiple config entries, they must all be removed.\n\nEXAMPLES:\n- Remove orphaned device: ha_remove_device(\"abc123def456\")\n\nNOTE: For most use cases, consider disabling the device instead:\nha_set_device(device_id=\"abc123\", disabled_by=\"user\")",
    "inputSchema": {
      "properties": {
        "device_id": {
          "type": "str",
          "description": "Device ID to remove from the registry"
        }
      },
      "required": [
        "device_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": true
    },
    "tags": [
      "Device Registry"
    ],
    "source_file": "tools_registry.py"
  },
  {
    "name": "ha_set_device",
    "title": "Set Device",
    "description": "Update device properties such as name, area, disabled state, or labels.\n\nIMPORTANT: Renaming a device does NOT rename its entities!\nDevice and entity names are independent. To rename entities, use ha_set_entity(new_entity_id=...).\n\nCommon workflow for full rename:\n1. ha_set_device(device_id=\"abc\", name=\"Living Room Sensor\")  # Rename device\n2. ha_set_entity(\"sensor.old\", new_entity_id=\"sensor.living_room\")  # Rename entities separately\n\nEXAMPLES:\n- Rename device: ha_set_device(\"abc123\", name=\"Living Room Hub\")\n- Move to area: ha_set_device(\"abc123\", area_id=\"living_room\")\n- Disable device: ha_set_device(\"abc123\", disabled_by=\"user\")\n- Enable device: ha_set_device(\"abc123\", disabled_by=\"\")\n- Add labels: ha_set_device(\"abc123\", labels=[\"important\", \"sensor\"])",
    "inputSchema": {
      "properties": {
        "device_id": {
          "type": "str",
          "description": "Device ID to update"
        },
        "name": {
          "type": "str | None",
          "description": "New display name for the device (sets name_by_user)",
          "default": null
        },
        "area_id": {
          "type": "str | None",
          "description": "Area/room ID to assign the device to. Use empty string '' to unassign.",
          "default": null
        },
        "disabled_by": {
          "type": "str | None",
          "description": "Set to 'user' to disable, or None/empty string to enable",
          "default": null
        },
        "labels": {
          "type": "str | list[str] | None",
          "description": "Labels to assign to the device (replaces existing labels)",
          "default": null
        }
      },
      "required": [
        "device_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Device Registry"
    ],
    "source_file": "tools_registry.py"
  },
  {
    "name": "ha_manage_energy_prefs",
    "title": "Manage Energy Dashboard Preferences",
    "description": "Manage the Home Assistant Energy Dashboard preferences: grid / solar /\nbattery / gas / water energy sources, device consumption sensors for\nelectricity and water, and cost tariffs.\n\nWHEN TO USE:\n- mode='get' / 'set': inspect or replace the full Energy Dashboard\n  config. Use 'set' for bulk edits or anything touching multiple\n  top-level keys at once.\n- mode='add_device' / 'remove_device': add or remove a single\n  device-consumption entry. The tool performs a fresh read-modify-write\n  internally; the caller does NOT manage config_hash.\n- mode='add_source': append a single entry to ``energy_sources``.\n  Same atomic read-modify-write semantics.\n\nWHEN NOT TO USE:\n- To create the underlying statistics themselves — they must already\n  exist as HA entities before being referenced here; create them via\n  the relevant integration's config flow first.\n\nCAVEATS:\n- ``energy/save_prefs`` has per-key FULL-REPLACE semantics. Passing\n  ``{\"device_consumption\": [<one entry>]}`` deletes every other device\n  the user had configured — silently, with no error. mode='set'\n  requires a fresh ``config_hash`` for optimistic locking; convenience\n  modes hide this entirely.\n- The per-key ``config_hash`` form lets an agent submit only the\n  top-level key it wants to change: ``config`` keys must equal the dict\n  keys (any key outside the canonical set is rejected with\n  ``VALIDATION_FAILED``), and a per-key submission still fully replaces\n  that key's value. A mismatch on any locked key returns\n  ``RESOURCE_LOCKED`` with the offending keys in ``mismatched_keys``.\n- ``dry_run=True`` skips the hash check entirely for both forms.\n- Writes need an administrator token; Home Assistant rejects the save\n  otherwise.\n- After a successful write, the tool calls ``energy/validate`` and\n  returns residual issues (missing stats, unit mismatches) as\n  ``post_save_validation_errors``; the save persists regardless —\n  correct the config and write again if needed.\n- 'add_source' rejects duplicates by ``(type, stat_energy_from)`` for\n  solar/battery/gas/water; grid entries are appended without a duplicate\n  check (multiple grid variants are legitimate), so the caller\n  de-duplicates grid sources.",
    "inputSchema": {
      "properties": {
        "mode": {
          "type": "Literal['get', 'set', 'add_device', 'remove_device', 'add_source']",
          "description": "Operation mode. Primitives: 'get' reads the current prefs; 'set' writes a full prefs payload."
        },
        "config": {
          "type": "dict[str, Any] | None",
          "description": "Full prefs payload for mode='set'. Must contain the top-level keys you intend to replace: 'energy_sources', 'device_consumption', 'device_consumption_water'. Any omitted key is preserved. Call with mode='get' first, mutate the returned config, then pass the whole object back. Ignored by convenience modes.",
          "default": null
        },
        "config_hash": {
          "type": "str | dict[_PrefsKey, str] | None",
          "description": "Hash from a previous mode='get' call. REQUIRED for mode='set' unless dry_run=True. Two forms: str (full-blob lock) or dict (per-key lock, taken from the config_hash_per_key field of mode='get'). Pass the dict form as a native object, NOT a JSON-encoded string — a stringified dict is treated as a full-blob token and will report RESOURCE_LOCKED; clients that can only send strings should use the str full-blob form. Ignored by convenience modes.",
          "default": null
        },
        "dry_run": {
          "type": "bool",
          "description": "If True, no write is performed. For mode='set': runs a local shape check on the proposed config AND calls the server's energy/validate against the CURRENT persisted state. For convenience modes: simulates the mutation against a fresh read and reports what would change without writing — but still raises RESOURCE_ALREADY_EXISTS (duplicate add_device, or duplicate add_source for solar/battery/gas/water), RESOURCE_NOT_FOUND (missing remove_device), or VALIDATION_FAILED (post-mutator shape error) when the proposed mutation is not applicable.",
          "default": false
        },
        "stat_consumption": {
          "type": "str | None",
          "description": "Statistic entity_id for mode='add_device' / 'remove_device' (e.g. 'sensor.fridge_energy'). Required for those modes; ignored otherwise.",
          "default": null
        },
        "name": {
          "type": "str | None",
          "description": "Display name for mode='add_device'; ignored otherwise.",
          "default": null
        },
        "included_in_stat": {
          "type": "str | None",
          "description": "'Parent' statistic for mode='add_device'. Set this to a statistic that already INCLUDES this device's consumption (e.g., a whole-home or circuit-level meter that this device feeds into). The Energy Dashboard will subtract this device's reading from the parent so the parent's contribution is not double-counted. Ignored otherwise.",
          "default": null
        },
        "water": {
          "type": "bool",
          "description": "If True, mode='add_device' / 'remove_device' targets 'device_consumption_water' instead of 'device_consumption'.",
          "default": false
        },
        "source": {
          "type": "dict[str, Any] | None",
          "description": "Single energy_sources entry for mode='add_source'. Must contain 'type' (one of grid|solar|battery|gas|water) and the type-specific required fields (e.g. solar/battery/gas/water require 'stat_energy_from'). Every source type also accepts an optional 'name' (display label in the energy graphs); battery additionally accepts 'stat_soc' (state-of-charge statistic). Note: HA Core's voluptuous schema for grid sources requires the full field set (cost_adjustment_day, stat_energy_to, stat_cost, entity_energy_price, number_energy_price, entity_energy_price_export, number_energy_price_export, stat_compensation) — the local shape check is narrower, so a minimal {'type': 'grid'} passes locally but surfaces in post_save_validation_errors after writing. Pass the unused fields as None to satisfy the server. Required for mode='add_source'; ignored otherwise.",
          "default": null
        }
      },
      "required": [
        "mode"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": false
    },
    "tags": [
      "Energy"
    ],
    "source_file": "tools_energy.py"
  },
  {
    "name": "ha_get_entity",
    "title": "Get Entity",
    "description": "Get entity registry information for one or more entities.\n\nReturns detailed entity registry metadata: area, custom name/icon,\noriginal name, disabled_by/hidden_by (with enabled/hidden shorthands),\naliases (a null entry = the entity's own name), labels, categories,\ndevice_class override, per-domain options, platform, device_id,\nconfig_entry_id and unique_id.\n\nRESOLVER MODE:\nPass unique_id (instead of entity_id) to resolve a stable integration\nunique_id to its entity_id(s). Since the registry's unique key is\n(domain, platform, unique_id), the same unique_id can match multiple\nplatforms — all matches are returned in entity_entries with a `matches`\ncount. Narrow with domain/platform. Resolver reads as_partial_dict, so\naliases and the device_class override come back as defaults ([]/null).\n\nWhen config_entry_id is non-null — e.g. for UI-created template / group /\nutility_meter / derivative helpers — pass it to\n``ha_get_integration(entry_id=..., include_options=True)`` to read the\nhelper's current config (template body, group members, etc.) without\nscanning a domain list. Voice-assistant exposure is stored under options\nbut is set via ha_set_entity(expose_to=...).\n\nResolved-name enrichment (present only when the ha_mcp_tools component\nadvertises it; otherwise these keys are absent): `area` (assigned area\nNAME, device-inherited when the entity has none), `floor`, and\n`label_names`. This tool's base `labels` carries the label ids;\nha_search result_fields and ha_get_entity_exposure instead emit the\nresolved names under `labels`.\n\nRELATED TOOLS: ha_set_entity() to modify these properties; ha_get_state()\nfor current state/attributes; ha_search() to find entities.\n\nEXAMPLES:\n- Single entity: ha_get_entity(\"sensor.temperature\")\n- Multiple entities: ha_get_entity([\"light.living_room\", \"switch.porch\"])\n- Resolve a unique_id: ha_get_entity(unique_id=\"00:11:22:33\", domain=\"sensor\")",
    "inputSchema": {
      "properties": {
        "entity_id": {
          "type": "str | list[str] | None",
          "description": "Entity ID or list of entity IDs to retrieve (e.g., 'sensor.temperature' or ['light.living_room', 'switch.porch']).",
          "default": null
        },
        "unique_id": {
          "type": "str | None",
          "description": "Stable integration unique_id (entity_id is mutable, unique_id is not). Mutually exclusive with entity_id.",
          "default": null
        },
        "domain": {
          "type": "str | None",
          "description": "Resolver filter (unique_id mode only): restrict matches to this entity domain, e.g. 'sensor'.",
          "default": null
        },
        "platform": {
          "type": "str | None",
          "description": "Resolver filter (unique_id mode only): restrict matches to this integration platform, e.g. 'hue'.",
          "default": null
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "readOnlyHint": true,
      "idempotentHint": true
    },
    "tags": [
      "Entity Registry"
    ],
    "source_file": "tools_entities.py"
  },
  {
    "name": "ha_get_entity_exposure",
    "title": "Get Entity Exposure",
    "description": "Get entity exposure settings - list all or get settings for a specific entity.\n\nWithout an entity_id: lists the entities that have explicit exposure\nsettings for voice assistants (Alexa, Google Assistant, Assist) as\n`exposed_entities`, plus a per-assistant `summary` count. An entity absent\nfrom that list uses the default exposure settings.\n\nWith an entity_id: returns `exposed_to` (assistant -> True/False from the\nentity's explicit settings), `is_exposed_anywhere`, and\n`has_custom_settings`. When has_custom_settings is False the defaults\napply and exposed_to is all False; the default values themselves are\nnot computed.\n\nEXAMPLES:\n- Filter by assistant: ha_get_entity_exposure(assistant=\"cloud.alexa\")\n- Get specific entity: ha_get_entity_exposure(entity_id=\"light.living_room\")\n\nWhen the ha_mcp_tools component advertises the exposure capability, each\nrecord is additively enriched with the entity's name/area so no second\nha_search is needed to identify it: friendly_name, domain, area, floor,\nand labels (plus state for entities that have one) on a single-entity\nlookup, and a parallel entity_info map keyed by entity_id when listing.\nThese fields are absent when the component is unavailable.",
    "inputSchema": {
      "properties": {
        "entity_id": {
          "type": "str | None",
          "description": "Entity ID to check exposure settings for.",
          "default": null
        },
        "assistant": {
          "type": "str | None",
          "description": "Filter by assistant: 'conversation', 'cloud.alexa', or 'cloud.google_assistant'.",
          "default": null
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Entity Registry"
    ],
    "source_file": "tools_voice_assistant.py"
  },
  {
    "name": "ha_remove_entity",
    "title": "Remove Entity",
    "description": "Remove one or more entities from the Home Assistant entity registry.\n\nPermanently removes the entity registration; the entity will no longer\nappear in the UI or be available to automations. Use only for orphaned\nor stale entity entries: if the underlying device or integration is\nstill active, the entity may be re-added automatically on the next HA\nrestart or reload. This cannot be undone without restoring from backup.\nFor most use cases, consider disabling instead:\nha_set_entity(entity_id=\"sensor.old\", enabled=False).\n\nBULK MODE:\nPass a list of entity IDs to remove up to 100 at once — handy for\nclearing the restored=true orphans an integration leaves behind after\nits filters change. Removals run sequentially and return:\n  {removed: [...], skipped: [...], errors: [{entity_id, code, message}]}\nwhere skipped = ids already absent (not-found is idempotent, not an\nerror). Bulk mode is NOT auto-backed-up (the snapshot is single-entity);\nsingle-id removal still is.\n\nEXAMPLES:\n- Remove orphaned sensor: ha_remove_entity(\"sensor.old_temperature\")\n- Bulk cleanup: ha_remove_entity([\"sensor.orphan_1\", \"sensor.orphan_2\"])\n\nUse ha_search / ha_get_entity to verify an entity before removing it.",
    "inputSchema": {
      "properties": {
        "entity_id": {
          "type": "str | list[str]",
          "description": "Entity ID, or a list of entity IDs, to remove from the entity registry (e.g., 'sensor.old_temperature')."
        }
      },
      "required": [
        "entity_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": true
    },
    "tags": [
      "Entity Registry"
    ],
    "source_file": "tools_entities.py"
  },
  {
    "name": "ha_set_entity",
    "title": "Set Entity",
    "description": "Update entity properties in the entity registry.\n\nAllows modifying entity metadata such as area assignment, display name,\nicon, \"Show As\" device class override, per-domain registry options,\nenabled/disabled state, visibility, aliases, labels, voice assistant\nexposure, and entity_id rename in a single call.\n\nBULK OPERATIONS:\nWhen entity_id is a list, only labels, expose_to, and categories parameters are supported.\nOther parameters (area_id, name, icon, device_class, options, enabled, hidden, aliases, use_entity_name_alias, new_entity_id, new_device_name) require single entity.\n\nSHOW AS / DEVICE CLASS: a device_class change applies instantly, no reload\nneeded.\n\nREGISTRY OPTIONS: multi-domain options are sent as separate registry\nupdates because HA's WS schema requires options_domain + options to be\npaired one domain at a time.\n\nENTITY ID RENAME: Voice exposure settings are preserved automatically.\n\nWARNING: Renaming an entity_id does NOT update references in automations,\nscripts, templates, or dashboards. All consumers of the old entity_id must\nbe updated manually — HA does not propagate the rename automatically.\n\nRename limitations:\n- Entity history is preserved (HA 2022.4+)\n- Entities without unique IDs cannot be renamed\n- Entities disabled by their integration cannot be renamed\n\nDEVICE RENAME:\nnew_device_name can be combined with new_entity_id to rename both in one\ncall. The device is looked up automatically.\n\nUse ha_search() or ha_get_device() to find entity IDs.\nUse ha_config_get_label() to find available label IDs.\n\nEXAMPLES:\n- Assign to area: ha_set_entity(\"sensor.temp\", area_id=\"living_room\")\n- Set Show As: ha_set_entity(\"binary_sensor.zone_10\", device_class=\"window\")\n- Set sensor precision: ha_set_entity(\"sensor.power\", options={\"sensor\": {\"display_precision\": 2}})\n- Rename entity and device: ha_set_entity(\"light.old\", new_entity_id=\"light.new\", new_device_name=\"New Lamp\")\n- Add labels to multiple: ha_set_entity([\"light.a\", \"light.b\"], labels=[\"new\"], label_operation=\"add\")\n- Expose to Alexa: ha_set_entity(\"light.lamp\", expose_to={\"cloud.alexa\": True})\n\nENABLED/DISABLED WARNING:\nA disabled entity will NOT appear in state queries, dashboards, or\nautomations until re-enabled AND the integration is reloaded. This is\nNOT the same as \"turning off\" an entity.\n\nFor automations and scripts, enabled=False is blocked. For automations,\nuse ha_config_set_automation(identifier=\"automation.xxx\", enabled=False)\n(or enabled=True to re-enable).\nFor scripts, ha_call_service(\"script\", \"turn_off\", entity_id=\"script.xxx\")\nonly stops a currently running execution; it does not disable the script.\nHome Assistant has no script runtime enable/disable service.",
    "inputSchema": {
      "properties": {
        "entity_id": {
          "type": "str | list[str]",
          "description": "Entity ID or list of entity IDs to update."
        },
        "area_id": {
          "type": "str | None",
          "description": "Area/room ID to assign the entity to. Use empty string '' to unassign from current area.",
          "default": null
        },
        "name": {
          "type": "str | None",
          "description": "Display name for the entity. Use empty string '' to remove custom name and revert to default.",
          "default": null
        },
        "icon": {
          "type": "str | None",
          "description": "Icon for the entity (e.g., 'mdi:thermometer'). Use empty string '' to remove custom icon.",
          "default": null
        },
        "device_class": {
          "type": "str | None",
          "description": "Override the entity's display device class — what the HA UI's 'Show As' dropdown writes. Use empty string '' to clear the override and fall back to the integration default. Examples: 'window', 'door', 'motion' for binary_sensor; 'temperature', 'humidity' for sensor.",
          "default": null
        },
        "options": {
          "type": "dict[str, dict[str, Any]] | None",
          "description": "Per-domain entity registry options (e.g. sensor 'display_precision', weather 'forecast_type'). Pass a dict mapping domain to a sub-dict, e.g. {\"sensor\": {\"display_precision\": 2}}. Multiple domains are sent as separate registry updates. For 'Show As' use device_class; for voice-assistant exposure use expose_to, not options.<assistant>.should_expose.",
          "default": null
        },
        "enabled": {
          "type": "bool | None",
          "description": "True to enable the entity, False to disable it. WARNING: Setting enabled=False is a registry-level disable — it completely removes the entity from the state machine and hides it from the UI. A reload or restart is required to restore it after re-enabling.",
          "default": null
        },
        "hidden": {
          "type": "bool | None",
          "description": "True to hide the entity from UI, False to show it.",
          "default": null
        },
        "aliases": {
          "type": "str | list[str | None] | None",
          "description": "List of voice assistant aliases for the entity (replaces existing aliases). A null entry is the entity's own name (HA's 'use entity name' switch); it is kept automatically unless your list already contains null. To turn that switch off or on, use use_entity_name_alias.",
          "default": null
        },
        "use_entity_name_alias": {
          "type": "bool | None",
          "description": "HA's 'use entity name' voice-alias switch. True keeps the entity's own name answering in Assist, False turns it off so only the aliases match. Omit to leave it as is. Works with or without aliases.",
          "default": null
        },
        "categories": {
          "type": "dict[str, str | None] | None",
          "description": "Category assignment as a dict mapping scope to category_id. Example: {\"automation\": \"category_id_here\"}. Use null value to clear: {\"automation\": null}.",
          "default": null
        },
        "labels": {
          "type": "str | list[str] | None",
          "description": "List of label IDs for the entity. Behavior depends on label_operation parameter. Use [] with label_operation='set' to clear.",
          "default": null
        },
        "label_operation": {
          "type": "Literal['set', 'add', 'remove']",
          "description": "How to apply labels: 'set' replaces all labels, 'add' adds to existing, 'remove' removes specified labels.",
          "default": "set"
        },
        "expose_to": {
          "type": "dict[str, bool] | None",
          "description": "Control voice assistant exposure. Pass a dict mapping assistant IDs to booleans. Valid assistants: 'conversation' (Assist), 'cloud.alexa', 'cloud.google_assistant'. Example: {\"conversation\": true, \"cloud.alexa\": false}.",
          "default": null
        },
        "new_entity_id": {
          "type": "str | None",
          "description": "New entity ID to rename to (e.g., 'light.new_name'). Domain must match the original.",
          "default": null
        },
        "new_device_name": {
          "type": "str | None",
          "description": "New display name for the associated device. If provided, both entity and device are updated in one operation.",
          "default": null
        }
      },
      "required": [
        "entity_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": true
    },
    "tags": [
      "Entity Registry"
    ],
    "source_file": "tools_entities.py"
  },
  {
    "name": "ha_delete_file",
    "title": "Delete File",
    "description": "Delete a file from allowed directories in the Home Assistant config.\n\nPermanently removes a file; this cannot be undone. Allowed directories:\n`www/`, `themes/`, `custom_templates/`, `dashboards/`, plus any custom\ndirectories OR HAOS sibling volumes (`/share`, `/media`, `/ssl`,\n`/backup`) configured in the ha-mcp settings UI. Configuration files\ncannot be deleted; path traversal (../) is blocked.\n\nEXAMPLE: ha_delete_file(path=\"www/deprecated-style.css\", confirm=True)",
    "inputSchema": {
      "properties": {
        "path": {
          "type": "str",
          "description": "File path in an allowed delete directory; pass the absolute path for a HAOS sibling volume. Example: 'www/old-file.css'"
        },
        "confirm": {
          "type": "bool",
          "default": false,
          "description": "Must be True to confirm deletion."
        }
      },
      "required": [
        "path"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Files",
      "beta"
    ],
    "source_file": "tools_filesystem.py"
  },
  {
    "name": "ha_list_files",
    "title": "List Files",
    "description": "List files in a directory within the Home Assistant config directory.\n\nReturns file names, sizes, and modification times for one of the allowed\ndirectories:\n- `www/` - Web assets (CSS, JS, images for dashboards)\n- `themes/` - Theme files\n- `custom_templates/` - Jinja2 template files\n- `dashboards/` - YAML-mode dashboard files\n- `blueprints/` - Automation/script blueprint sources (read-only)\n- Your configured `packages/` folder, when `homeassistant: packages:` is\n  set (the folder name you bound, default `packages/`)\n- Plus any custom directories OR HAOS sibling volumes (`/share`,\n  `/media`, `/ssl`, `/backup`) configured in the ha-mcp settings UI\nPath traversal (../) is blocked.\n\nEXAMPLE: ha_list_files(path=\"www/\", pattern=\"*.css\")",
    "inputSchema": {
      "properties": {
        "path": {
          "type": "str",
          "description": "Directory path. Relative to the config dir for the built-in allowlist; absolute for a configured custom directory or HAOS sibling volume. Example: 'www/' or '/share/llm'"
        },
        "pattern": {
          "type": "str | None",
          "default": null,
          "description": "Optional glob pattern to filter files. Example: '*.css', '*.yaml', '*.js'"
        }
      },
      "required": [
        "path"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "tags": [
      "Files",
      "beta"
    ],
    "source_file": "tools_filesystem.py"
  },
  {
    "name": "ha_read_file",
    "title": "Read File",
    "description": "Read a file from the Home Assistant config directory.\n\nGeneral-purpose escape hatch — prefer a dedicated tool when one exists:\nha_manage_blueprints(action=\"get\") for a blueprint body,\nha_config_get_yaml for a config key, ha_config_get_automation/script/scene\nfor storage-mode items. Reach for ha_read_file only for raw on-disk text\nthose tools don't expose.\n\nSome files have special handling:\n- `secrets.yaml`: Values are masked for security\n- `home-assistant.log` / `home-assistant.log.fault`: Prefer\n  ha_get_logs(source='error_log') and\n  ha_get_logs(source='fault_log') over reading these directly.\n\n**Allowed Read Paths:**\n- `configuration.yaml`, `automations.yaml`, `scripts.yaml`, `scenes.yaml`\n- `secrets.yaml` (values masked)\n- `packages/*.yaml`\n- `home-assistant.log`, `home-assistant.log.fault` (tail only)\n- `www/**`, `themes/**`, `custom_templates/**`, `dashboards/**`, `blueprints/**`\n- `custom_components/**/*.py` (read-only)\n- Plus any custom directories OR HAOS sibling volumes (`/share`,\n  `/media`, `/ssl`, `/backup`) configured in the ha-mcp settings UI\nPath traversal (../) is blocked.\n\nEXAMPLE: ha_read_file(path=\"packages/alert2.yaml\", yaml_path=\"alert2\")",
    "inputSchema": {
      "properties": {
        "path": {
          "type": "str",
          "description": "File path. Relative to the config dir for the built-in allowlist; absolute for a configured HAOS sibling volume. Examples: 'configuration.yaml', 'www/custom.css', '/share/llm/notes.md'"
        },
        "tail_lines": {
          "type": "int | None",
          "default": null,
          "constraints": {
            "ge": 1,
            "le": 10000
          },
          "description": "For log files, return only the last N lines. Default: None (return full file, or last 1000 lines for logs)"
        },
        "yaml_path": {
          "type": "str | None",
          "default": null,
          "description": "Dotted YAML key path (e.g. 'alert2', 'mqtt.sensor'). When set, the response also carries 'subtree': the round-trip text of just that key's value (null when the key is absent); comments and HA tags ('!secret', '!include') survive as written and a '!secret' is never resolved."
        }
      },
      "required": [
        "path"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "tags": [
      "Files",
      "beta"
    ],
    "source_file": "tools_filesystem.py"
  },
  {
    "name": "ha_write_file",
    "title": "Write File",
    "description": "Write a file to allowed directories in the Home Assistant config.\n\nCreates or updates files (custom CSS/JS for dashboards, Jinja2 macro\ntemplates, theme YAML) in restricted directories only.\n\n**Allowed Write Directories:**\n- `www/` - Web assets for dashboards\n- `themes/` - Theme YAML files\n- `custom_templates/` - Jinja2 template files\n- `dashboards/` - YAML-mode dashboard files\n- Plus any custom directories OR HAOS sibling volumes (`/share`,\n  `/media`, `/ssl`, `/backup`) configured in the ha-mcp settings UI\nConfiguration files (configuration.yaml, etc.) cannot be written; path\ntraversal (../) is blocked.\n\nText content only. Overwriting a file that currently holds binary\ncontent still succeeds, but its prior bytes cannot be captured by\nauto-backup (only modifications/deletions of text files are\nsnapshotted); the skip is logged, the write is not blocked.\n\nEXAMPLE: ha_write_file(path=\"custom_templates/formatters.jinja\", content=\"{% macro shout(text) %}{{ text | upper }}{% endmacro %}\")",
    "inputSchema": {
      "properties": {
        "path": {
          "type": "str",
          "description": "File path in an allowed write directory; pass the absolute path for a HAOS sibling volume. Example: 'www/custom.css', '/share/llm/out.txt'"
        },
        "content": {
          "type": "str",
          "description": "The content to write to the file."
        },
        "overwrite": {
          "type": "bool",
          "default": false,
          "description": "Whether to overwrite if the file exists."
        },
        "create_dirs": {
          "type": "bool",
          "default": true,
          "description": "Whether to create parent directories if they don't exist."
        }
      },
      "required": [
        "path",
        "content"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Files",
      "beta"
    ],
    "source_file": "tools_filesystem.py"
  },
  {
    "name": "ha_config_list_groups",
    "title": "List Groups",
    "description": "List Home Assistant entity groups with their member entities.\n\nReturns one page of old-style groups created via the group.set service\nor YAML configuration (platform-specific groups such as light or cover\ngroups are separate entities); `total_count` and `has_more` report the\nfull set. Each group includes its entity_id, friendly name, state,\nmember entities, icon and all mode.\n\nEXAMPLE: ha_config_list_groups(offset=100)",
    "inputSchema": {
      "properties": {
        "limit": {
          "type": "int",
          "default": 100,
          "constraints": {
            "ge": 1,
            "le": 500
          },
          "description": "Max groups to return per page"
        },
        "offset": {
          "type": "int",
          "default": 0,
          "constraints": {
            "ge": 0
          },
          "description": "Number of groups to skip for pagination"
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Groups"
    ],
    "source_file": "tools_groups.py"
  },
  {
    "name": "ha_config_remove_group",
    "title": "Remove Group",
    "description": "Remove a service-based Home Assistant entity group via the group.remove service.\n\n**When NOT to use:** for groups created through `ha_config_set_helper(helper_type=\"group\", ...)`,\nuse `ha_remove_helpers_integrations`. Those config-entry-backed groups are not reachable via the\ngroup.remove service.\n\n**When to use:** removing groups created with `ha_config_set_group` or defined in YAML\nvia `group:` configuration. Config-entry-backed deletion tools cannot find these.\n\nEXAMPLES:\n- Remove group: ha_config_remove_group(\"living_room_lights\")\n\nUse ha_config_list_groups() to find existing groups.\n\n**WARNING:**\n- Removing a group used in automations may cause those automations to fail.\n- Groups defined in YAML can be removed at runtime but will reappear after restart.\n- This only removes old-style groups, not platform-specific groups.",
    "inputSchema": {
      "properties": {
        "object_id": {
          "type": "str",
          "description": "Group identifier without 'group.' prefix (e.g., 'living_room_lights')"
        },
        "wait": {
          "type": "bool",
          "description": "Wait for group to be fully removed before returning.",
          "default": true
        }
      },
      "required": [
        "object_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": true
    },
    "tags": [
      "Groups"
    ],
    "source_file": "tools_groups.py"
  },
  {
    "name": "ha_config_set_group",
    "title": "Create or Update Group",
    "description": "Create or update a service-based Home Assistant entity group via the group.set service.\n\n**When NOT to use:** for typical \"combine these entities into one controllable group\"\nrequests, prefer `ha_config_set_helper(helper_type=\"group\", ...)`. Config-entry-backed\ngroups are registered in the entity registry, so `ha_set_entity` can assign them to\nareas and they are deletable via `ha_remove_helpers_integrations`.\n\n**When to use:** compatibility with existing groups already configured via group.set\nor YAML, or the rare case where entity-registry membership is explicitly unwanted.\nGroups created here are only removable via `ha_config_remove_group` —\n`ha_remove_helpers_integrations` will not find them.\n\n**For NEW groups:** Provide object_id and entities (required).\n**For EXISTING groups:** Provide object_id and any fields to update.\nentities, add_entities, and remove_entities are mutually exclusive.\n\nEXAMPLES:\n- Create: ha_config_set_group(\"bedroom_lights\", entities=[\"light.lamp\", \"light.ceiling\"], name=\"Bedroom Lights\")\n- Add entities: ha_config_set_group(\"lights\", add_entities=[\"light.extra\"])",
    "inputSchema": {
      "properties": {
        "object_id": {
          "type": "str",
          "description": "Group identifier without 'group.' prefix (e.g., 'living_room_lights')"
        },
        "entities": {
          "type": "list[str] | None",
          "description": "List of entity IDs for the group. When updating, replaces all entities.",
          "default": null
        },
        "name": {
          "type": "str | None",
          "description": "Friendly display name for the group",
          "default": null
        },
        "icon": {
          "type": "str | None",
          "description": "Material Design Icon (e.g., 'mdi:lightbulb-group')",
          "default": null
        },
        "all_on": {
          "type": "bool | None",
          "description": "If True, all entities must be on for group to be on",
          "default": null
        },
        "add_entities": {
          "type": "list[str] | None",
          "description": "Add these entities to an existing group",
          "default": null
        },
        "remove_entities": {
          "type": "list[str] | None",
          "description": "Remove these entities from an existing group",
          "default": null
        },
        "wait": {
          "type": "bool",
          "description": "Wait for group to be queryable before returning. Set to False for bulk operations.",
          "default": true
        }
      },
      "required": [
        "object_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Groups"
    ],
    "source_file": "tools_groups.py"
  },
  {
    "name": "ha_get_hacs_info",
    "title": "Get HACS Info",
    "description": "Get HACS (Home Assistant Community Store) data — search the store or fetch repository details.\n\nUse ``action=\"search\"`` to search/browse/list store repositories, or\n``action=\"info\"`` for one repository's full details (README, versions, GitHub\nstats). This tool is read-only; to install or add repositories use\n``ha_manage_hacs``, and for non-HACS entities/config use the domain-specific tools.\n\n**DASHBOARD TIP:** ``action=\"search\", installed_only=True, category=\"lovelace\"``\ndiscovers installed custom cards to wire into ``ha_config_set_dashboard()``.\n\n**Examples:**\n- Search the store: ha_get_hacs_info(action=\"search\", query=\"mushroom\", category=\"lovelace\")\n- List installed: ha_get_hacs_info(action=\"search\", installed_only=True)\n- Repository details: ha_get_hacs_info(action=\"info\", repository_id=\"441028036\")\n\n**Caveats:** ``info`` fetches full repository detail from GitHub, so it can hit GitHub\nrate limits / needs HACS's configured GitHub token; ``search`` reads HACS's locally\ncached repository index.",
    "inputSchema": {
      "properties": {
        "action": {
          "type": "Literal['search', 'info']",
          "description": "'search' the store, or 'info' for one repository"
        },
        "query": {
          "type": "str",
          "description": "Search keyword (action='search')",
          "default": ""
        },
        "category": {
          "type": "Literal['integration', 'lovelace', 'theme', 'appdaemon', 'python_script'] | None",
          "description": "Filter by category (action='search')",
          "default": null
        },
        "installed_only": {
          "type": "bool",
          "description": "Only return installed repositories (action='search')",
          "default": false
        },
        "max_results": {
          "type": "int",
          "constraints": {
            "ge": 1,
            "le": 100
          },
          "description": "Maximum number of results (action='search')",
          "default": 10
        },
        "offset": {
          "type": "int",
          "constraints": {
            "ge": 0
          },
          "description": "Results to skip for pagination (action='search')",
          "default": 0
        },
        "repository_id": {
          "type": "str | None",
          "description": "Numeric HACS ID or 'owner/repo' path (action='info')",
          "default": null
        }
      },
      "required": [
        "action"
      ]
    },
    "annotations": {
      "openWorldHint": true,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "HACS"
    ],
    "source_file": "tools_hacs.py"
  },
  {
    "name": "ha_manage_hacs",
    "title": "Manage HACS",
    "description": "Manage HACS (Home Assistant Community Store) — install/update, remove, add custom repositories, or refresh repository information.\n\nThis tool performs writes; to search the store or read repository details use\n``ha_get_hacs_info``. Use ``action=\"update_information\"`` to run the HACS UI's\n\"Update information\" action — a forced re-fetch of one repository's release data\nfrom GitHub, so a pending update becomes visible to HACS and its update entity\nimmediately.\n\n**Examples:**\n- Install latest: ha_manage_hacs(action=\"download\", repository_id=\"441028036\")\n- Install a version: ha_manage_hacs(action=\"download\", repository_id=\"piitaya/lovelace-mushroom\", version=\"v4.0.0\")\n- Remove: ha_manage_hacs(action=\"remove\", repository_id=\"owner/repo\")\n- Add a custom repo: ha_manage_hacs(action=\"add_repository\", repository=\"owner/repo\", category=\"lovelace\")\n- Refresh release data: ha_manage_hacs(action=\"update_information\", repository_id=\"owner/repo\")\n\n**Caveats:** Installing an integration usually needs a Home Assistant restart to\nactivate; new Lovelace cards need a browser cache clear. ``add_repository`` requires ``owner/repo``\nformat plus a matching ``category``. Removing an integration deletes its files but\nthe loaded module persists until the next Home Assistant restart — delete its config\nentries first (``ha_remove_helpers_integrations``). HACS refreshes custom\nrepositories on its own only about every 48 hours, so ``update_information`` is the\nway to surface a just-published release.",
    "inputSchema": {
      "properties": {
        "action": {
          "type": "Literal['download', 'add_repository', 'remove', 'update_information']",
          "description": "'download' to install/update, 'add_repository' to register a custom repo, 'remove' to uninstall a downloaded repo, or 'update_information' to refresh a repository's release data from GitHub"
        },
        "repository_id": {
          "type": "str | None",
          "description": "Numeric HACS ID or 'owner/repo' path (action='download' / 'remove' / 'update_information')",
          "default": null
        },
        "version": {
          "type": "str | None",
          "description": "Specific version to install (action='download')",
          "default": null
        },
        "repository": {
          "type": "str | None",
          "description": "GitHub repo 'owner/repo' to add (action='add_repository')",
          "default": null
        },
        "category": {
          "type": "Literal['integration', 'lovelace', 'theme', 'appdaemon', 'python_script'] | None",
          "description": "Repository category (action='add_repository')",
          "default": null
        }
      },
      "required": [
        "action"
      ]
    },
    "annotations": {
      "openWorldHint": true,
      "destructiveHint": true
    },
    "tags": [
      "HACS"
    ],
    "source_file": "tools_hacs.py"
  },
  {
    "name": "ha_config_list_helpers",
    "title": "List Helpers",
    "description": "List Home Assistant helpers of a specific type with their configurations.\n\nReturns one page of helpers; `total_count` and `has_more` report the full\nset. Each record carries the complete configuration for its helper:\nid (immutable storage key), entity_id (current — address the helper by\nthis, where available), name (current display name), original_name\n(creation-time name), icon, type-specific settings, and area and label\nassignments.\n\nFor a helper renamed in the UI, id/original_name keep the storage values while\nentity_id/name reflect the current entity registry (entity_id is the identifier\nha_config_set_helper resolves against, so prefer it over id for a renamed helper).\nentity_id/original_name are present only for storage-collection helpers matched in\nthe entity registry — types with no backing entity (e.g. tag), and every record when\nthe registry read degrades, carry only id/name (a warning flags the degraded case).\n\nStorage types list what HA's ``{type}/list`` command returns: the\nstorage-backed helpers (created via UI/API), not the YAML-defined ones.\n``person`` is the exception — HA lists its YAML-configured persons\nalongside the storage ones, so both appear here.\n\nFlow-based types (template / group / utility_meter / derivative / etc.)\nrequire the ha_mcp_tools custom component (>= 1.1.0) and are served only\nthrough it. Requesting a flow type without the component returns a\nCOMPONENT_NOT_INSTALLED error.\n\nWith helper_type=\"all\", each record carries its own ``helper_type``.\nThis mode is component-only (there is no single built-in command that\nlists all types): without the ha_mcp_tools component it returns a\nCOMPONENT_NOT_INSTALLED error rather than a partial or empty list.\n\nEXAMPLES:\n- List all counters: ha_config_list_helpers(\"counter\")\n- List every helper type at once: ha_config_list_helpers(\"all\")\n- Next page: ha_config_list_helpers(\"input_boolean\", offset=100)\n\nFor detailed helper documentation, use ha_get_skill_guide.",
    "inputSchema": {
      "properties": {
        "helper_type": {
          "type": "Literal['input_button', 'input_boolean', 'input_select', 'input_number', 'input_text', 'input_datetime', 'counter', 'timer', 'schedule', 'zone', 'person', 'tag', 'all'] | SUPPORTED_HELPERS",
          "description": "Helper type to list. Storage types are listed on all installs; flow-based types require the ha_mcp_tools custom component. Pass 'all' to list every helper type in one call (also requires the ha_mcp_tools component)."
        },
        "limit": {
          "type": "int",
          "default": 100,
          "constraints": {
            "ge": 1,
            "le": 500
          },
          "description": "Max helpers to return per page"
        },
        "offset": {
          "type": "int",
          "default": 0,
          "constraints": {
            "ge": 0
          },
          "description": "Number of helpers to skip for pagination"
        }
      },
      "required": [
        "helper_type"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Helper Entities"
    ],
    "source_file": "tools_config_helpers.py"
  },
  {
    "name": "ha_config_set_helper",
    "title": "Create or Update Helper",
    "description": "Create or update Home Assistant helper entities and config subentries\n(30 types, unified interface).\n\nMUST call ha_get_skill_guide OR refer to your locally installed skills first.\n``helper-selection.md`` ships under ``skill_content`` by default.\n\nSIMPLE types (structured params, WebSocket API): input_boolean, input_button,\ninput_select, input_number, input_text, input_datetime, counter, timer, schedule,\nzone, person, tag. Create requires `name`; update requires `helper_id`.\n\nFLOW types (pass `config` dict, Config Entry Flow API): template, group,\nutility_meter, derivative, min_max, threshold, integration, statistics, trend,\nrandom, filter, tod, generic_thermostat, switch_as_x, generic_hygrostat,\nhistory_stats, mold_indicator. Create requires `name`; for updates pass the\nexisting entry_id as `helper_id` (options flows reject the `name` key).\n`otp` is a helper in the HA UI but not offered here — its flow needs a live\nTOTP code; create it with ha_set_integration(domain=\"otp\").\n\nCONFIG_SUBENTRY type (Config Subentry Flow API): pass `entry_id`,\n`subentry_type` and `config`; pass `subentry_id` to reconfigure an existing\nsubentry, omit it to create one.\n\nBehavior:\n- UPDATE preserves type-specific fields not re-passed (a rename never wipes\n  initial/icon/etc.); flow-helper and config subentry updates behave the same\n  way (see `config`).\n- Omitted `action` falls back to the `helper_id`-presence discriminator\n  (SIMPLE/FLOW) or the `subentry_id`-presence discriminator (config\n  subentries).\n- For flow-based helpers, config keys not declared by any step's data_schema\n  are silently ignored by HA. Validation errors carry the helper's\n  `data_schema` (and `menu_options` for menu-rooted helpers like\n  `template`/`group` when no sub-type is chosen yet), so a follow-up call\n  can self-correct without a separate schema-discovery round-trip.\n- Flows that present more than one menu (e.g. an MQTT device subentry\n  reconfigure looping through its summary menu) take `next_step_id` as a\n  LIST of successive selections, consumed one per menu encounter.\n\nEXAMPLES (menu-based types, where the first-call payload is non-obvious):\n- template sensor: ha_config_set_helper(helper_type=\"template\", name=\"Room Temp\", config={\"next_step_id\": \"sensor\", \"state\": \"{{ states('sensor.x')|float }}\", \"unit_of_measurement\": \"°C\"})\n- group: ha_config_set_helper(helper_type=\"group\", name=\"Kitchen Lights\", config={\"group_type\": \"light\", \"entities\": [\"light.a\", \"light.b\"]})\n- config subentry: ha_config_set_helper(helper_type=\"config_subentry\", entry_id=\"01HXYZ...\", subentry_type=\"conversation\", config={\"name\": \"Local agent\", \"model\": \"gemma3:27b\"})",
    "inputSchema": {
      "properties": {
        "helper_type": {
          "type": "Literal['counter', 'config_subentry', 'derivative', 'filter', 'generic_hygrostat', 'generic_thermostat', 'group', 'history_stats', 'input_boolean', 'input_button', 'input_datetime', 'input_number', 'input_select', 'input_text', 'integration', 'min_max', 'mold_indicator', 'person', 'random', 'schedule', 'statistics', 'switch_as_x', 'tag', 'template', 'threshold', 'timer', 'tod', 'trend', 'utility_meter', 'zone']",
          "description": "Type of helper entity to create or update"
        },
        "name": {
          "type": "str | None",
          "description": "Display name for simple/flow helper creation. Optional on helper update. Ignored for helper_type='config_subentry', which uses entry_id/subentry_type/subentry_id instead. For flow-based helper updates (template, group, utility_meter, ...), this is typically ignored because options flows don't expose renaming. Rename a flow helper by deleting and recreating instead.",
          "default": null
        },
        "helper_id": {
          "type": "str | None",
          "description": "Bare ID ('my_button') or full entity ID ('input_button.my_button'). Omit to create a new helper.",
          "default": null
        },
        "entry_id": {
          "type": "str | None",
          "description": "Parent config entry ID when helper_type='config_subentry'. Use ha_get_integration() to find entry IDs.",
          "default": null
        },
        "subentry_type": {
          "type": "str | None",
          "description": "Integration-defined subentry type when helper_type='config_subentry'.",
          "default": null
        },
        "subentry_id": {
          "type": "str | None",
          "description": "Existing config subentry ID to reconfigure when helper_type='config_subentry'.",
          "default": null
        },
        "show_advanced_options": {
          "type": "bool",
          "description": "When helper_type='config_subentry', ask older Home Assistant versions to expose advanced flow options. No-op on HA 2026.6+; pending removal before HA 2027.6.",
          "default": false
        },
        "icon": {
          "type": "str | None",
          "description": "Material Design Icon (e.g., 'mdi:bell', 'mdi:toggle-switch')",
          "default": null
        },
        "area_id": {
          "type": "str | None",
          "description": "Area/room ID to assign the helper to",
          "default": null
        },
        "labels": {
          "type": "str | list[str] | None",
          "description": "Labels to categorize the helper",
          "default": null
        },
        "min_value": {
          "type": "float | None",
          "description": "Minimum value (input_number/counter) or minimum length (input_text). Also accepts shorthand 'min'.",
          "default": null
        },
        "max_value": {
          "type": "float | None",
          "description": "Maximum value (input_number/counter) or maximum length (input_text). Also accepts shorthand 'max'.",
          "default": null
        },
        "step": {
          "type": "float | None",
          "description": "Step/increment value for input_number or counter",
          "default": null
        },
        "unit_of_measurement": {
          "type": "str | None",
          "description": "Unit of measurement for input_number (e.g., '°C', '%', 'W'). Also accepts shorthand 'unit'.",
          "default": null
        },
        "options": {
          "type": "str | list[str] | None",
          "description": "List of options for input_select (required for input_select)",
          "default": null
        },
        "initial": {
          "type": "str | int | None",
          "description": "Initial value for applicable helper types. For input_boolean, input_select, input_number, input_text, and input_datetime: setting `initial` — even to false/0 — disables last-state restore and forces that value on every HA restart; omit unless you want the helper to reset to that value on every restart instead of restoring its last state. For counter, `initial` is just the starting value — restore-on-restart is controlled separately by `restore` (default True).",
          "default": null
        },
        "mode": {
          "type": "str | None",
          "description": "Display mode: 'box'/'slider' for input_number, 'text'/'password' for input_text",
          "default": null
        },
        "has_date": {
          "type": "bool | None",
          "description": "Include date component for input_datetime",
          "default": null
        },
        "has_time": {
          "type": "bool | None",
          "description": "Include time component for input_datetime",
          "default": null
        },
        "restore": {
          "type": "bool | None",
          "description": "Restore state after restart (counter, timer). Defaults to True for counter, False for timer",
          "default": null
        },
        "duration": {
          "type": "str | None",
          "description": "Default duration for timer in format 'HH:MM:SS' or seconds (e.g., '0:05:00' for 5 minutes)",
          "default": null
        },
        "monday": {
          "type": "list[dict[str, Any]] | None",
          "description": "Schedule time ranges for Monday. List of {'from': 'HH:MM', 'to': 'HH:MM'} dicts. Optional 'data' dict for additional attributes (e.g. {'from': '07:00', 'to': '22:00', 'data': {'mode': 'comfort'}})",
          "default": null
        },
        "tuesday": {
          "type": "list[dict[str, Any]] | None",
          "description": "Schedule time ranges for Tuesday; same shape as monday.",
          "default": null
        },
        "wednesday": {
          "type": "list[dict[str, Any]] | None",
          "description": "Schedule time ranges for Wednesday; same shape as monday.",
          "default": null
        },
        "thursday": {
          "type": "list[dict[str, Any]] | None",
          "description": "Schedule time ranges for Thursday; same shape as monday.",
          "default": null
        },
        "friday": {
          "type": "list[dict[str, Any]] | None",
          "description": "Schedule time ranges for Friday; same shape as monday.",
          "default": null
        },
        "saturday": {
          "type": "list[dict[str, Any]] | None",
          "description": "Schedule time ranges for Saturday; same shape as monday.",
          "default": null
        },
        "sunday": {
          "type": "list[dict[str, Any]] | None",
          "description": "Schedule time ranges for Sunday; same shape as monday.",
          "default": null
        },
        "latitude": {
          "type": "float | None",
          "description": "Latitude for zone (required for zone)",
          "default": null
        },
        "longitude": {
          "type": "float | None",
          "description": "Longitude for zone (required for zone)",
          "default": null
        },
        "radius": {
          "type": "float | None",
          "description": "Radius in meters for zone (default: 100)",
          "default": null
        },
        "passive": {
          "type": "bool | None",
          "description": "Passive zone (won't trigger state changes for person entities)",
          "default": null
        },
        "user_id": {
          "type": "str | None",
          "description": "User ID to link to person entity",
          "default": null
        },
        "device_trackers": {
          "type": "list[str] | None",
          "description": "List of device_tracker entity IDs for person",
          "default": null
        },
        "picture": {
          "type": "str | None",
          "description": "Picture URL for person entity",
          "default": null
        },
        "tag_id": {
          "type": "str | None",
          "description": "Tag ID. On create, omit to auto-generate a uuid4 hex. On update, the existing tag_id is required (passed via helper_id).",
          "default": null
        },
        "description": {
          "type": "str | None",
          "description": "Description for tag",
          "default": null
        },
        "category": {
          "type": "str | None",
          "description": "Category ID to assign to this helper. Use ha_config_get_category(scope='helpers') to list available categories, or ha_config_set_category() to create one.",
          "default": null
        },
        "config": {
          "type": "dict[str, Any] | None",
          "description": "Config dict for flow-based helper types and helper_type='config_subentry'. Ignored for simple helper types. On update it is a patch: a field you omit keeps its current value, and a field set to null is cleared where the schema allows that field to be empty. A field two steps declare gets your one value both times; pass step_values={'<step_id>': {'<field>': <value>}} to give a step its own value, or to leave it out of that step; a LIST of those objects supplies one per encounter when the flow presents a step more than once.",
          "default": null
        },
        "wait": {
          "type": "bool",
          "description": "Wait for helper entity to be queryable before returning. Set to False for bulk operations.",
          "default": true
        },
        "action": {
          "type": "Literal['create', 'update'] | None",
          "description": "Explicit intent: 'create' a new helper or 'update' an existing one. Pass it so a helper_id typo fails as 'helper not found' instead of creating a helper.",
          "default": null
        },
        "MandatoryBPS": {
          "type": "bool",
          "default": true
        },
        "BestPracticeKey": {
          "type": "BestPracticeKeyParam",
          "default": null
        }
      },
      "required": [
        "helper_type"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Helper Entities"
    ],
    "source_file": "tools_config_helpers.py"
  },
  {
    "name": "ha_remove_helpers_integrations",
    "title": "Remove Helper or Integration",
    "description": "Remove a Home Assistant helper or integration config entry.\n\nUnifies three backend removal mechanisms — simple-helper websocket\ndelete, config-entry delete, and config-subentry delete — behind one\nentry point with four routing paths driven by helper_type.\n\nWHEN NOT TO USE:\n- Removing only an entity (without deleting its underlying helper or\n  config entry) — use `ha_remove_entity` instead.\n- YAML-configured helpers — they have no storage backend. Edit the\n  YAML file and reload the relevant integration.\n\nROUTING:\n- SIMPLE helper_type (input_button, input_boolean, input_select,\n  input_number, input_text, input_datetime, counter, timer, schedule,\n  zone, person, tag) + bare helper_id or entity_id → websocket delete.\n- FLOW helper_type (template, group, utility_meter, derivative, min_max,\n  threshold, integration, statistics, trend, random, filter, tod,\n  generic_thermostat, switch_as_x, generic_hygrostat, history_stats,\n  mold_indicator) + full entity_id → resolve entity_id to\n  config_entry_id via entity_registry, then delete the config entry. All\n  sub-entities (e.g. utility_meter tariffs) are removed together.\n- helper_type=None + entry_id → direct config entry delete (any\n  integration).\n- helper_type=\"config_subentry\" + parent entry_id + subentry_id →\n  delete one config subentry.\n\nA target that is confirmed absent raises a structured error rather than\nreturning silent success: ENTITY_NOT_FOUND for a SIMPLE target missing\nfrom both the state machine and the entity registry, or a FLOW\nentity_id missing from the registry (a bare helper_id on a FLOW target\nalso raises it — FLOW resolution needs a full entity_id);\nRESOURCE_NOT_FOUND for a YAML-configured helper with no config entry, a\nconfig entry the backend reports as 404, or a missing config subentry.\nCalling N times gives the same response. Transient connectivity failures\nraise their own codes (WEBSOCKET_DISCONNECTED, CONNECTION_FAILED) so\nretry logic can branch separately.\n\nEXAMPLES:\n- Remove SIMPLE button: ha_remove_helpers_integrations(target=\"my_button\", helper_type=\"input_button\", confirm=True)\n- Remove FLOW utility_meter (any sub-entity works): ha_remove_helpers_integrations(target=\"sensor.energy_peak\", helper_type=\"utility_meter\", confirm=True)\n- Remove any integration by entry_id: ha_remove_helpers_integrations(target=\"01HXYZ...\", confirm=True)\n- Remove a config subentry: ha_remove_helpers_integrations(target=\"01HXYZ...\", helper_type=\"config_subentry\", subentry_id=\"subentry-123\", confirm=True)\n\nWARNING: Removing a helper or integration that is referenced by\nautomations, scripts, or other integrations may cause those to fail.\nUse ha_search() / ha_get_integration() to verify before removal.\nRecovery requires a usable backup and supported restore path.",
    "inputSchema": {
      "properties": {
        "target": {
          "type": "str",
          "description": "What to remove. One of: (a) bare helper_id for SIMPLE helpers, e.g. 'my_button'; (b) full entity_id, e.g. 'input_button.my_button' or 'sensor.my_meter'; (c) config entry_id for any integration, e.g. value from ha_get_integration(); (d) parent config entry_id for a config subentry."
        },
        "helper_type": {
          "type": "HelperTypeLiteral | None",
          "description": "Helper type. Required when target is a helper_id (bare) or entity_id. Set to None when target is a config entry_id to remove any integration. Use 'config_subentry' to remove a config subentry under target.",
          "default": null
        },
        "subentry_id": {
          "type": "str | None",
          "description": "Config subentry ID to remove when helper_type='config_subentry'.",
          "default": null
        },
        "confirm": {
          "type": "bool",
          "description": "Must be True to confirm removal.",
          "default": false
        },
        "wait": {
          "type": "bool",
          "description": "Wait for entity removal. Default: True. Ignored when helper_type=None or helper_type='config_subentry' (no entity poll, require_restart returned).",
          "default": true
        }
      },
      "required": [
        "target"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": true
    },
    "tags": [
      "Helper Entities",
      "Integrations"
    ],
    "source_file": "tools_integrations.py"
  },
  {
    "name": "ha_get_automation_traces",
    "title": "Get Automation Traces",
    "description": "Get execution traces for automations and scripts to debug issues.\n\nTraces show what triggered a run, which conditions passed or failed,\nwhich actions executed (for 'choose', which branch was taken), any\nerrors, and variable values during execution. Traces are stored for a\nlimited time by Home Assistant. The 'state' field shows 'stopped'\n(completed), 'running', or an error state.\n\nUSAGE MODES:\n1. List recent traces (omit run_id): returns a summary of recent runs\n   with timestamps, triggers, and status. Use `offset` to page deeper\n   when `has_more` is true.\n   ha_get_automation_traces(\"automation.motion_light\")\n2. Get a detailed trace (provide run_id): full execution details\n   including trigger info, condition results, action trace with timing,\n   and context variables.\n   ha_get_automation_traces(\"automation.motion_light\", run_id=\"1705312800.123456\")\n3. Add logbook entries and context metadata: detailed=True. Script-style\n   action paths (sequence/, numeric) are always matched regardless of\n   this flag.\n4. Variables at every step (trigger sets and null entries are still\n   omitted): deduplicate=False.",
    "inputSchema": {
      "properties": {
        "automation_id": {
          "type": "str",
          "description": "Automation or script entity_id (e.g., 'automation.motion_light' or 'script.morning_routine')"
        },
        "run_id": {
          "type": "str | None",
          "description": "Specific trace run_id to retrieve detailed trace.",
          "default": null
        },
        "limit": {
          "type": "int",
          "description": "Maximum number of traces to return when listing (default: 10, max: 50).",
          "default": 10,
          "constraints": {
            "ge": 1,
            "le": 50
          }
        },
        "deduplicate": {
          "type": "bool",
          "description": "Deduplicate variables across action steps. Set to False to record the variables at every step; steps whose variables carry 'trigger', and null-valued entries, are omitted either way.",
          "default": true
        },
        "detailed": {
          "type": "bool",
          "description": "Include extra diagnostic data: logbook entries and context metadata.",
          "default": false
        },
        "sections": {
          "type": "str | None",
          "description": "Comma-separated list of trace sections to return. Valid values: trigger, conditions, actions, config, error, logbook, context. Omit to return all sections. Example: 'actions' or 'trigger,conditions'.",
          "default": null
        },
        "offset": {
          "type": "int",
          "description": "Number of traces to skip from the start of the requested order. Use with `limit` to page through stored traces when `total_available > limit`.",
          "default": 0,
          "constraints": {
            "ge": 0
          }
        },
        "order": {
          "type": "Literal['newest', 'oldest']",
          "description": "Order traces are returned in. 'newest' returns most-recent first; 'oldest' returns chronological-first.",
          "default": "newest"
        }
      },
      "required": [
        "automation_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "History & Statistics"
    ],
    "source_file": "tools_traces.py"
  },
  {
    "name": "ha_get_history",
    "title": "Get Entity History or Statistics",
    "description": "Get historical data from Home Assistant's recorder.\n\nUse source=\"history\" (default) to troubleshoot why a value changed, check\nevent sequences, or analyze recent patterns. Use source=\"statistics\" for\nlong-term trends beyond the ~10-day recorder retention and period\naverages; entities must have state_class (measurement, total,\ntotal_increasing).\n\nHistory-only params: minimal_response, significant_changes_only.\nStatistics-only params: period, statistic_types.\n\nAll data is fetched from HA before slicing; limit/offset are client-side.\nWith multiple entity_ids, offset must be 0 — use a single entity_id for\noffset > 0. Use has_more and next_offset from the response to paginate.\nAdministrators can optionally enable recorder workload guardrails in\nAdvanced settings. When enabled, oversized entity/time workloads are\nrejected before the recorder query is issued; narrow the time range or\nentity list to stay within the budget. Calendar statistics may first\nread HA's configured timezone so the estimate follows local calendar\nboundaries.\n\nEXAMPLES:\n- ha_get_history(entity_ids=\"sensor.bedroom_temperature\", start_time=\"24h\")\n- Paginate oldest-first: ha_get_history(entity_ids=\"sensor.temperature\", start_time=\"7d\", limit=100, offset=100, order=\"asc\")\n- ha_get_history(source=\"statistics\", entity_ids=\"sensor.living_room_temperature\", start_time=\"6m\", period=\"month\", statistic_types=[\"mean\", \"min\", \"max\"])",
    "inputSchema": {
      "properties": {
        "entity_ids": {
          "type": "str | list[str]",
          "description": "Entity ID(s) to query. Can be a single ID, comma-separated string, or JSON array."
        },
        "source": {
          "type": "Literal['history', 'statistics']",
          "description": "Data source: \"history\" for raw state changes at full resolution (~10 day retention), or \"statistics\" for pre-aggregated long-term data (permanent, requires state_class).",
          "default": "history"
        },
        "start_time": {
          "type": "str | None",
          "description": "Start time: ISO datetime or relative (e.g., '24h', '7d', '30d'). Default: 24h ago for history, 30d ago for statistics",
          "default": null
        },
        "end_time": {
          "type": "str | None",
          "description": "End time: ISO datetime. Default: now",
          "default": null
        },
        "minimal_response": {
          "type": "bool",
          "description": "Return only states/timestamps without attributes. Ignored when source=\"statistics\"",
          "default": true
        },
        "significant_changes_only": {
          "type": "bool",
          "description": "Filter to significant state changes only. Ignored when source=\"statistics\"",
          "default": true
        },
        "limit": {
          "type": "int | None",
          "description": "Max entries per entity. Default: 100. For source=\"history\": state changes. For source=\"statistics\": aggregated rows. With multiple entity_ids, total rows returned can reach limit × len(entity_ids).",
          "default": null,
          "constraints": {
            "ge": 1,
            "le": 1000
          }
        },
        "offset": {
          "type": "int | None",
          "description": "Number of entries to skip per entity for pagination.",
          "default": null,
          "constraints": {
            "ge": 0
          }
        },
        "period": {
          "type": "str",
          "description": "Aggregation period: \"5minute\", \"hour\", \"day\", \"week\", \"month\", \"year\". Ignored when source=\"history\"",
          "default": "day"
        },
        "statistic_types": {
          "type": "str | list[str] | None",
          "description": "Statistics types: \"mean\", \"min\", \"max\", \"sum\", \"state\", \"change\". Default: all. Ignored when source=\"history\"",
          "default": null
        },
        "order": {
          "type": "Literal['asc', 'desc']",
          "default": "desc",
          "description": "Sort order for history entries. \"desc\": newest first. \"asc\": oldest first. Ignored when source=\"statistics\"."
        },
        "fields": {
          "type": "str | list[str] | None",
          "default": null,
          "description": "Return only the specified top-level response keys to reduce response size. None = full response. History keys: success, source, entities, period, query_params. Statistics keys: success, source, entities, period_type, time_range, statistic_types, query_params, warnings."
        }
      },
      "required": [
        "entity_ids"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "History & Statistics"
    ],
    "source_file": "tools_history.py"
  },
  {
    "name": "ha_get_logs",
    "title": "Get Logs",
    "description": "Get Home Assistant logs from various sources.\n\nPrefer source='system' for triage: it returns HA's own deduplicated\nsystem_log entries with counts, first_occurred and full tracebacks, and\nits counts run since each error first occurred. error_log with\nstructured=True counts only what is inside the fetched window (reported\nas window_start/window_end; every install reads a capped window) and\ndrops tracebacks, which structured=False gets back; use it for entries\nbelow system_log's WARNING+ ~50-entry cap, or for the per-component\nrollup. In structured mode limit/order do not apply: issues are ranked\nby count, then severity, then recency, over a fixed deep window.\n\nRaw-text sources (error_log, supervisor, system_service) read a bounded\nwindow per call, so level/search filter and limit slice within that\nwindow only; window_lines reports the size requested. Logbook responses\ncarry has_more plus a pagination_hint; error_log and fault_log carry\nhas_more with a next_offset to pass back while it stays true.",
    "inputSchema": {
      "properties": {
        "source": {
          "type": "Literal['logbook', 'system', 'error_log', 'supervisor', 'system_service', 'logger', 'fault_log']",
          "description": "'logbook': entity state-change history. 'system': HA's structured system_log entries (errors, warnings). 'error_log': raw log text (home-assistant.log on container/pip installs; HA Core's journald stream on Supervisor-backed installs). 'supervisor': app (add-on) container logs (needs slug). 'system_service': Supervisor-managed system service logs (needs slug). 'logger': effective log level per integration (confirms logger.set_level changes took effect). 'fault_log': HA Core's faulthandler crash dump (home-assistant.log.fault), written only when HA dies from a native fatal signal, which never reaches journald or error_log; empty on a healthy install (crash_recorded=False); whole crash blocks are ordered with each block's lines kept in place; reads through the 'HA-MCP File & YAML Tools' entry.",
          "default": "logbook"
        },
        "limit": {
          "type": "int | None",
          "description": "Max entries/lines to return. Does not apply to source='error_log' with structured=True.",
          "default": null
        },
        "search": {
          "type": "str | None",
          "description": "Keyword filter on entries/lines; matches the integration domain for source='logger'. In structured error_log mode it matches the message and logger name only; on the raw path the whole line.",
          "default": null
        },
        "order": {
          "type": "Literal['newest', 'oldest']",
          "description": "Sort order for time-ordered sources (logbook, system, error_log, supervisor, system_service, fault_log): 'newest' returns most-recent first; 'oldest' returns chronological-first. For raw-text sources it sets the read direction of the most-recent window; fault_log orders whole crash blocks. Ignored for source='logger', and for source='error_log' with structured=True.",
          "default": "newest"
        },
        "hours_back": {
          "type": "int",
          "constraints": {
            "ge": 1
          },
          "description": "Logbook only: how many hours back to read.",
          "default": 1
        },
        "entity_id": {
          "type": "str | None",
          "description": "Logbook only: restrict to this entity.",
          "default": null
        },
        "end_time": {
          "type": "str | None",
          "description": "Logbook only: end of the window (ISO datetime).",
          "default": null
        },
        "offset": {
          "type": "int",
          "constraints": {
            "ge": 0
          },
          "description": "Page deeper into source='logbook', 'error_log' and 'fault_log' (ignored for other sources). On error_log it counts raw log lines back from the newest entry; on fault_log it counts lines from the start of the assembled crash text. Pass the response's 'next_offset' to continue while 'has_more' is true.",
          "default": 0
        },
        "compact": {
          "type": "bool",
          "description": "Logbook only: strip attribute dicts to save context.",
          "default": true
        },
        "level": {
          "type": "str | None",
          "description": "system / error_log only: keep only entries at exactly this level (ERROR, WARNING, INFO, DEBUG, CRITICAL); it is not a threshold.",
          "default": null
        },
        "structured": {
          "type": "bool",
          "description": "source='error_log' only. When True, return a deduplicated, component-grouped summary of the log (counted issues sorted by frequency) instead of raw text. Ignored for other sources.",
          "default": false
        },
        "top_n": {
          "type": "int | None",
          "constraints": {
            "ge": 1
          },
          "description": "Max distinct issues to return when structured=True (default 20, capped at 500).",
          "default": null
        },
        "slug": {
          "type": "str | None",
          "description": "source='supervisor': app slug, e.g. 'core_mosquitto' (use ha_get_app() to list installed slugs). source='system_service': service name, one of supervisor, host, core, dns, audio, cli, multicast, observer — here 'supervisor' is the Supervisor service's own logs, not an app with that name.",
          "default": null
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "History & Statistics"
    ],
    "source_file": "tools_logs.py"
  },
  {
    "name": "ha_get_integration",
    "title": "Get Integration",
    "description": "Get integration (config entry) information with pagination.\n\nWithout an entry_id: Lists all configured integrations with optional filters.\nWith an entry_id: Returns detailed information including full options/configuration.\n\nEXAMPLES:\n- List / search: ha_get_integration(query=\"zigbee\")\n- Get an entry with its editable fields: ha_get_integration(entry_id=\"abc123\", include_schema=True)\n- Diagnostics dump, paged along a list-valued path: ha_get_integration(entry_id=\"abc123\", include_diagnostics=True, diagnostics_data_path=\"<list-valued path>\", diagnostics_data_limit=10, diagnostics_data_offset=20)\n- Inspect a subentry reconfigure schema: ha_get_integration(entry_id=\"abc123\", include_subentry_schema=True, subentry_type=\"conversation\", subentry_id=\"sub123\")\n- List template entries with their options: ha_get_integration(domain=\"template\")\n\nSTATES: 'loaded', 'setup_error', 'setup_retry', 'not_loaded',\n'failed_unload', 'migration_error'.\n\nOPTIONS: ``options`` reflect the entry's persisted values; a field that\nwas never set may be absent (rather than shown at its schema default).\nValues that match a ``secrets.yaml`` entry are returned as\n``\"**redacted**\"``. Use ``include_schema=True`` to see every editable\nfield and its default/type. Nested option *sections* (e.g. a template\nhelper's ``advanced_options``) are additively flattened one level —\neach section's leaf keys are copied to the top of ``options`` (mirroring\nthe OptionsFlow-derived read) while the raw nested section is preserved\nfor fidelity, and an existing top-level key is never overwritten.\n\nEach entry carries ``log_level``: the canonical Python logger level name\n(``DEBUG``/``INFO``/``WARNING``/``ERROR``/``CRITICAL``) when the\nintegration has a ``logger.set_level`` override, or ``\"DEFAULT\"``\n(uppercase sentinel) when no override is set; and ``log_level_raw``: the\noriginal numeric level (e.g. ``10`` for DEBUG) when HA returned an int,\n``None`` otherwise. This is distinct from the app side, where\n``ha_get_app`` returns Supervisor's lowercase ``\"default\"`` literal — do\nnot cross-compare.",
    "inputSchema": {
      "properties": {
        "entry_id": {
          "type": "str | None",
          "description": "Config entry ID to get details for.",
          "default": null
        },
        "query": {
          "type": "str | None",
          "description": "When listing, search by domain or title.",
          "default": null
        },
        "domain": {
          "type": "str | None",
          "description": "Filter by integration domain (e.g. 'template', 'group'). When set, includes the full options/configuration for each entry.",
          "default": null
        },
        "include_options": {
          "type": "bool",
          "description": "Include the options object for each entry. Automatically enabled when domain filter is set. For UI-created flow-based helpers (template, group, utility_meter, derivative, ...), the current config — template body, group members, source entity, etc. — is surfaced here by probing the options flow. Prefer this over include_schema when you only need to read the current values; use include_schema when you also need the field types or selector metadata.",
          "default": false
        },
        "include_schema": {
          "type": "bool",
          "description": "When entry_id is set, also return the options flow schema (available fields and their types). Only applies when supports_options=true.",
          "default": false
        },
        "include_subentries": {
          "type": "bool",
          "description": "When entry_id is set, include config subentries for the integration entry.",
          "default": false
        },
        "include_subentry_schema": {
          "type": "bool",
          "description": "When entry_id is set, return introspection-only config subentry schema information; no subentry is created. Pair with subentry_type, and optionally subentry_id for reconfigure schema.",
          "default": false
        },
        "subentry_type": {
          "type": "str | None",
          "description": "Integration-defined subentry type used with include_subentry_schema=True.",
          "default": null
        },
        "subentry_id": {
          "type": "str | None",
          "description": "Existing subentry ID used with include_subentry_schema=True to inspect a reconfigure flow.",
          "default": null
        },
        "show_advanced_options": {
          "type": "bool",
          "description": "When include_subentry_schema=True, ask older Home Assistant versions to expose advanced flow options. No-op on HA 2026.6+; pending removal before HA 2027.6.",
          "default": false
        },
        "exact_match": {
          "type": "bool",
          "description": "Use exact substring matching for query filter. Set to False for fuzzy matching when the query may contain typos.",
          "default": true
        },
        "limit": {
          "type": "int",
          "default": 50,
          "constraints": {
            "ge": 1,
            "le": 200
          },
          "description": "Max entries to return per page in list mode"
        },
        "offset": {
          "type": "int",
          "default": 0,
          "constraints": {
            "ge": 0
          },
          "description": "Number of entries to skip for pagination"
        },
        "include_diagnostics": {
          "type": "bool",
          "description": "When entry_id is set, also fetch the integration's diagnostics dump — integration-defined JSON (commonly includes redacted config, device list, state snapshots; exact top-level keys vary by integration), the same artifact as the UI's 'Download diagnostics'. Payloads can be large (Hue ~290 KB, ZHA/MQTT/ESPHome several MB) — pair with diagnostics_fields or diagnostics_truncate_at_bytes.",
          "default": false
        },
        "include_knx_project": {
          "type": "bool",
          "description": "When entry_id is a KNX config entry, also return the parsed ETS project: the full group-address table (address, name, DPT, description) under knx_project.group_addresses, plus the group-range hierarchy and project metadata. This GA table is not in the diagnostics dump; per-entity GA assignments are (config_store / configuration_yaml). Ignored (with a warning) when the entry is not a KNX integration. KNX exposes a single project, so the result is the same for every KNX entry_id.",
          "default": false
        },
        "device_id": {
          "type": "str | None",
          "description": "With include_diagnostics=True, return the device-scoped diagnostics dump for this device instead of the full integration dump. Some integrations only expose config-entry-level dumps.",
          "default": null
        },
        "diagnostics_fields": {
          "type": "list[str] | str | None",
          "description": "Top-level keys to keep from the diagnostics data payload (e.g. ['home_assistant', 'issues']). Accepts a JSON list or comma-separated string. Only applies when include_diagnostics=True and the data payload is a dict. Unknown keys are dropped and listed under omitted_fields.",
          "default": null
        },
        "diagnostics_truncate_at_bytes": {
          "type": "int | None",
          "description": "Byte cap on the serialized diagnostics payload (after diagnostics_fields and diagnostics_data_path have been applied). On hit, drops data and emits truncated=true, bytes_total, byte_cap, plus available_fields (when the capped value is a dict). Recommended starting point: 20000 bytes. Only applies when include_diagnostics=True.",
          "default": null,
          "constraints": {
            "ge": 1
          }
        },
        "diagnostics_data_path": {
          "type": "str | None",
          "description": "Dotted path into the diagnostics data sub-tree (e.g. '<list-valued path>' for per-device records, 'home_assistant.version' for HA core version; the exact key path varies by integration version). Walks into the post-fields payload. Resolution failures replace data with null and surface data_path_error. Only applies when include_diagnostics=True.",
          "default": null
        },
        "diagnostics_data_offset": {
          "type": "int | None",
          "description": "Pagination start index for list-valued diagnostics_data_path results. Ignored when diagnostics_data_path is unset, diagnostics_data_limit is unset, or the resolved value is not a list. Only applies when include_diagnostics=True.",
          "default": 0,
          "constraints": {
            "ge": 0
          }
        },
        "diagnostics_data_limit": {
          "type": "int | None",
          "description": "Pagination window size for list-valued diagnostics_data_path results. When set with a list-resolved path, swaps data for a pagination envelope {path, items, offset, limit, total, has_more}. Default None returns the full resolved value. Only applies when include_diagnostics=True.",
          "default": null,
          "constraints": {
            "ge": 1
          }
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Integrations"
    ],
    "source_file": "tools_integrations.py"
  },
  {
    "name": "ha_get_system_health",
    "title": "Get System Health (incl. ZHA/Z-Wave/integration diagnostics)",
    "description": "Get Home Assistant system health, including Zigbee (ZHA), Z-Wave JS, and per-integration diagnostics dumps.\n\nReturns health check results from integrations, system resources, and\nconnectivity; available information varies by installation type and\nloaded integrations. Optional sections are selected with `include`.\n\nThe result also carries ``ha_mcp_update`` ``{current, latest,\nupdate_available}``, reporting whether a newer ha-mcp release is\navailable (PyPI for pip/Docker, the Supervisor store for the app) —\nproactively tell the user to upgrade. Present on every install type\nincluding the HA app; omitted for the ``unknown`` version, when\n``HA_MCP_DISABLE_UPDATE_CHECK`` is set, or when the update check itself\nfailed.\n\nEXAMPLES:\n- ha_get_system_health(include=\"repairs,zha_network,zwave_network,config_check\")\n- Page a list-valued diagnostics sub-tree: ha_get_system_health(include=\"diagnostics\", config_entry_id=\"abc\", diagnostics_data_path=\"<list-valued path>\", diagnostics_data_limit=10), then repeat with diagnostics_data_offset=10 while has_more is true.",
    "inputSchema": {
      "properties": {
        "include": {
          "type": "str | None",
          "description": "Comma-separated extra sections: 'repairs' (Repair items, active only unless include_dismissed_repairs=True); 'zha_network' (ZHA devices with radio signal summary: name, LQI, RSSI); 'zha_network_full' (all ZHA device details; large on 100+ device networks); 'zwave_network' (Z-Wave JS status and node summary: status, security, routing); 'thread_network' (per border-router channel, extended_pan_id and border_agent_id; not per-node Thread health); 'matter_network' (Matter integration presence: config_entry_id, state, title; per-node health is in Matter node diagnostics); 'themes' (installed theme names, count, default_theme, default_dark_theme); 'diagnostics' (per-integration diagnostics dump, integration-defined JSON; REQUIRES config_entry_id; payloads can be large — pair with diagnostics_fields or diagnostics_truncate_at_bytes); 'config_check' (validate the HA configuration, the pre-restart check ha_restart runs automatically; returns {result: valid|invalid, is_valid, errors}); 'dead_entities' (orphaned/stale entity-registry entries: config_entry_orphans whose owning integration instance is gone, and stale_restored entries HA restored on startup that the loaded integration no longer provides, each with entity_id + platform for cleanup via ha_remove_entity; unknown-state entities and merely-offline devices are excluded).",
          "default": null
        },
        "include_dismissed_repairs": {
          "type": "bool | None",
          "description": "Include user-dismissed/ignored repairs. Only meaningful when 'repairs' is in include.",
          "default": false
        },
        "config_entry_id": {
          "type": "str | None",
          "description": "Config entry ID of the integration (find via ha_get_integration). Required when include contains 'diagnostics'.",
          "default": null
        },
        "device_id": {
          "type": "str | None",
          "description": "With include='diagnostics', return the device-scoped dump for this device instead of the full integration dump. Some integrations only expose config-entry-level dumps.",
          "default": null
        },
        "diagnostics_fields": {
          "type": "list[str] | str | None",
          "description": "Top-level keys to keep from the diagnostics data payload (e.g. ['home_assistant', 'issues']). Accepts a JSON list or comma-separated string. Only applies with include='diagnostics'.",
          "default": null
        },
        "diagnostics_truncate_at_bytes": {
          "type": "int | None",
          "constraints": {
            "ge": 1
          },
          "description": "Byte cap on the serialized diagnostics payload (post-projection / post-data_path). On hit, drops data and emits truncated=true, bytes_total, byte_cap, plus available_fields (when the capped value is a dict). Recommended starting point: 20000. Only applies with include='diagnostics'.",
          "default": null
        },
        "diagnostics_data_path": {
          "type": "str | None",
          "description": "Dotted path into the diagnostics data sub-tree (e.g. 'data.devices' for ZHA per-device records). Walks into the post-fields payload. Resolution failures replace data with null and surface data_path_error. Only applies with include='diagnostics'.",
          "default": null
        },
        "diagnostics_data_offset": {
          "type": "int | None",
          "constraints": {
            "ge": 0
          },
          "description": "Pagination start index for list-valued diagnostics_data_path results. Only applies with include='diagnostics'.",
          "default": 0
        },
        "diagnostics_data_limit": {
          "type": "int | None",
          "constraints": {
            "ge": 1
          },
          "description": "Pagination window for list-valued diagnostics_data_path results; data becomes {path, items, offset, limit, total, has_more}. Only applies with include='diagnostics'.",
          "default": null
        }
      }
    },
    "annotations": {
      "openWorldHint": true,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Integrations",
      "Matter",
      "System",
      "Thread",
      "Z-Wave",
      "Zigbee"
    ],
    "source_file": "tools_system.py"
  },
  {
    "name": "ha_set_integration",
    "title": "Set Integration",
    "description": "Manage an integration (config entry): enable/disable, add, update options, or reconfigure.\n\nModes (pick one):\n- Enable/disable: entry_id + enabled.\n- Add integration: domain (+ config) — drives the domain's config\n  flow, including menus and multi-step forms.\n- Update options: entry_id + config — drives the entry's options\n  flow (what the \"Configure\" button does in the HA UI).\n- Reconfigure: entry_id + reconfigure=True + config — connection\n  settings such as host, port, credentials. Repeat with the token the\n  preflight returns as confirm_token to apply.\n\nWHEN NOT TO USE:\n- Helpers (template, group, utility_meter, ...): use\n  ha_config_set_helper. The exception is `otp`, which is a helper in\n  the HA UI but is created HERE via domain=\"otp\" — its flow needs a\n  live TOTP code, so ha_config_set_helper deliberately omits it.\n- Config subentries: use\n  ha_config_set_helper(helper_type='config_subentry').\n- Removing an entry: use ha_remove_helpers_integrations.\n\nUse ha_get_integration() to find entry IDs, and\nha_get_integration(entry_id=..., include_schema=True) to inspect the\noptions fields before an update. Its supports_reconfigure field tells\nyou whether an entry qualifies for reconfigure=True.\n\nCaveats: adding an integration runs its config flow exactly as the HA\nUI would (may pair devices, scan the network, create entities). Flows\nrequiring a browser step (OAuth) or an asynchronous provider step\nerror out at that step instead of completing. Reconfigure edits the\nsettings a live integration connects with: a wrong host or credential\ntakes it offline, and there is no automatic rollback — the returned\nrollback metadata describes repeating the official flow by hand with\nthe previous values, which this tool cannot read back. The preflight\ndoes not validate config keys against the integration's form; wrong\nfield names surface on the confirm call.\n\nEXAMPLES:\n- Add: ha_set_integration(domain=\"workday\", config={\"name\": \"Workday\"})\n- Update options: ha_set_integration(entry_id=\"abc123\", config={\"scan_interval\": 30})\n- Reconfigure preflight: ha_set_integration(entry_id=\"abc123\", reconfigure=True, config={\"host\": \"10.0.0.5\"}), then repeat adding confirm_token=\"sha256:...\"",
    "inputSchema": {
      "properties": {
        "entry_id": {
          "type": "str | None",
          "description": "Config entry ID of an existing integration.",
          "default": null
        },
        "enabled": {
          "type": "bool | None",
          "description": "True to enable, False to disable the entry. Mutually exclusive with 'domain' and 'config'.",
          "default": null
        },
        "domain": {
          "type": "str | None",
          "description": "Integration domain to add (e.g. 'workday', 'local_calendar').",
          "default": null
        },
        "config": {
          "type": "dict[str, Any] | None",
          "description": "Flow form data. Updating an existing entry — options or reconfigure — is a patch: a field you omit keeps its current value, and a field set to null is cleared where the integration's schema allows that field to be empty. Multi-step flows consume keys per step. A field two steps declare gets your one value both times; pass step_values={'<step_id>': {'<field>': <value>}} to give a step its own value, or to leave the field out of that step; a LIST of those objects supplies one per encounter when the flow presents a step more than once. Menu steps take 'next_step_id' — a string, or a list of successive selections for flows that present more than one menu (e.g. a menu revisited after each branch, ending in a finish option). The step's data_schema is returned on validation errors so field names can be corrected.",
          "default": null
        },
        "reconfigure": {
          "type": "bool",
          "default": false,
          "description": "Use the existing config entry's official reconfigure flow (its connection settings) instead of its options flow. Without confirm_token this is a read-only preflight that returns one."
        },
        "expected_device_id": {
          "type": "str | None",
          "default": null,
          "description": "Requires reconfigure=True. Device registry ID the entry must still own, before and after the change."
        },
        "expected_unique_id": {
          "type": "str | None",
          "default": null,
          "description": "Requires reconfigure=True, AND the ha_mcp_tools custom component: Home Assistant does not expose a config entry's unique_id over its API. Without the component this is rejected — anchor on expected_device_id, expected_mac or expected_entity_ids instead."
        },
        "expected_mac": {
          "type": "str | None",
          "default": null,
          "description": "Requires reconfigure=True. MAC or IEEE the entry's device must still report."
        },
        "expected_entity_ids": {
          "type": "list[str] | None",
          "default": null,
          "description": "Requires reconfigure=True. Exact entity IDs that must remain associated with the entry."
        },
        "confirm_token": {
          "type": "str | None",
          "default": null,
          "description": "Requires reconfigure=True. Any token still matching the entry's current state and the same requested config is accepted, so a token stays valid while nothing moves."
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "readOnlyHint": false,
      "idempotentHint": false
    },
    "tags": [
      "Integrations"
    ],
    "source_file": "tools_integrations.py"
  },
  {
    "name": "ha_config_get_category",
    "title": "Get Category",
    "description": "Get category info - list all categories for a scope or get a specific one by ID.\n\nWithout a category_id: Lists all Home Assistant categories for the given scope.\nWith a category_id: Returns configuration for that specific category.\n\nCategories are domain-scoped organizational groups for automations, scripts, scenes, and helpers.\n\nEXAMPLES:\n- List helper categories: ha_config_get_category(\"helpers\")\n- Get specific category: ha_config_get_category(\"automation\", category_id=\"my_category_id\")\n\nUse ha_config_set_category() to create or update categories.\nUse ha_set_entity(categories={\"automation\": \"category_id\"}) to assign categories to entities.",
    "inputSchema": {
      "properties": {
        "scope": {
          "type": "str",
          "description": "Domain scope for categories (e.g., 'automation', 'script', 'scene', 'helpers')."
        },
        "category_id": {
          "type": "str | None",
          "description": "ID of the category to retrieve.",
          "default": null
        }
      },
      "required": [
        "scope"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Labels & Categories"
    ],
    "source_file": "tools_categories.py"
  },
  {
    "name": "ha_config_get_label",
    "title": "Get Label",
    "description": "Get label info - list all labels or get a specific one by ID.\n\nWithout a label_id: Lists all Home Assistant labels with their configurations.\nWith a label_id: Returns configuration for that specific label.\n\nEXAMPLES:\n- List all labels: ha_config_get_label()\n- Get specific label: ha_config_get_label(\"my_label_id\")\n\nUse ha_config_set_label() to create or update labels.\nUse ha_set_entity(labels=[\"label1\", \"label2\"]) to assign labels to entities,\nha_set_device(labels=[...]) for devices, or\nha_set_area_or_floor(kind=\"area\", labels=[...]) for areas.",
    "inputSchema": {
      "properties": {
        "label_id": {
          "type": "str | None",
          "description": "ID of the label to retrieve.",
          "default": null
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Labels & Categories"
    ],
    "source_file": "tools_labels.py"
  },
  {
    "name": "ha_config_remove_category",
    "title": "Remove Category",
    "description": "Delete a Home Assistant category.\n\nRemoves the category from the category registry for the given scope.\nThis will also remove the category assignment from all entities in that scope.\n\nEXAMPLES:\n- Delete category: ha_config_remove_category(\"automation\", \"my_category_id\")\n\nUse ha_config_get_category() to find category IDs.\n\n**WARNING:** Deleting a category will remove it from all assigned entities.\nThis action cannot be undone.",
    "inputSchema": {
      "properties": {
        "scope": {
          "type": "str",
          "description": "Domain scope for the category (e.g., 'automation', 'script', 'scene', 'helpers')."
        },
        "category_id": {
          "type": "str",
          "description": "ID of the category to delete"
        }
      },
      "required": [
        "scope",
        "category_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": true
    },
    "tags": [
      "Labels & Categories"
    ],
    "source_file": "tools_categories.py"
  },
  {
    "name": "ha_config_remove_label",
    "title": "Remove Label",
    "description": "Delete a Home Assistant label.\n\nRemoves the label from the label registry. This will also remove the label\nfrom all entities, devices, and areas that have it assigned.\n\nEXAMPLES:\n- Delete label: ha_config_remove_label(\"my_label_id\")\n\nUse ha_config_get_label() to find label IDs.\n\n**WARNING:** Deleting a label will remove it from all assigned entities.\nThis action cannot be undone.",
    "inputSchema": {
      "properties": {
        "label_id": {
          "type": "str",
          "description": "ID of the label to delete"
        }
      },
      "required": [
        "label_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": true
    },
    "tags": [
      "Labels & Categories"
    ],
    "source_file": "tools_labels.py"
  },
  {
    "name": "ha_config_set_category",
    "title": "Create or Update Category",
    "description": "Create or update a Home Assistant category.\n\nCreates a new category if category_id is not provided, or updates an existing category if category_id is provided.\n\nCategories are domain-scoped organizational groups for automations, scripts,\nscenes, and helpers. Unlike labels (which are cross-domain), categories are\nspecific to a single domain scope.\n\nEXAMPLES:\n- Create automation category: ha_config_set_category(\"Lighting\", scope=\"automation\")\n- Create with icon: ha_config_set_category(\"Security\", scope=\"automation\", icon=\"mdi:shield\")\n- Update category: ha_config_set_category(\"Updated Name\", scope=\"automation\", category_id=\"my_category_id\")\n\nAfter creating a category, use ha_set_entity(categories={\"automation\": \"category_id\"}) to assign it.",
    "inputSchema": {
      "properties": {
        "name": {
          "type": "str",
          "description": "Display name for the category"
        },
        "scope": {
          "type": "str",
          "description": "Domain scope for the category (e.g., 'automation', 'script', 'scene', 'helpers')."
        },
        "category_id": {
          "type": "str | None",
          "description": "Category ID for updates.",
          "default": null
        },
        "icon": {
          "type": "str | None",
          "description": "Material Design Icon (e.g., 'mdi:tag', 'mdi:label')",
          "default": null
        }
      },
      "required": [
        "name",
        "scope"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Labels & Categories"
    ],
    "source_file": "tools_categories.py"
  },
  {
    "name": "ha_config_set_label",
    "title": "Create or Update Label",
    "description": "Create or update a Home Assistant label.\n\nCreates a new label if label_id is not provided, or updates an existing\nlabel if label_id is provided. Labels are a cross-domain tagging system\nfor entities, devices, and areas.\n\nEXAMPLES:\n- Create: ha_config_set_label(\"Security\", color=\"red\", icon=\"mdi:shield\", description=\"Security-related devices\")\n- Update: ha_config_set_label(\"Updated Name\", label_id=\"my_label_id\", color=\"blue\")\n- Create and apply to areas: ha_config_set_label(\"Site Home\", areas=[\"kitchen\", \"living_room\"])\n\nAfter creating a label, use ha_set_entity(labels=[\"label_id\"]) to assign it to entities,\nha_set_device(labels=[\"label_id\"]) for devices, or\nha_set_area_or_floor(kind=\"area\", labels=[\"label_id\"]) for areas (replaces the area's set).",
    "inputSchema": {
      "properties": {
        "name": {
          "type": "str",
          "description": "Display name for the label"
        },
        "label_id": {
          "type": "str | None",
          "description": "Label ID for updates.",
          "default": null
        },
        "color": {
          "type": "str | None",
          "description": "Color for the label (e.g., 'red', 'blue', 'green', or hex like '#FF5733')",
          "default": null
        },
        "icon": {
          "type": "str | None",
          "description": "Material Design Icon (e.g., 'mdi:tag', 'mdi:label')",
          "default": null
        },
        "description": {
          "type": "str | None",
          "description": "Description of the label's purpose",
          "default": null
        },
        "areas": {
          "type": "str | list[str] | None",
          "description": "Area IDs to apply this label to (adds the label without removing existing ones). Omit to leave area assignments unchanged; an empty list is a no-op (assigns nothing and removes nothing). To clear an area's labels use ha_set_area_or_floor(kind='area', labels=[]).",
          "default": null
        }
      },
      "required": [
        "name"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Labels & Categories"
    ],
    "source_file": "tools_labels.py"
  },
  {
    "name": "ha_manage_radio",
    "title": "Manage Radios (Z-Wave / Zigbee / Matter / Thread)",
    "description": "Manage Home Assistant radios — Z-Wave, Zigbee, Matter, and Thread.\n\nFor read-only inspection prefer ha_get_device / ha_get_system_health,\nwhich mirror the 'diagnostics' and 'network_status' actions; use this\ntool for writes and the active 'ping' probe (unique to this tool). Write\nactions perform inclusion/commissioning, removal, healing,\nreconfiguration, firmware updates and credential provisioning.\n\nCaveats: long-running actions (inclusion, rebuild routes, firmware) start the\noperation and return immediately with long_running=true; completion\nhappens out-of-band. Interactive Z-Wave S2 secure inclusion (read-the-\nPIN pairing) is not scriptable — use SmartStart/QR provisioning here or\nthe HA UI.",
    "inputSchema": {
      "properties": {
        "radio": {
          "type": "Literal['zwave', 'zigbee', 'matter', 'thread']",
          "description": "Which radio to manage."
        },
        "action": {
          "type": "str",
          "description": "Operation to perform. Actions vary per radio; an unknown action returns the supported list for that radio. Common: 'diagnostics', 'network_status', 'ping', 'add'/'commission', 'remove_device', 'reinterview'/'reconfigure', 'firmware_update'."
        },
        "device_id": {
          "type": "str | None",
          "description": "Target device (node) for node-scoped actions.",
          "default": null
        },
        "entity_id": {
          "type": "str | None",
          "description": "Resolve the device from this entity for node-scoped actions.",
          "default": null
        },
        "params": {
          "type": "dict[str, Any] | None",
          "description": "Action-specific parameters (e.g. code, pin, channel, property, value).",
          "default": null
        },
        "confirm": {
          "type": "bool",
          "description": "Required (True) to run destructive actions (e.g. remove_device, network restore, change_channel, hard_reset, remove_fabric).",
          "default": false
        }
      },
      "required": [
        "radio",
        "action"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": false
    },
    "tags": [
      "Matter",
      "Radio Management",
      "Thread",
      "Z-Wave",
      "Zigbee"
    ],
    "source_file": "tools_radio.py"
  },
  {
    "name": "ha_config_get_scene",
    "title": "Get or Find Scenes",
    "description": "Get a scene's complete configuration, or list and search scenes without scene_id.\n\nUse ha_search for cross-domain discovery and dependency searches. For ordinary\nscene discovery, use this tool and pass a returned scene_id back to retrieve\nthe complete entities dict and config_hash for editing (pass that hash to\nha_config_set_scene for python_transform updates).\n\nListing returns compact metadata. Integration-managed scenes have no editable\nstorage config or scene_id. Partial content-search results explicitly\nreport unread configs and are not exhaustive.\n\nEXAMPLES:\n- Get scene: ha_config_get_scene(\"movie_night\")\n- Find scenes: ha_config_get_scene(query=\"movie\")\n- Find attribute values: ha_config_get_scene(query=\"rainbow\", search_in_config=True)\n\nFor detailed scene configuration help, use ha_get_skill_guide.",
    "inputSchema": {
      "properties": {
        "scene_id": {
          "type": "str | None",
          "description": "Scene identifier",
          "default": null
        },
        "query": {
          "type": "str | None",
          "description": "Filter scene names or IDs",
          "default": null
        },
        "search_in_config": {
          "type": "bool",
          "description": "Also search full stored scene attribute values within a bounded scan",
          "default": false
        },
        "limit": {
          "type": "int",
          "description": "Maximum scenes per page",
          "constraints": {
            "ge": 1,
            "le": 100
          },
          "default": 20
        },
        "offset": {
          "type": "int",
          "description": "Pagination offset",
          "constraints": {
            "ge": 0
          },
          "default": 0
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Scenes"
    ],
    "source_file": "tools_config_scenes.py"
  },
  {
    "name": "ha_config_remove_scene",
    "title": "Remove Scene",
    "description": "Delete a Home Assistant scene.\n\nEXAMPLE: ha_config_remove_scene(\"old_scene\")\n\nOnly scenes created via the Home Assistant UI can be deleted. Scenes\ndefined in YAML configuration files (scenes.yaml or configuration.yaml)\ncannot be deleted through the API and return a 405 Method Not Allowed\nerror; edit the configuration file directly instead.\n\nWARNING: Deleting a scene that is referenced by automations or scripts\n(via ``scene.turn_on``) may cause those to fail.",
    "inputSchema": {
      "properties": {
        "scene_id": {
          "type": "str",
          "description": "Scene identifier to delete (e.g., 'old_scene')"
        },
        "wait": {
          "type": "bool",
          "description": "Wait for scene to be fully removed before returning.",
          "default": true
        }
      },
      "required": [
        "scene_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": true
    },
    "tags": [
      "Scenes"
    ],
    "source_file": "tools_config_scenes.py"
  },
  {
    "name": "ha_config_set_scene",
    "title": "Create or Update Scene",
    "description": "Create or update a Home Assistant scene.\n\nMUST call ha_get_skill_guide OR refer to your locally installed skills first.\n\nSupports two modes: full config replacement (``config``) or\nPython transformation of an existing scene (``python_transform``).\nSee the field descriptions for ``python_transform`` examples and\nthe ``config`` shape contract.\n\nWHEN TO USE:\n- ``python_transform``: surgical edits to an existing scene\n  (add/remove/update a single entity entry).\n- ``config``: creating a new scene, or wholesale replacement.\n\nWHEN NOT TO USE:\n- To activate a scene at runtime, use ha_call_service(domain=\"scene\",\n  service=\"turn_on\", target=...) — this tool only manages scene\n  *configuration*, not the runtime turn-on/off side.\n- To list or look up existing scenes, use ha_config_get_scene.\n\nSCENE SHAPE: Automations use a list of actions; scenes capture a snapshot\nof states as a dict.\n\nEXAMPLE:\n\nha_config_set_scene(scene_id=\"movie_night\", config={\n    \"name\": \"Movie Night\",\n    \"entities\": {\n        \"light.living_room\": {\"state\": \"on\", \"brightness\": 50},\n    },\n    \"icon\": \"mdi:movie\",\n})\n\nThe top-level ``SKILL.md`` for home-assistant-best-practices ships in\nthis response under ``skill_content`` by default — generic\nbest-practice index covering entity-naming and\nsafe-refactoring patterns that intersect with scene authoring. For\ndetailed scene configuration help beyond that, use ha_get_skill_guide.",
    "inputSchema": {
      "properties": {
        "scene_id": {
          "type": "str",
          "description": "Scene identifier (e.g., 'movie_night')"
        },
        "config": {
          "type": "dict[str, Any] | None",
          "description": "Scene configuration dictionary. Must include 'entities' (a dict keyed by entity_id, NOT a list). Optional fields: 'name' (defaults to scene_id), 'icon', 'id'. Mutually exclusive with python_transform.",
          "default": null
        },
        "python_transform": {
          "type": "str | None",
          "description": "Python expression to transform existing scene config. Mutually exclusive with config. WARNING: Expressions with infinite loops will hang the server. Examples: Add entity: python_transform=\"config['entities']['light.bed'] = {'state': 'on'}\" Update brightness: python_transform=\"config['entities']['light.kitchen']['brightness'] = 50\" Remove entity: python_transform=\"del config['entities']['light.kitchen']\" \n\nPYTHON TRANSFORM SECURITY:\n\n✅ ALLOWED:\n- Dictionary/list access: config['views'][0]['cards'][1]\n- Slicing: config['views'][0]['cards'][1:3]\n- Assignment: config['key'] = 'value'\n- Deletion: del config['key'] or config.pop('key')\n- List methods: append, insert, pop, remove, clear, extend\n- Dict methods: update, get, setdefault, keys, values, items\n- Loops: for, if/else, pass, break, continue\n- Comprehensions: [x for x in ...], {k: v for ...}, (x for x in ...)\n- Ternary: x if condition else y\n- Iterable unpacking (* in calls/literals): f(*xs), [*xs, y]\n- Dict unpacking (**) in calls and dict literals: {**d, 'k': v}\n- Keyword arguments: func(key=value)\n- Lambdas (e.g. for `key=`): sorted(items, key=lambda x: x['score'])\n- String methods: startswith, endswith, lower, upper, strip, split, join, replace\n- Safe builtins: isinstance, len, range, enumerate, zip, sorted, reversed,\n  min, max, sum, abs, any, all, round, str, int, float, bool, list, dict,\n  tuple, set\n\n❌ FORBIDDEN:\n- Imports: import, from, __import__\n- File operations: open, read, write\n- Dunder access: __class__, __bases__, __subclasses__\n- Dangerous builtins: eval, exec, compile, getattr, setattr, delattr, hasattr\n- Function definitions: def, class\n- Exception handling: try/except (validate with isinstance/in/.get() instead)\n- While loops: use bounded for loops or comprehensions instead",
          "default": null
        },
        "config_hash": {
          "type": "str | None",
          "description": "Config hash from ha_config_get_scene for optimistic locking. REQUIRED for python_transform (validates scene unchanged). Optional for config updates (validates before full replacement if provided).",
          "default": null
        },
        "category": {
          "type": "str | None",
          "description": "Category ID to assign to this scene. Use ha_config_get_category(scope='scene') to list available categories, or ha_config_set_category() to create one.",
          "default": null
        },
        "wait": {
          "type": "bool",
          "description": "Wait for scene to be queryable before returning. Set to False for bulk operations.",
          "default": true
        },
        "MandatoryBPS": {
          "type": "bool",
          "default": true
        },
        "BestPracticeKey": {
          "type": "BestPracticeKeyParam",
          "default": null
        }
      },
      "required": [
        "scene_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Scenes"
    ],
    "source_file": "tools_config_scenes.py"
  },
  {
    "name": "ha_config_get_script",
    "title": "Get Script Config",
    "description": "Get Home Assistant script configuration.\n\nReturns the complete configuration for a script, including sequence, mode, fields, and other settings.\n\nThe returned `config_hash` is stable across consecutive reads of an unchanged config — `compute_config_hash` documents the underlying contract.\n\nThe returned `script_id` is the canonical bare storage key resolved by the REST client (matching what `ha_config_set_script` / `ha_config_remove_script` expect), falling back to the input identifier on the rare path where the REST envelope omits it. Prefix handling matches `ha_config_get_automation` in behavior (mechanism differs: automations resolve via state lookup; scripts strip the prefix).\n\nEXAMPLES:\n- Get script (bare form): ha_config_get_script(\"morning_routine\")\n- Get script (entity_id form): ha_config_get_script(\"script.morning_routine\")\n\nFor detailed script configuration help, use ha_get_skill_guide.",
    "inputSchema": {
      "properties": {
        "script_id": {
          "type": "str",
          "description": "Script identifier — bare storage key ('morning_routine') or entity_id form ('script.morning_routine'); a leading 'script.' prefix is stripped before lookup."
        }
      },
      "required": [
        "script_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Scripts"
    ],
    "source_file": "tools_config_scripts.py"
  },
  {
    "name": "ha_config_remove_script",
    "title": "Remove Script",
    "description": "Delete a Home Assistant script.\n\nEXAMPLE: ha_config_remove_script(\"old_script\")\n\nOnly scripts created via the Home Assistant UI can be deleted. Scripts\ndefined in YAML configuration files (scripts.yaml or configuration.yaml)\ncannot be deleted through the API and return a 405 Method Not Allowed\nerror; edit the configuration file directly instead.\n\nWARNING: Deleting a script that is used by automations may cause those automations to fail.",
    "inputSchema": {
      "properties": {
        "script_id": {
          "type": "str",
          "description": "Script identifier to delete — bare storage key ('old_script') or entity_id form ('script.old_script'); a leading 'script.' prefix is stripped before lookup."
        },
        "wait": {
          "type": "bool",
          "description": "Wait for script to be fully removed before returning.",
          "default": true
        }
      },
      "required": [
        "script_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": true
    },
    "tags": [
      "Scripts"
    ],
    "source_file": "tools_config_scripts.py"
  },
  {
    "name": "ha_config_set_script",
    "title": "Create or Update Script",
    "description": "Create or update a Home Assistant script.\n\nMUST call ha_get_skill_guide OR refer to your locally installed skills first.\n\nPrefer native actions (`choose` / `if`, `wait_for_trigger`, `repeat`,\n`for:`) over templates in logic positions; templates belong only in\n`data.*`, notification text, `event_data` and `variables`. The\nbest-practice checker reports violations under `best_practice_warnings`.\n`automation-patterns.md` and `template-guidelines.md` ship under\n`skill_content` by default.\n\nScripts use 'sequence', NOT 'trigger' or 'action'; for trigger-based\nexecution use ha_config_set_automation.\n\nMODES (pick one):\n- python_transform: surgical edits to an existing script. Requires\n  config_hash from ha_config_get_script(), e.g.\n  python_transform=\"config['sequence'].append({'delay': {'seconds': 5}})\"\n- config: new scripts or full restructures. Needs 'sequence' (regular) or\n  'use_blueprint' {path, input} (blueprint-based).\n- take_control_of_blueprint: convert a blueprint-backed script into a\n  standalone one. Takes no config of its own.\n\nEXAMPLES:\n- Create: ha_config_set_script(script_id=\"blink_light\", config={\"alias\": \"Light Blink\", \"sequence\": [{\"action\": \"light.turn_on\", \"target\": {\"entity_id\": \"light.living_room\"}}, {\"delay\": {\"seconds\": 2}}, {\"action\": \"light.turn_off\", \"target\": {\"entity_id\": \"light.living_room\"}}]})\n- From a blueprint: ha_config_set_script(script_id=\"notification_script\", config={\"alias\": \"My Notification Script\", \"use_blueprint\": {\"path\": \"notification_script.yaml\", \"input\": {\"message\": \"Hello World\"}}})\n- Take control: ha_config_set_script(script_id=\"notification_script\", take_control_of_blueprint=True)\n\nTAKE CONTROL is one-way: later blueprint edits stop reaching the script;\nto change an input value, update 'use_blueprint.input' instead. It does\nNOT free the blueprint — Home Assistant keeps counting the converted\nscript as a user, so deleting that blueprint stays refused until the\nscript is removed. To preview the rendering without writing anything,\nuse ha_manage_blueprints(action=\"substitute\", domain=\"script\", path=..., input=...).",
    "inputSchema": {
      "properties": {
        "script_id": {
          "type": "str",
          "description": "Script identifier — bare storage key ('morning_routine') or entity_id form ('script.morning_routine'); a leading 'script.' prefix is stripped before lookup."
        },
        "config": {
          "type": "dict[str, Any] | None",
          "description": "Script configuration dictionary. Mutually exclusive with python_transform.",
          "default": null
        },
        "python_transform": {
          "type": "str | None",
          "description": "Python expression to transform existing script config. Mutually exclusive with config. WARNING: Expressions with infinite loops will hang the server. Examples: Simple: python_transform=\"config['sequence'][0]['data']['message'] = 'Hello'\" Pattern: python_transform=\"for step in config['sequence']: if step.get('alias') == 'My Step': step['data']['value'] = 100\" \n\nPYTHON TRANSFORM SECURITY:\n\n✅ ALLOWED:\n- Dictionary/list access: config['views'][0]['cards'][1]\n- Slicing: config['views'][0]['cards'][1:3]\n- Assignment: config['key'] = 'value'\n- Deletion: del config['key'] or config.pop('key')\n- List methods: append, insert, pop, remove, clear, extend\n- Dict methods: update, get, setdefault, keys, values, items\n- Loops: for, if/else, pass, break, continue\n- Comprehensions: [x for x in ...], {k: v for ...}, (x for x in ...)\n- Ternary: x if condition else y\n- Iterable unpacking (* in calls/literals): f(*xs), [*xs, y]\n- Dict unpacking (**) in calls and dict literals: {**d, 'k': v}\n- Keyword arguments: func(key=value)\n- Lambdas (e.g. for `key=`): sorted(items, key=lambda x: x['score'])\n- String methods: startswith, endswith, lower, upper, strip, split, join, replace\n- Safe builtins: isinstance, len, range, enumerate, zip, sorted, reversed,\n  min, max, sum, abs, any, all, round, str, int, float, bool, list, dict,\n  tuple, set\n\n❌ FORBIDDEN:\n- Imports: import, from, __import__\n- File operations: open, read, write\n- Dunder access: __class__, __bases__, __subclasses__\n- Dangerous builtins: eval, exec, compile, getattr, setattr, delattr, hasattr\n- Function definitions: def, class\n- Exception handling: try/except (validate with isinstance/in/.get() instead)\n- While loops: use bounded for loops or comprehensions instead",
          "default": null
        },
        "config_hash": {
          "type": "str | None",
          "description": "Config hash from ha_config_get_script for optimistic locking. Required for python_transform; optional for config updates (validates before full replacement if provided).",
          "default": null
        },
        "take_control_of_blueprint": {
          "type": "bool",
          "description": "Convert a blueprint-backed script into an editable standalone one -- the UI's \"Take control\". Renders the blueprint with its current inputs and saves the result over the same script, which keeps its script_id, alias and description and then has its own sequence and no 'use_blueprint'. Mutually exclusive with config and python_transform.",
          "default": false
        },
        "category": {
          "type": "str | None",
          "description": "Category ID to assign to this script. Use ha_config_get_category(scope='script') to list available categories, or ha_config_set_category() to create one.",
          "default": null
        },
        "wait": {
          "type": "bool",
          "description": "Wait for script to be queryable before returning. Set to False for bulk operations.",
          "default": true
        },
        "MandatoryBPS": {
          "type": "bool",
          "default": true
        },
        "BestPracticeKey": {
          "type": "BestPracticeKeyParam",
          "default": null
        }
      },
      "required": [
        "script_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Scripts"
    ],
    "source_file": "tools_config_scripts.py"
  },
  {
    "name": "ha_get_overview",
    "title": "Get System Overview",
    "description": "Get AI-friendly system overview with intelligent categorization.\n\nReturns comprehensive system information at the requested detail level,\nincluding Home Assistant base_url, version, location, timezone, entity overview,\nand active persistent notifications (if any).\nUse 'minimal' (default) for most queries. Domain counts and states_summary\nare always complete regardless of entity pagination.\n\nRequests whose fields= are composed only of system_info, notification,\nrepair, or server metadata fields skip the unrelated state, service, and\nregistry reads.\n\nDo not use this tool to inspect a known entity or a narrow set of entities.\nUse ha_get_state for one entity, ha_get_entity for registry metadata, or\nha_search with a domain or area filter. An unprojected overview collects\nsystem-wide state, service, and registry data and can be expensive on large\nHome Assistant installations.\n\nWhen the ha-mcp settings-UI sidecar is running (stdio mode, e.g. Claude\nDesktop / Claude Code) the response carries ``settings_url``, the local\nURL of the tool-configuration page; in standalone HTTP / Docker modes\nwith an HTTP settings prefix it instead carries ``settings_url_hint``,\nsaying where the page is mounted and how to construct the full URL. Hand\nwhichever is present to the user when they ask how to enable or disable\ntools or change server settings.\n\nThe response also carries ``ha_mcp_update`` ``{current, latest,\nupdate_available}`` (PyPI for pip/Docker, the Supervisor store for the\napp) — proactively tell the user when ``update_available`` is true.\nOmitted for the ``unknown`` version, when ``HA_MCP_DISABLE_UPDATE_CHECK``\nis set, or when the update check itself failed.",
    "inputSchema": {
      "properties": {
        "detail_level": {
          "type": "Literal['minimal', 'standard', 'full']",
          "default": "minimal",
          "description": "'minimal': 10 entities/domain, top-5 states; 'standard': 200 entities/page, top-10 states (use offset for more); 'full': 200 entities/page + entity_id + state + full states."
        },
        "domains": {
          "type": "str | list[str] | None",
          "default": null,
          "description": "Filter to specific domains (e.g. 'light,sensor' or ['light','sensor']). None = all domains."
        },
        "limit": {
          "type": "int | None",
          "default": null,
          "constraints": {
            "ge": 1
          },
          "description": "Max total entities across all domains (default: unlimited for minimal, 200 for standard/full)."
        },
        "offset": {
          "type": "int",
          "default": 0,
          "constraints": {
            "ge": 0
          },
          "description": "Number of entities to skip for pagination"
        },
        "max_entities_per_domain": {
          "type": "int | None",
          "default": null,
          "description": "Override default entity cap per domain (minimal=10, standard/full=unlimited). 0 = no limit on entities or states."
        },
        "include_state": {
          "type": "bool | None",
          "default": null,
          "description": "Include state field for entities (None = auto based on level). Full defaults to True."
        },
        "include_entity_id": {
          "type": "bool | None",
          "default": null,
          "description": "Include entity_id field for entities (None = auto based on level). Full defaults to True."
        },
        "include_notifications": {
          "type": "bool | None",
          "default": true,
          "description": "Include active persistent notifications."
        },
        "include_dismissed_repairs": {
          "type": "bool | None",
          "default": false,
          "description": "Include user-dismissed/ignored repairs."
        },
        "fields": {
          "type": "str | list[str] | None",
          "default": null,
          "description": "Return only the specified top-level response keys to reduce response size (e.g. [\"system_info\", \"domain_stats\"]). None = full response. Available keys: success, system_summary, domain_stats, area_analysis, ai_insights, pagination, partial, warnings, device_types, service_availability, system_info, notification_count, notifications, repair_count, dismissed_repair_count, repairs, repairs_error, tool_discovery, settings_url, settings_url_hint, read_only_mode, read_only_mode_hint, ha_mcp_update. Note: ``settings_url`` (stdio mode), ``settings_url_hint`` (standalone HTTP/Docker mode), the ``read_only_mode`` / ``read_only_mode_hint`` pair (only while Read Only Mode is on), and ``ha_mcp_update`` (when an update check applies) are emitted regardless of ``fields=`` projection."
        }
      }
    },
    "annotations": {
      "openWorldHint": true,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Search & Discovery"
    ],
    "source_file": "tools_search.py"
  },
  {
    "name": "ha_get_state",
    "title": "Get Entity State",
    "description": "Get current status, state, and attributes of one or more entities (lights, switches, sensors, climate, covers, locks, fans, etc.).\n\nPass a string entity_id for one entity, or a list (max 100, duplicates\ndeduplicated) for several, fetched in parallel. A bulk call returns\nsuccess=True if at least one state was retrieved; check 'error_count'\nfor failed lookups.\n\n`fields=` projects the per-entity record keys, NOT the outer bulk\nresponse wrapper: in single-entity mode it filters the returned record;\nin bulk mode it filters each record inside `states[entity_id]` while\nouter keys (`success`, `count`, `states`, `errors`, ...) are always\npreserved. `attribute_keys=` further narrows the `attributes` sub-dict\nand is only applied when `\"attributes\"` is in `fields=` (or\n`fields=None`); otherwise it is a no-op and a `warnings` list is emitted\noutside the projected record(s) — at the response wrapper level in bulk\nmode, at the top-level result (sibling of `data`/`metadata`) in\nsingle-entity mode — so `fields=[\"state\"]` still returns a record with\nonly `state`.\n\nEXAMPLES:\n- Single: ha_get_state(\"light.kitchen\")\n- Multiple: ha_get_state([\"light.kitchen\", \"light.living_room\", \"sensor.temperature\"])\n- Slim bulk: ha_get_state([\"light.kitchen\", \"sensor.temperature\"], fields=[\"state\", \"attributes\"], attribute_keys=[\"brightness\"])",
    "inputSchema": {
      "properties": {
        "entity_id": {
          "type": "str | list[str]",
          "description": "Entity ID or list of entity IDs to retrieve state for (e.g., 'light.kitchen' or ['light.kitchen', 'sensor.temperature'])"
        },
        "fields": {
          "type": "str | list[str] | None",
          "default": null,
          "description": "Return only the specified top-level entity record keys to reduce response size (e.g. [\"state\", \"attributes\"]). None = full entity record. Available keys: entity_id, state, attributes, last_changed, last_reported, last_updated, context."
        },
        "attribute_keys": {
          "type": "str | list[str] | None",
          "default": null,
          "description": "Return only the specified keys from each entity's attributes dict (e.g. [\"brightness\", \"color_temp_kelvin\"] for lights). None = full attributes. Unknown keys are silently dropped."
        }
      },
      "required": [
        "entity_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Search & Discovery"
    ],
    "source_file": "tools_search.py"
  },
  {
    "name": "ha_search",
    "title": "Search",
    "description": "Search for entities (lights, sensors, switches, climate, etc.) by name, domain, or area — AND inside automation/script/scene/helper/dashboard configurations — in one call.\n\nTwo surfaces run in parallel and return tagged results:\n  - **entities**: entity-registry matches (entity_id, friendly name,\n    area). Filter with `domain_filter`/`area_filter`/`state_filter`.\n  - **automations / scripts / scenes / helpers / dashboards**: matches\n    *inside* config definitions — triggers, actions, sequences, scene\n    entity-sets, helper bodies, dashboard cards. Driven by `query`;\n    narrow with `search_types`.\n\nUse dedicated get/list tools first for a known resource type, including\nha_config_get_scene for scene listing and content search. Use this for\nbroader discovery or deep searches across resource types.\n\nFor control requests with exclusions such as \"except\", \"excluding\", or\n\"but not\", include `is_group` and `member_entity_ids` in `result_fields`.\nDo not control an aggregate whose members include an excluded entity;\nprefer leaf entities when the exception cannot be verified safely.\nA withheld member list still returns is_group=true; absence of\nmember_entity_ids must not be interpreted as a leaf entity.\n\nWhen NOT to use:\n  - To read a known entity_id's state: use `ha_get_state` (cheaper).\n  - To inspect one automation/script/scene config by id: use the\n    matching `ha_config_get_*`.\n  - To list installed Apps (add-ons): use `ha_get_app`.\n\nConfig-body search is skipped when `domain_filter`/`area_filter`/\n`state_filter` signal entity-only intent (keeping name lookups off the\nexpensive backend); a `warnings[]` entry names the skip. Repeat without\nentity filters to search configuration contents too.\n\nCaveats:\n  - `partial: True` means results are NOT exhaustive — a surface raised,\n    or the config-body branch lost data (per-id time budget exhausted,\n    an individual fetch failed, or a helper-type list fetch failed).\n    Empty buckets with `partial: True` mean \"search failed\", not \"no\n    results\". The cause is in `partial_reason`, also mirrored into\n    `warnings[]` with an \"incomplete results: \" prefix. Do not treat a\n    partial response as complete.\n  - `count` is items in this response (post-pagination), not corpus\n    totals — use `entity_total_matches` + `config_total_matches`.\n  - `limit`/`offset` apply per-surface. Flat `has_more`/`next_offset`\n    page the next call (iterate `offset = next_offset`); per-surface\n    `entity_*`/`config_*` variants show which surface still has results.\n\nExamples:\n    - Find a light by name: ha_search(\"kitchen\", domain_filter=\"light\")\n    - List sensors in an area: ha_search(domain_filter=\"sensor\", area_filter=\"Living Room\")\n    - Find lights safely before an \"all except one\" control request:\n      ha_search(\"living room\", domain_filter=\"light\",\n      result_fields=[\"entity_id\", \"friendly_name\", \"is_group\",\n      \"member_entity_ids\"])\n    - Which automations use an entity: ha_search(\"light.bed_light\")\n    - All unavailable entities: ha_search(state_filter=\"unavailable\")",
    "inputSchema": {
      "properties": {
        "query": {
          "type": "str | None",
          "default": null,
          "description": "What to search for (entity name fragment, free-text config term, entity_id). Pass the exact entity_id, not a name fragment, when checking what a rename or delete would break: that form reports automations, scripts and scenes referencing it even when their configuration could not be read. Omit `query` to enumerate by `domain_filter`, `area_filter`, and/or `state_filter` alone (registry-listing mode); configuration-body search is skipped in that mode because there is no term to match against."
        },
        "domain_filter": {
          "type": "str | None",
          "default": null,
          "description": "Narrow entity-registry results to a single domain (e.g. 'light', 'sensor')."
        },
        "area_filter": {
          "type": "str | None",
          "default": null,
          "description": "Narrow entity-registry results to an area (id, name, or alias), an exact floor (id, name, or alias), or an unambiguous close-spelling floor match; a floor match expands to all areas on that floor."
        },
        "search_types": {
          "type": "str | list[str] | None",
          "default": null,
          "description": "Configuration types to include in body search: 'automation', 'script', 'scene', 'helper', 'dashboard'. Explicitly providing this selects configuration-only search and skips entities. Omit it for entity discovery. Default = automation+script+scene+helper."
        },
        "limit": {
          "type": "int",
          "default": 10,
          "constraints": {
            "ge": 1
          },
          "description": "Maximum results per surface (entities, configs)."
        },
        "offset": {
          "type": "int",
          "default": 0,
          "constraints": {
            "ge": 0
          },
          "description": "Number of results to skip for pagination."
        },
        "exact_match": {
          "type": "bool",
          "default": true,
          "description": "Exact substring matching. Set False for fuzzy matching when the query may have typos."
        },
        "include_hidden": {
          "type": "bool",
          "default": true,
          "description": "Include hidden entities in registry results (with a score penalty so they sort below visible matches). Set False to exclude entirely."
        },
        "include_config": {
          "type": "bool",
          "default": false,
          "description": "Include full configuration bodies in body-search results. Otherwise summaries only."
        },
        "group_by_domain": {
          "type": "bool",
          "default": false,
          "description": "Group entity-registry results by domain (entity-side only). Adds a `by_domain` map to the response."
        },
        "per_domain_limit": {
          "type": "int | None",
          "default": null,
          "description": "When `group_by_domain=True`, cap entity-registry results per domain to this number. Ignored otherwise."
        },
        "state_filter": {
          "type": "str | None",
          "default": null,
          "description": "Filter entity-registry results to a specific state (e.g. \"on\", \"off\", \"unavailable\"). Case-insensitive. Can be used standalone (no query/domain/area) to enumerate every entity in that state; entity_total_matches reflects the filtered count."
        },
        "result_fields": {
          "type": "str | list[str] | None",
          "default": null,
          "description": "Project each entity-registry record to only the specified keys (e.g. [\"entity_id\", \"state\"]). None = full records. Base keys: entity_id, friendly_name, domain, state, score, match_type. Opt-in enrichment/membership keys (computed on request): area, floor, labels, aliases, is_group, member_entity_ids. Membership is recognized only when HA explicitly exposes a valid group_entities or legacy entity_id collection; member IDs are sorted, direct (not recursively expanded), and omitted if visibility/include_hidden excludes a member. Requesting member_entity_ids also retains is_group. An unknown key is rejected."
        },
        "fields": {
          "type": "str | list[str] | None",
          "default": null,
          "description": "Project the response to the named top-level keys (e.g. [\"entities\", \"automations\"]); None = full response. Diagnostic / pagination keys are always retained so projection cannot hide partial / error state. Distinct from `result_fields` (which projects each entity record's keys). Available keys: success, query, entities, automations, scripts, scenes, helpers, dashboards, search_types, search_type, entity_total_matches, config_total_matches, count, offset, limit, has_more, next_offset, entity_has_more, entity_next_offset, config_has_more, config_next_offset, by_domain, state_filter_note, area_names, domain_filter, area_filter, message, warnings, errors, partial, partial_reason."
        },
        "config_time_budget": {
          "type": "float | None",
          "default": null,
          "constraints": {
            "ge": 0.001,
            "le": 300
          },
          "description": "Per-call override for the per-id config-fetch wall-clock budget (seconds). Replaces the per-type HAMCP_*_CONFIG_TIME_BUDGET defaults for the automation, script, AND scene branches. Use when a `partial: True` response names time-budget skipping. None = use the per-type env defaults."
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Search & Discovery"
    ],
    "source_file": "tools_search.py"
  },
  {
    "name": "ha_bulk_control",
    "title": "Bulk Control",
    "description": "Manage explicit operations or one deterministic structural bulk action.\n\nWhen NOT to use: use ``ha_call_service`` for service-specific payloads or\nbackend-native group targeting, and ``ha_search`` for fuzzy name discovery.\n\n**Operations mode** (``operations``, no ``selector``): put every target in\nthis one call. Parallel execution is the default, and invalid items are\nreported without aborting valid operations in the same batch — but a batch\nin which every item fails validation dispatches nothing and fails the call.\nA batch that targets a group/aggregate entity together with one or more of\nits own individual members also fails closed (nothing dispatched): Home\nAssistant applies the action to every member when the group is targeted\nregardless of what else is listed, so a member row cannot exclude that\nmember from the group's own action. Use selector mode with\n``exclude_entity_ids`` when a group action must exclude specific members.\n\n**Selector mode** (``selector`` + ``action``): use exact area or floor IDs\nwhen exclusions must be applied after recursively expanding generic\naggregate membership. Resolves a frozen visible leaf set before dispatch;\nit is not transactional, so Home Assistant may still report per-leaf\nfailures. A selector resolving to more than 100 entities fails closed\ninstead of dispatching a partial/oversized batch — narrow it (a more\nspecific area/floor, or add ``exclude_entity_ids``) and retry. Set\n``dry_run`` to preview the resolved set without changing state.",
    "inputSchema": {
      "properties": {
        "operations": {
          "type": "list[SkipValidation[BulkControlOperation]]",
          "description": "Explicit entity operations. Each item requires exact entity_id and action. Use action='off', not service='turn_off'."
        },
        "parallel": {
          "type": "bool",
          "default": true
        },
        "selector": {
          "type": "SkipValidation[BulkControlSelector] | None",
          "description": "Optional exact structural scope using domain plus area_ids and/or floor_ids, with optional exclude_entity_ids.",
          "default": null
        },
        "action": {
          "type": "str | None",
          "description": "One device action applied to every resolved leaf.",
          "default": null
        },
        "parameters": {
          "type": "dict[str, Any] | None",
          "description": "Optional action parameters for selector mode.",
          "default": null
        },
        "timeout_seconds": {
          "type": "float | None",
          "constraints": {
            "ge": 0,
            "le": 60,
            "allow_inf_nan": false,
            "strict": true
          },
          "default": null
        },
        "validate_first": {
          "type": "bool",
          "constraints": {
            "strict": true
          },
          "default": true
        },
        "dry_run": {
          "type": "bool",
          "constraints": {
            "strict": true
          },
          "default": false
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Service & Device Control"
    ],
    "source_file": "tools_service.py"
  },
  {
    "name": "ha_call_event",
    "title": "Call Event",
    "description": "Execute a custom event on the Home Assistant event bus.\n\nWhen NOT to use: for controlling entities (lights, switches, climate) — use\nha_call_service instead. For triggering automations by name, use\nha_call_service(\"automation\", \"trigger\").\n\nUse this to publish custom event types consumed by event-triggered automations,\nNode-RED flows, or custom integrations that subscribe to specific event types.\n\nCaveats: Events are fire-and-forget; this tool confirms the event was accepted\nby the bus but does not verify whether any automation or subscriber acted on it.",
    "inputSchema": {
      "properties": {
        "event_type": {
          "type": "str"
        },
        "data": {
          "type": "dict[str, Any] | None",
          "default": null
        }
      },
      "required": [
        "event_type"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": false
    },
    "tags": [
      "Service & Device Control"
    ],
    "source_file": "tools_service.py"
  },
  {
    "name": "ha_call_service",
    "title": "Call Service",
    "description": "Execute Home Assistant services to control entities and trigger automations.\n\nThis is the universal tool for controlling all Home Assistant entities. Services follow\nthe pattern domain.service (e.g., light.turn_on, climate.set_temperature).\n\nEXAMPLES:\n- ha_call_service(\"light\", \"turn_on\", entity_id=\"light.living_room\")\n- ha_call_service(\"climate\", \"set_temperature\", entity_id=\"climate.thermostat\", data={\"temperature\": 22})\n- ha_call_service(\"automation\", \"trigger\", entity_id=\"automation.morning_routine\")\n\nResult compaction (default ON): ``result`` is trimmed to the targeted\nentity's record (drops parent-group propagation) and stripped of\n``context`` / ``last_*`` metadata and heavy attribute lists\n(``effect_list``, ``hue_scenes``).\n\nFor detailed service documentation, use ha_get_skill_guide.\n\n**WebSocket command escape hatch (advanced):**\nA few Home Assistant operations are WebSocket-only commands, not\nregistered services — most notably dismissing a Repairs issue. Pass\n``ws_command`` (instead of domain/service) to send one, with its\nparameters in ``data``:\nha_call_service(ws_command=\"repairs/ignore_issue\", data={\"domain\": \"sun\", \"issue_id\": \"abc\", \"ignore\": True})\n(get domain/issue_id from ha_get_overview repairs or ha_get_system_health\ninclude=\"repairs\"). Only one-shot request/response commands are\nsupported, and the other service parameters (entity_id,\nreturn_response, etc.) don't apply.\n\nUnavailable in Read Only Mode, including read-like services and WebSocket\ncommands. Use dedicated read tools while that mode is enabled.",
    "inputSchema": {
      "properties": {
        "domain": {
          "type": "str | None",
          "description": "Service domain (e.g. 'light', 'climate', 'automation'). Required for a service call.",
          "default": null
        },
        "service": {
          "type": "str | None",
          "description": "Service name within domain (e.g. 'turn_on', 'set_temperature', 'trigger'). Required for a service call.",
          "default": null
        },
        "entity_id": {
          "type": "str | None",
          "description": "Entity ID(s) the service call targets — one ID ('light.living_room') or several comma-separated ('light.a,light.b'). Optional for services that don't target a specific entity.",
          "default": null
        },
        "data": {
          "type": "dict[str, Any] | None",
          "description": "Extra service-call parameters beyond entity_id (e.g. {'temperature': 22} for climate.set_temperature). Also carries the raw command payload when ws_command is set. If entity_id is also present in data, the entity_id parameter wins.",
          "default": null
        },
        "return_response": {
          "type": "bool",
          "description": "If True, the service's response data is returned once, as the top-level 'service_response' key — never nested inside 'result'.",
          "default": false
        },
        "wait": {
          "type": "bool",
          "description": "If True (default), wait for the entity state to change before returning. Applies only to state-changing services called with a single entity_id. A comma-separated multi-target does not get confirmed by this: it falls through to a legacy path that polls for the literal composite entity_id and times out after 10s. Set wait=False for multi-target calls.",
          "default": true
        },
        "verbose": {
          "type": "bool",
          "description": "Return HA's raw changed-state records unchanged. With return_response=True the response data still surfaces once as the top-level service_response key, never nested in result. Large for nested-group targets — prefer result_fields / result_attribute_keys.",
          "default": false
        },
        "result_fields": {
          "type": "str | list[str] | None",
          "default": null,
          "description": "Project each record in 'result' to only these top-level keys (e.g. ['entity_id', 'state']). Setting this DISABLES default compaction — no entity-id filter, no metadata strip — and applies the explicit projection instead."
        },
        "result_attribute_keys": {
          "type": "str | list[str] | None",
          "default": null,
          "description": "Project each record's 'attributes' dict to only these keys (e.g. ['brightness', 'rgb_color']). Setting this DISABLES default compaction. Requires 'attributes' to be present in result_fields (or result_fields=None)."
        },
        "ws_command": {
          "type": "str | None",
          "default": null,
          "description": "Advanced escape hatch: send a raw one-shot Home Assistant WebSocket command that is NOT a registered service (e.g. 'repairs/ignore_issue' to dismiss a Repairs issue). When set, omit domain/service and the other service params. Streaming/two-phase and service-invoking commands (call_service, execute_script) are rejected."
        }
      }
    },
    "annotations": {
      "readOnlyHint": false,
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Service & Device Control"
    ],
    "source_file": "tools_service.py"
  },
  {
    "name": "ha_get_operation_status",
    "title": "Get Operation Status",
    "description": "Get the status of one or more device operations with real-time WebSocket verification.\n\nThe timeout_seconds wait window bounds single and bulk checks. Bulk checks poll\nall operations concurrently under one shared window and report\nper-item failures inside detailed_results instead of aborting the\nbatch.\n\nUse this to track operations initiated by ha_bulk_control or ha_call_service.\nFor current entity states, use ha_get_state instead.",
    "inputSchema": {
      "properties": {
        "operation_id": {
          "type": "str | list[str]",
          "description": "Single operation ID, or a list of IDs for a bulk status check."
        },
        "timeout_seconds": {
          "type": "float",
          "constraints": {
            "ge": 0,
            "allow_inf_nan": false
          },
          "default": 10
        }
      },
      "required": [
        "operation_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "readOnlyHint": true
    },
    "tags": [
      "Service & Device Control"
    ],
    "source_file": "tools_service.py"
  },
  {
    "name": "ha_list_services",
    "title": "List Available Services",
    "description": "List available Home Assistant services with optional pagination and detail control.\n\nDiscovers services/actions that can be called via ha_call_service.\n\nEXAMPLES:\n- Light services with full parameter details: ha_list_services(domain=\"light\", detail_level=\"full\")\n- Search: ha_list_services(query=\"temperature\")\n- Next page: ha_list_services(offset=50)",
    "inputSchema": {
      "properties": {
        "domain": {
          "type": "str | None",
          "description": "Filter by domain (e.g., 'light', 'switch', 'climate').",
          "default": null
        },
        "query": {
          "type": "str | None",
          "description": "Search in service names and descriptions.",
          "default": null
        },
        "limit": {
          "type": "int",
          "default": 50,
          "constraints": {
            "ge": 1,
            "le": 200
          },
          "description": "Max services to return per page"
        },
        "offset": {
          "type": "int",
          "default": 0,
          "constraints": {
            "ge": 0
          },
          "description": "Number of services to skip for pagination"
        },
        "detail_level": {
          "type": "Literal['summary', 'full']",
          "default": "summary",
          "description": "'summary': name, description, domain, service, and target when the service has one. 'full': additionally include parameter field schemas."
        },
        "service_fields": {
          "type": "str | list[str] | None",
          "default": null,
          "description": "Project each service record to only the specified keys. E.g. [\"name\", \"description\"] returns slim service records. None = full records. Unknown keys yield empty records. Available keys: name, description, domain, service, target (when present), fields (full mode only)."
        },
        "fields": {
          "type": "str | list[str] | None",
          "default": null,
          "description": "Return only the specified top-level response keys to reduce response size (e.g. [\"services\"]). None = full response. Available keys: success, domains, services, total_count, count, offset, limit, has_more, next_offset, detail_level, filters_applied."
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Service & Device Control"
    ],
    "source_file": "tools_services.py"
  },
  {
    "name": "ha_config_get_yaml",
    "title": "Read YAML Config Fragment",
    "description": "Get the current YAML fragment under a key, from one config file or across a glob.\n\nUse before ha_config_set_yaml to inspect what a key currently holds, and\nto find which file defines it — the returned ``file`` + ``yaml_path`` are\nexactly the arguments that address the same fragment for an edit.\n\nNot for storage-mode items: automations, scripts and scenes created\nthrough the UI live outside YAML — read those with ha_config_get_automation,\nha_config_get_script and ha_config_get_scene. This tool sees only what is\nwritten in the config files themselves.\n\nReads the file, so it reflects what is on disk rather than what Home\nAssistant currently has loaded; a fragment edited but not yet reloaded\ndiffers from the running config. Comments and HA tags survive as written\nand a ``!secret`` is never resolved to its value, so ``content`` can be\nhanded back to ha_config_set_yaml unchanged. Files outside the read\nallowlist stay unreadable, and secrets.yaml reads back with its values\nmasked.",
    "inputSchema": {
      "properties": {
        "yaml_path": {
          "type": "str",
          "description": "Dotted YAML key path to look up, e.g. 'alert2', 'mqtt', 'template'. Any key is readable — unlike ha_config_set_yaml, which only writes an allowlisted set."
        },
        "file": {
          "type": "str",
          "default": "configuration.yaml",
          "description": "Config-relative file to read. Accepts an fnmatch glob to search several files at once — 'packages/*.yaml' matches one directory level, not a nested tree."
        },
        "include_content": {
          "type": "bool",
          "default": true,
          "description": "Return the round-trip YAML text of each match. Set False to discover only which files define the key."
        },
        "include_parsed": {
          "type": "bool",
          "default": false,
          "description": "Additionally return each match as structured data. HA tags stay in source form ('!secret api_key'), never resolved."
        }
      },
      "required": [
        "yaml_path"
      ]
    },
    "annotations": {
      "readOnlyHint": true,
      "openWorldHint": false
    },
    "tags": [
      "System",
      "beta"
    ],
    "source_file": "tools_yaml_read.py"
  },
  {
    "name": "ha_config_set_yaml",
    "title": "Raw YAML Config Edit",
    "description": "Update raw YAML configuration in configuration.yaml, packages/*.yaml, or themes/*.yaml (LAST RESORT).\n\nMUST call ha_get_skill_guide OR refer to your locally installed skills first.\n``template-guidelines.md`` ships under ``skill_content`` by default.\n\nDestructive, disabled by default. Prefer the dedicated tools:\nha_config_set_helper for template sensors and every other helper type,\nha_config_set_automation / ha_config_set_script / ha_config_set_scene\nfor storage-mode items.\n\nIntended for YAML-only integrations with no config-flow or API\nequivalent (command_line, rest, shell_command, notify platforms),\nfor integrations with significant YAML-only configuration (knx\nentities in package files), for registering YAML-mode dashboards via\n``lovelace.dashboards.<url_path>`` (no other ``lovelace.*`` keys),\nand for editing theme files in ``themes/*.yaml``. Themes only load when\nconfiguration.yaml carries the ``frontend: themes:`` include (e.g.\n``!include_dir_merge_named themes``); this tool cannot add that include\n(``frontend`` is not an allowed key). ``automation``, ``script`` and\n``scene`` keys are accepted only when ``file`` is a ``packages/*.yaml``;\nwrites of those keys to ``configuration.yaml`` are rejected — use the\nstorage-mode tools.\n\nCheck ``post_action`` in the response: most keys need a full HA\nrestart. For ``themes/*.yaml`` this tool *performs* the reload itself\n(``frontend.reload_themes``), so ``post_action`` is ``reload_performed``\n(or ``reload_available`` plus ``reload_error`` if that reload failed).\nFor template, mqtt, group, automation, script, and scene it only\n*advertises* the reload service (``post_action: reload_available`` with\na ``reload_service`` to call yourself); the edit is on disk but not yet\nlive. Preserves YAML comments and HA tags (``!include``,\n``!secret``) on round-trip; ``replace`` swaps the subtree as-is.\n\nTwo-step confirmation (default ON, toggle ENABLE_YAML_EDIT_CONFIRM /\nServer Settings): the first call returns ``preview: true`` with a\nunified ``diff`` of exactly what would change on disk and a\n``confirm_token`` — nothing is written. Review the diff, then repeat\nthe identical call adding ``confirm_token`` to apply. Every applied\nwrite also returns the final ``diff``. A token mismatch means the file\nchanged since the preview (or the token was wrong); use the freshly\nreturned token.",
    "inputSchema": {
      "properties": {
        "yaml_path": {
          "type": "str",
          "description": "Top-level YAML key to modify. Only a narrow allowlist of YAML-only or YAML-heavy integration keys is accepted (e.g., 'command_line', 'rest', 'shell_command', 'notify', 'knx'). For YAML-mode dashboards, use the dotted form 'lovelace.dashboards.<url_path>' where <url_path> is lowercase, hyphenated, and not a reserved HA route. For themes in themes/*.yaml, use the theme name (simple name without dots; content is the mapping of theme variables only, without the theme name)."
        },
        "action": {
          "type": "str",
          "description": "Action to perform: 'add' (insert/merge content under key), 'replace' (overwrite key with new content), or 'remove' (delete the key entirely)."
        },
        "content": {
          "type": "str | None",
          "default": null,
          "description": "YAML content for the value under yaml_path. Required for 'add' and 'replace' actions."
        },
        "file": {
          "type": "str",
          "default": "configuration.yaml",
          "description": "Relative path to the YAML config file. Also supports 'packages/*.yaml' and 'themes/*.yaml'."
        },
        "confirm_token": {
          "type": "str | None",
          "default": null,
          "description": "Confirmation token from a prior preview response."
        },
        "MandatoryBPS": {
          "type": "bool",
          "default": true
        },
        "BestPracticeKey": {
          "type": "BestPracticeKeyParam",
          "default": null
        }
      },
      "required": [
        "yaml_path",
        "action"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": false
    },
    "tags": [
      "System",
      "beta"
    ],
    "source_file": "tools_yaml_config.py"
  },
  {
    "name": "ha_manage_backup",
    "title": "Manage Backups",
    "description": "Polymorphic backup tool. See the tool description for the routing matrix.",
    "inputSchema": {
      "properties": {
        "scope": {
          "type": "Literal['snapshot', 'edits']",
          "description": "'snapshot' for full HA tarballs; 'edits' for per-entity auto-backups."
        },
        "action": {
          "type": "Literal['create', 'restore', 'list', 'view', 'diff', 'delete']",
          "description": "Operation to perform. Valid (scope, action) combinations are listed in the tool description."
        },
        "name": {
          "type": "str | None",
          "default": null,
          "description": "(snapshot.create) Tarball name. Auto-generated if not provided."
        },
        "backup_id": {
          "type": "str | None",
          "default": null,
          "description": "(snapshot.restore / snapshot.delete) Tarball ID (e.g. 'dd7550ed')."
        },
        "restore_database": {
          "type": "bool",
          "default": false,
          "description": "(snapshot.restore) Include the database in the restore; otherwise config only."
        },
        "confirm": {
          "type": "bool",
          "default": false,
          "description": "(snapshot.delete) Must be True to confirm deletion."
        },
        "domain": {
          "type": "str | None",
          "default": null,
          "description": "(edits.create / edits.list / edits.delete) Filter auto-backups by domain (e.g. 'automation', 'helper_timer'). Required for edits.create."
        },
        "entity_id": {
          "type": "str | None",
          "default": null,
          "description": "(edits.create / edits.list / edits.delete) Filter auto-backups by entity ID. Required for edits.create."
        },
        "backup_name": {
          "type": "str | None",
          "default": null,
          "description": "(edits.view / edits.restore / edits.delete) Auto-backup filename (format '<domain>.<entity_id>.<timestamp>[_NN].yaml'). Not a tarball ID."
        },
        "older_than_days": {
          "type": "int | None",
          "default": null,
          "constraints": {
            "ge": 0
          },
          "description": "(edits.delete) Bulk-delete auto-backups older than this many days."
        },
        "limit": {
          "type": "int",
          "default": 200,
          "constraints": {
            "ge": 1,
            "le": 10000
          },
          "description": "(edits.list / snapshot.list) Maximum number of entries to return."
        }
      },
      "required": [
        "scope",
        "action"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "System"
    ],
    "source_file": "backup.py"
  },
  {
    "name": "ha_manage_custom_tool",
    "title": "Custom Tool",
    "description": "Create and run a custom tool in a sandbox, or manage saved custom tools.\n\nLAST RESORT — search for existing tools first.\n\n**Modes** (mutually exclusive): ``code`` + ``justification``;\n``run_saved``; ``list_saved=True``.\n\n**Available functions in sandbox:**\n- ``api_get(endpoint)`` / ``api_post(endpoint, data)`` — HA REST API,\n  for operations not covered by existing tools\n- ``ws_send(message)`` — send a HA WebSocket command, for operations\n  only available over WebSocket (most registry CRUD, template rendering,\n  Lovelace operations). ``message`` must include a ``\"type\"`` field; the\n  MCP server adds ``id`` and handles auth.\n- ``call_tool(name, args)`` — call a registered MCP tool\n- ``delete_saved_tool(name)`` — remove a previously saved custom tool.\n  Returns ``{\"deleted\": True, \"name\": name}`` or ``{\"error\": ...}``.\n\nSaved tools persist across server restarts when\n``CODE_MODE_SAVED_TOOLS_PATH`` is set (the app sets this by default to\n``/data/saved_tools.json``).\n\nExample — list areas via WebSocket:\n```python\nresult = await ws_send({\"type\": \"config/area_registry/list\"})\nresult.get(\"result\", [])\n```\n\nExample — chain existing tools:\n```python\nresult = await call_tool(\"ha_search\", {\"query\": \"light\", \"limit\": 5})\nlights = result.get(\"entities\", [])\nfor e in lights:\n    await call_tool(\"ha_call_service\", {\"domain\": \"light\", \"service\": \"turn_off\", \"entity_id\": e[\"entity_id\"]})\n{\"turned_off\": len(lights)}\n```",
    "inputSchema": {
      "properties": {
        "code": {
          "type": "str | None",
          "description": "Python code to execute.  Last expression is the return value.",
          "default": null
        },
        "justification": {
          "type": "str | None",
          "description": "Why no existing tool works.",
          "default": null
        },
        "save_as": {
          "type": "str | None",
          "description": "Save the tool under this name for reuse (alphanumeric/underscores, max 64 chars).",
          "default": null
        },
        "run_saved": {
          "type": "str | None",
          "description": "Name of a previously saved tool to re-run.",
          "default": null
        },
        "list_saved": {
          "type": "bool",
          "description": "Set True to list all saved tools.",
          "default": false
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": false,
      "readOnlyHint": false
    },
    "tags": [
      "System",
      "beta"
    ],
    "source_file": "tools_code.py"
  },
  {
    "name": "ha_manage_security_policy",
    "title": "Manage Security Policy",
    "description": "Manage the tool security policy that gates high-stakes tool calls behind user approval.\n\nWhen NOT to use: this tool cannot see or decide pending approval\nrequests — listing, approving, and denying them is developer-mode\nonly (ha_dev_manage_server). For ordinary Home Assistant entity,\nautomation, or dashboard work use the ha_config_* tools.\n\nWhen to use: to read the current policy, to add or remove per-tool\napproval rules and their matching conditions, and to change the\napproval wait time or how long an approval is remembered.\n\nCaveats: set replaces the WHOLE document, so send back an edited\ncopy of what get returned, not a fragment. An omitted field is not\nleft alone, it reverts to its default: dropping\nevent_decisions_enabled switches off approving from Home Assistant\nevents, and the response says so in a warning when it does. Writes are\nversion-guarded: a concurrent edit is rejected instead of\nsilently overwritten. Rule edits apply to the running server\nimmediately and can remove approval gates. Whether this tool itself\nis registered is a separate setting that takes effect on restart.\n\nEXAMPLES:\nha_manage_security_policy(\"get\")\nha_manage_security_policy(\"set\", policy={\"wait_seconds\": 60, \"approval_ttl_minutes\": 5, \"event_decisions_enabled\": False, \"rules\": [{\"tool_name\": \"ha_call_service\"}], \"version\": 3})",
    "inputSchema": {
      "properties": {
        "action": {
          "type": "Literal['get', 'set']",
          "description": "get: read the whole policy document. set: replace it with the supplied one."
        },
        "policy": {
          "type": "dict[str, Any] | None",
          "default": null,
          "description": "set: the full policy object {wait_seconds, approval_ttl_minutes, event_decisions_enabled, rules, version}"
        },
        "expected_version": {
          "type": "int | None",
          "default": null,
          "description": "set: the version from your last get, for optimistic-concurrency safety (else the policy's own version field is used)"
        }
      },
      "required": [
        "action"
      ]
    },
    "annotations": {
      "readOnlyHint": false,
      "destructiveHint": true,
      "idempotentHint": false,
      "openWorldHint": false
    },
    "tags": [
      "System"
    ],
    "source_file": "tools_security_policy.py"
  },
  {
    "name": "ha_manage_theme",
    "title": "Manage Frontend Themes",
    "description": "Manage Home Assistant frontend themes.\n\nWhen NOT to use: themes are YAML files - Home Assistant has no API to\ncreate or edit them. Installing community themes goes through HACS\n(ha_manage_hacs); editing custom theme files goes through\nha_config_set_yaml (beta, edits themes/<name>.yaml keyed by theme name\nand attempts an automatic theme reload).\n\nWhen to use: action='list' discovers installed theme names and the\ncurrent defaults; action='set' selects the backend default theme\n(optionally per light/dark mode).\n\nSCREENSHOT-ENGINE ACTIONS (per-user, not the backend default):\nTaking a dashboard screenshot makes the Puppet engine write the saved\ntheme of the Home Assistant user its token belongs to, which also\nflips that user's live web and mobile sessions. The screenshot tools\nare read-only and only *report* this; use action='set_engine_theme'\nwith the value quoted in their warning to put it back, and\naction='get_engine_theme' to inspect it. These act on that engine\naccount's per-user profile via frontend/set_user_data, which is a\ndifferent layer from the backend default that action='set' changes.\nGiving the engine its own dedicated user and token avoids the issue\nentirely.\n\nCaveats: action='set' changes the backend-selected default only -\nusers who explicitly picked a theme in their profile keep their\nchoice. Theme names are validated by Home Assistant at call time.\n\nEXAMPLES:\n- List themes: ha_manage_theme(action=\"list\")\n- Set default theme: ha_manage_theme(action=\"set\", theme_name=\"nord\")\n- Set dark-mode theme: ha_manage_theme(\n      action=\"set\", theme_name=\"nord\", mode=\"dark\")\n- Restore built-in default: ha_manage_theme(\n      action=\"set\", theme_name=\"default\")\n- Inspect the engine account's theme: ha_manage_theme(\n      action=\"get_engine_theme\")\n- Undo a screenshot's theme change (pass BOTH values from the\n  warning, so a theme changed since then is not overwritten):\n  ha_manage_theme(action=\"set_engine_theme\",\n      value={\"theme\": \"\", \"dark\": False},\n      expected_current={\"theme\": \"default\", \"dark\": True})",
    "inputSchema": {
      "properties": {
        "action": {
          "type": "ThemeAction",
          "description": "Theme operation: 'list' installed themes, 'set' the backend default theme, or read/restore the screenshot engine account's own per-user theme with 'get_engine_theme' / 'set_engine_theme'."
        },
        "theme_name": {
          "type": "str | None",
          "description": "Theme name when action='set'. Must be an installed theme; 'default' restores the built-in theme, 'none' resets the chosen mode to the built-in default.",
          "default": null
        },
        "expected_current": {
          "type": "dict[str, Any] | None",
          "description": "Guard for action='set_engine_theme': the stored theme is read immediately before the write and the write is skipped if it no longer equals this. Omitting this value or passing null both mean 'expect no stored theme', enforced like any other value; the guard is always applied unless force is set. Best-effort, not atomic: a change landing between that read and the write is not caught.",
          "default": null
        },
        "force": {
          "type": "bool",
          "description": "action='set_engine_theme' only: skip the expected_current guard and overwrite unconditionally.",
          "default": false
        },
        "value": {
          "type": "dict[str, Any] | None",
          "description": "Frontend user-data theme object when action='set_engine_theme', e.g. {'theme': '', 'dark': False}. An empty dict restores default/auto behavior.",
          "default": null
        },
        "mode": {
          "type": "Literal['light', 'dark'] | None",
          "description": "Which mode the theme applies to when action='set'. Defaults to light.",
          "default": null
        }
      },
      "required": [
        "action"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": true,
      "readOnlyHint": false
    },
    "tags": [
      "System"
    ],
    "source_file": "tools_themes.py"
  },
  {
    "name": "ha_manage_updates",
    "title": "Manage Updates",
    "description": "Manage Home Assistant updates -- list, read details, batch install, skip, or un-skip.\n\nCovers Core, OS, supervisor, apps (add-ons), device firmware, and HACS\nupdate entities. In Read Only Mode the read actions ('list', 'get') stay\navailable; write actions are blocked.\n\nInstalls run asynchronously in Home Assistant and can take minutes:\n'install' returns once the service calls are accepted, with per-entity\nresults. Poll action='list' to watch in_progress until installed_version\nreaches latest_version. action='list' also returns ha_mcp_update — this\nMCP server's own update status {current, latest, update_available}, so\na newer ha-mcp release can be flagged.\n\nEXAMPLES:\n- Pre-update analysis: ha_manage_updates(action=\"get\", entity_ids=[\"update.home_assistant_core_update\"], include_release_notes=True)\n- Update everything pending in a category: ha_manage_updates(action=\"install\", categories=[\"addons\", \"hacs\"])",
    "inputSchema": {
      "properties": {
        "action": {
          "type": "str",
          "description": "'list' (all pending updates, default), 'get' (details/release notes for one update), 'install' (apply pending updates), 'skip' (hide the offered version), or 'clear_skipped' (re-offer a skipped version).",
          "default": "list"
        },
        "entity_ids": {
          "type": "list[str] | str | None",
          "description": "Update entity_id(s) to act on. 'get' takes exactly one; skip/clear_skipped require at least one; for install, mutually exclusive with categories.",
          "default": null
        },
        "categories": {
          "type": "list[str] | str | None",
          "description": "For install: apply every pending update in these categories ('addons', 'hacs', 'devices', 'other'). As with the HA 'Update all' button, core/os/supervisor are excluded (target those individually via entity_ids) and skipped updates are never included.",
          "default": null
        },
        "include_skipped": {
          "type": "bool",
          "description": "For list: include updates that have been skipped.",
          "default": false
        },
        "include_release_notes": {
          "type": "bool",
          "description": "For get on a Core update entity: fetch multi-version release notes and breaking changes for all versions between installed and latest. Adds breaking_changes, multi_version_release_notes, and installed_integrations to the response.",
          "default": false
        },
        "backup": {
          "type": "bool",
          "description": "For install: create a backup before installing where the update entity supports it (apps (add-ons)).",
          "default": false
        }
      }
    },
    "annotations": {
      "destructiveHint": true,
      "openWorldHint": true
    },
    "tags": [
      "System"
    ],
    "source_file": "tools_updates.py"
  },
  {
    "name": "ha_reload_core",
    "title": "Reload Core Components",
    "description": "Execute a Home Assistant configuration reload without a full restart.\n\nReloads specific configuration components so changes take effect without\nrestarting Home Assistant — much faster than a full restart. Use it after\nediting automation/script YAML, adding YAML helpers, or changing\ncustomize.yaml or themes.\n\nEXAMPLES:\n- ha_reload_core(target=\"automations\")\n- ha_reload_core(entry_id=\"abc123\")",
    "inputSchema": {
      "properties": {
        "target": {
          "type": "str",
          "description": "What to reload: 'all', 'automations', 'scripts', 'scenes', 'groups', 'input_booleans', 'input_numbers', 'input_texts', 'input_selects', 'input_datetimes', 'input_buttons', 'timers', 'templates', 'persons', 'zones', 'core' (customize, packages) or 'themes'.",
          "default": "all"
        },
        "entry_id": {
          "type": "str | None",
          "description": "Reload a SINGLE config entry (one integration instance) instead of sweeping subsystems — the fast path after editing a custom component on disk. Pass it alone (leave target at 'all'); combining it with an explicit target is a validation error. Find the id via ha_get_integration.",
          "default": null
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "System"
    ],
    "source_file": "tools_system.py"
  },
  {
    "name": "ha_restart",
    "title": "Restart Home Assistant",
    "description": "Execute a Home Assistant restart.\n\nWARNING: restarts the entire Home Assistant instance; all automations\nare unavailable during the restart, which typically takes 1-5 minutes.\nConfig is validated automatically before the restart proceeds (to\npre-check, call ha_get_system_health(include=\"config_check\")). For\nconfiguration changes, consider ha_reload_core() instead, which reloads\nspecific components without a full restart.\n\nEXAMPLE: ha_restart(confirm=True)",
    "inputSchema": {
      "properties": {
        "confirm": {
          "type": "bool",
          "description": "Must be True to confirm the restart.",
          "default": false
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "System"
    ],
    "source_file": "tools_system.py"
  },
  {
    "name": "ha_get_todo",
    "title": "Get Todo",
    "description": "Get todo lists or items - list all todo lists or get items from a specific list.\n\nWithout an entity_id: lists every entity in the 'todo' domain (shopping\nlists and other todo-type integrations) with entity_id, friendly_name\nand state (number of incomplete items or current status).\n\nWith an entity_id: returns that list's items — uid, summary, status\n(needs_action or completed), description, and due date where supported\n— optionally filtered by status (None returns all).\n\nEXAMPLES:\n- List all todo lists: ha_get_todo()\n- Get incomplete items: ha_get_todo(\"todo.shopping_list\", status=\"needs_action\")",
    "inputSchema": {
      "properties": {
        "entity_id": {
          "type": "str | None",
          "description": "Todo list entity ID (e.g., 'todo.shopping_list').",
          "default": null
        },
        "status": {
          "type": "Literal['needs_action', 'completed'] | None",
          "description": "Filter items by status: 'needs_action' for incomplete, 'completed' for done. Only applies when entity_id is provided.",
          "default": null
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Todo Lists"
    ],
    "source_file": "tools_todo.py"
  },
  {
    "name": "ha_remove_todo_item",
    "title": "Remove Todo Item",
    "description": "Remove an item from a Home Assistant todo list.\n\nPermanently deletes an item from the specified todo list. To mark it\ncompleted instead, use ha_set_todo_item() with status=\"completed\".\n\nEXAMPLE: ha_remove_todo_item(\"todo.shopping_list\", \"Buy milk\")",
    "inputSchema": {
      "properties": {
        "entity_id": {
          "type": "str",
          "description": "Todo list entity ID (e.g., 'todo.shopping_list')"
        },
        "item": {
          "type": "str",
          "description": "Item to remove - can be the item UID (from ha_get_todo) or the exact item summary/name"
        }
      },
      "required": [
        "entity_id",
        "item"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": true
    },
    "tags": [
      "Todo Lists"
    ],
    "source_file": "tools_todo.py"
  },
  {
    "name": "ha_set_todo_item",
    "title": "Set Todo Item",
    "description": "Create or update a todo item in Home Assistant.\n\nWithout `item` (create mode): creates a new item; summary is required.\nWith `item` (update mode): updates that item; at least one of rename,\nstatus, description, due_date or due_datetime is required.\n\nEXAMPLES:\n- Add item: ha_set_todo_item(\"todo.tasks\", summary=\"Pay bills\", due_date=\"2024-12-31\")\n- Complete item: ha_set_todo_item(\"todo.shopping_list\", item=\"Buy milk\", status=\"completed\")\n- Rename item: ha_set_todo_item(\"todo.tasks\", item=\"Old task\", rename=\"New task name\")\n\nNot all todo integrations support all features: the Shopping List\nintegration supports summary and status but not descriptions or due dates.",
    "inputSchema": {
      "properties": {
        "entity_id": {
          "type": "str",
          "description": "Todo list entity ID (e.g., 'todo.shopping_list')"
        },
        "summary": {
          "type": "str | None",
          "description": "Item text/name. Ignored in update mode — use 'rename' to change the item name.",
          "default": null
        },
        "item": {
          "type": "str | None",
          "description": "Existing item to update - can be the item UID or the exact item summary/name.",
          "default": null
        },
        "status": {
          "type": "Literal['needs_action', 'completed'] | None",
          "description": "Item status: 'completed' to mark done, 'needs_action' to mark incomplete. Only used in update mode.",
          "default": null
        },
        "description": {
          "type": "str | None",
          "description": "Detailed description for the item",
          "default": null
        },
        "due_date": {
          "type": "str | None",
          "description": "Due date in YYYY-MM-DD format (e.g., '2024-12-25')",
          "default": null
        },
        "due_datetime": {
          "type": "str | None",
          "description": "Due datetime in ISO format (e.g., '2024-12-25T14:00:00'). Overrides due_date if both provided.",
          "default": null
        },
        "rename": {
          "type": "str | None",
          "description": "New name/summary for an existing item. Only used in update mode.",
          "default": null
        }
      },
      "required": [
        "entity_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Todo Lists"
    ],
    "source_file": "tools_todo.py"
  },
  {
    "name": "ha_eval_template",
    "title": "Evaluate Template",
    "description": "Execute a Jinja2 template render, or an automation condition check, in Home Assistant.\n\nRenders templates with Home Assistant's template engine (all states,\nfunctions and filters available), or checks a condition block the way\nan automation would and returns true/false.\n\nWhen to use: any one-shot answer DERIVED from current HA state (an\naverage across sensors, a count of entities matching a condition, a\nrendered message with live values), and testing a template or condition\nbefore embedding it in an automation. One render beats fetching N states\nand doing the math yourself.\n\nWhen NOT to use: a plain single-entity value is ha_get_state /\nha_search. Templates in automation `condition:` / `trigger:` positions\nor templated action names: prefer native constructs, which are validated\nat config load, where a template only errors at runtime and a non-truthy\nrender is silently false — see ha_get_skill_guide for the anti-pattern\nlist. Test the native condition here with `condition`.\n\nResults: a render that works but hits something suspect (an undefined\nvariable, a missing attribute) still returns its result with the\nmessages in `warnings`; strict=true makes those hard errors. A failed\nrender returns Home Assistant's error text and, with the ha_mcp_tools\ncomponent installed, the template `line` and `source_line` it failed on\nwhen the error names one.\n\nEXAMPLES:\n- ha_eval_template(template=\"{{ states.light | selectattr('state', 'eq', 'on') | list | count }}\")\n- ha_eval_template(template=\"Hello {{ name }}\", variables={\"name\": \"Alice\"})\n- ha_eval_template(condition={\"condition\": \"numeric_state\", \"entity_id\": \"sensor.temperature\", \"above\": 20})",
    "inputSchema": {
      "properties": {
        "template": {
          "type": "str | None",
          "description": "Jinja2 template to render. Omit when using 'condition'.",
          "default": null
        },
        "condition": {
          "type": "dict[str, Any] | None",
          "description": "Automation condition to test instead of a template, e.g. {'condition': 'numeric_state', 'entity_id': 'sensor.temp', 'above': 20}. Test several at once with {'condition': 'and', 'conditions': [...]}.",
          "default": null
        },
        "variables": {
          "type": "dict[str, Any] | None",
          "description": "Variables available to the template or condition, e.g. sample trigger data: {'trigger': {'to_state': {'state': 'on'}}}",
          "default": null
        },
        "strict": {
          "type": "bool",
          "description": "Template only: fail on undefined variables instead of rendering them as empty (reported in warnings when report_errors is on)",
          "default": false
        },
        "timeout": {
          "type": "int",
          "constraints": {
            "ge": 1,
            "le": 60
          },
          "description": "Template only: maximum render time in seconds",
          "default": 3
        },
        "report_errors": {
          "type": "bool",
          "description": "Template only: have Home Assistant report render errors and warnings. With false, a failed render is only written to Home Assistant's log.",
          "default": true
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Utilities"
    ],
    "source_file": "tools_utility.py"
  },
  {
    "name": "ha_report_issue",
    "title": "Report Issue or Feedback",
    "description": "Get diagnostic information and templates for filing issue reports or feedback.\n\nGenerates templates for two report types, and you MUST pick from the\nconversation context:\n- RUNTIME BUG: the user reports an error, failure, or unexpected\n  behavior; a tool returned an error or incorrect result; something in\n  ha-mcp is broken.\n- AGENT BEHAVIOR FEEDBACK: the user says you used the wrong tool,\n  suggests a more efficient workflow, or reports your inefficiency or\n  mistakes.\nIf unclear which, ask: \"Are you reporting a bug in ha-mcp, or providing\nfeedback on how I used the tools?\"\n\nOUTPUT: both templates plus diagnostic data. The full response is LARGE\n(the captured logs appear in the raw log keys AND inside each template)\n— pass fields=... to fetch only the keys you need once you know which\ntemplate applies. Key fields: `runtime_bug_template` /\n`agent_behavior_template`; `recent_logs`, `startup_logs` (captured\nha-mcp tool/server log entries); `addon_logs` (app container\nstdout/stderr, HA app installs only, empty string otherwise);\n`core_error_log` (Home Assistant error log over REST, carries auth /\nintegration errors that don't show in addon_logs); `missing_tool_hint`\n— check this FIRST when the report is about a missing/unavailable tool:\na stale client tool list (not a bug) is the usual cause and refreshing\nthe MCP connection is the fix; `suggested_title`,\n`duplicate_check_urls`, `anonymization_guide`.",
    "inputSchema": {
      "properties": {
        "tool_call_count": {
          "type": "int",
          "default": 10,
          "constraints": {
            "ge": 1,
            "le": 16
          },
          "description": "Number of ha_* tool calls made since the issue started; determines how many log entries to include."
        },
        "fields": {
          "type": "str | list[str] | None",
          "default": null,
          "description": "Return only the specified top-level response keys. None = full response. Typical for a runtime bug: 'runtime_bug_template,suggested_title,runtime_bug_submit_url,duplicate_check_urls,anonymization_guide,missing_tool_hint,known_client_issues_hint,instructions'; for agent feedback swap in agent_behavior_template and agent_behavior_submit_url. The templates already embed the relevant logs, so the raw log keys are only needed for your own analysis. Available keys: diagnostic_info, recent_logs, startup_logs, addon_logs, core_error_log, log_count, startup_log_count, formatted_report, runtime_bug_template, agent_behavior_template, anonymization_guide, suggested_title, runtime_bug_submit_url, agent_behavior_submit_url, duplicate_check_urls, missing_tool_hint, known_client_issues_hint, instructions."
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Utilities"
    ],
    "source_file": "tools_bug_report.py"
  },
  {
    "name": "ha_get_zone",
    "title": "Get Zone",
    "description": "Get zone information - list all zones or get details for a specific one.\n\nWithout a zone_id: Lists all Home Assistant zones with their coordinates and radius.\nWith a zone_id: Returns detailed configuration for a specific zone.\n\nEXAMPLES:\n- List all zones: ha_get_zone()\n- Get specific zone: ha_get_zone(zone_id=\"abc123\")\n\nWith the ha_mcp_tools custom component installed, YAML-defined zones —\nincluding the auto-synthesized 'home' zone — are included and marked\n``editable=false`` / ``source=\"yaml\"`` (storage zones created via UI/API\nare ``source=\"storage\"``). Without the component, only storage zones are\nlisted and YAML-defined zones such as 'home' will not appear.",
    "inputSchema": {
      "properties": {
        "zone_id": {
          "type": "str | None",
          "description": "Zone ID to get details for (from ha_get_zone() list).",
          "default": null
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "idempotentHint": true,
      "readOnlyHint": true
    },
    "tags": [
      "Zones"
    ],
    "source_file": "tools_zones.py"
  },
  {
    "name": "ha_remove_zone",
    "title": "Remove Zone",
    "description": "Remove a Home Assistant zone.\n\nEXAMPLES:\n- Remove zone: ha_remove_zone(\"abc123\")\n\n**WARNING:** Removing a zone used in automations may cause those automations to fail.\nUse ha_get_zone() to find the zone_id for the zone you want to remove.\n\n**NOTE:** The 'home' zone cannot be removed as it is typically defined in configuration.yaml.",
    "inputSchema": {
      "properties": {
        "zone_id": {
          "type": "str",
          "description": "Zone ID to remove"
        }
      },
      "required": [
        "zone_id"
      ]
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true,
      "idempotentHint": true
    },
    "tags": [
      "Zones"
    ],
    "source_file": "tools_zones.py"
  },
  {
    "name": "ha_set_zone",
    "title": "Set Zone",
    "description": "Create or update a Home Assistant zone.\n\nOmit zone_id to create a new zone (name, latitude, longitude required).\nProvide zone_id to update an existing zone (only specified fields change).\nThe 'home' zone is typically defined in YAML and cannot be modified here.\n\nEXAMPLES:\n- Create: ha_set_zone(name=\"Office\", latitude=40.7128, longitude=-74.0060, radius=150, icon=\"mdi:briefcase\")\n- Update: ha_set_zone(zone_id=\"abc123\", radius=200)",
    "inputSchema": {
      "properties": {
        "name": {
          "type": "str | None",
          "description": "Display name for the zone",
          "default": null
        },
        "latitude": {
          "type": "float | None",
          "description": "Latitude coordinate of the zone center",
          "default": null
        },
        "longitude": {
          "type": "float | None",
          "description": "Longitude coordinate of the zone center",
          "default": null
        },
        "zone_id": {
          "type": "str | None",
          "description": "Zone ID to update (use ha_get_zone to find IDs)",
          "default": null
        },
        "radius": {
          "type": "float | None",
          "description": "Radius of the zone in meters (must be > 0, defaults to 100 on create)",
          "default": null
        },
        "icon": {
          "type": "str | None",
          "description": "Material Design Icon (e.g., 'mdi:briefcase', 'mdi:school')",
          "default": null
        },
        "passive": {
          "type": "bool | None",
          "description": "Passive mode - if True, zone will not trigger enter/exit automations (defaults to False on create)",
          "default": null
        }
      }
    },
    "annotations": {
      "openWorldHint": false,
      "destructiveHint": true
    },
    "tags": [
      "Zones"
    ],
    "source_file": "tools_zones.py"
  }
]
